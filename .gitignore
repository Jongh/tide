# tide 상태 파일 (로컬 전용 — .tide/phase·.tide/debug-session만 무시, .tide/deps는 커밋)
.tide/phase
.tide/debug-session

# MkDocs 빌드 출력 (docs 사이트)
site/_build/

# Claude Code 로컬 설정 (머신 전용)
.claude/settings.local.json
# 하니스가 만드는 CLAUDE.md의 바이트 동일 사본 — 저장소의 어떤 파일도 참조하지 않는다.
# 커밋하면 함정 목록이 두 벌이 되어 한쪽이 조용히 낡는다(단일 원본은 CLAUDE.md).
AGENTS.md

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
