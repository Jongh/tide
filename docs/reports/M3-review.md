# M3 리뷰보고서 (review)

## 비판점

### 차단 (0건)

없음.

### 권장 (0건)

없음.

### 사소 (3건 — 수용)

1. **현재 대화 세션의 구 호출명 잔존** — 진행 중이던 세션은 시작 시점의 스킬
   스냅샷(`/tide:tide-*`)을 유지한다. 새 세션부터 새 호출명이 적용됨을 실증했으므로
   저장소 문제가 아닌 일시 현상. 세션 재시작으로 해소.
2. **README CHANGELOG 절의 구 표기** — 과거 릴리즈 노트(v0.2.0·v0.3.0)에 남은
   `/tide-*` 표기는 이력으로 보존한다. 완료 기준 3의 "CHANGELOG 제외"를 README 내
   CHANGELOG 절(동일 노트의 미러)에도 적용하는 해석을 여기 명시해 둔다.
3. **수동 복사 설치의 일반명 충돌 가능성** — `~/.claude/skills/`로 복사하면
   `milestone`·`status` 같은 일반명 스킬이 되어 충돌 여지가 있다. README가 비권장
   경로로 명시하고 충돌 위험을 경고하므로 수용.

## 수정 내용

- 없음 — 리뷰 중 직접 수정한 항목 없음. impl 단계의 grep 검수가 누락(리뷰 템플릿의
  안내 2줄)을 이미 잡아 수정했고, 리뷰에서 추가 결함은 발견되지 않았다.

## 검증

- **잔존 참조 재검수**: 살아있는 문서·스킬·hook에서 `commands/`·`templates/`·
  `${CLAUDE_PLUGIN_ROOT}` grep — 검출 전부 정당(마이그레이션 노트의 과거 경로 설명,
  과거 릴리즈 노트, hook 등록의 올바른 변수 사용)함을 분류 확인.
- **스킬 노출**: 새 세션에서 `tide:` 네임스페이스 6종(kickoff/milestone/impl/review/
  release/status) 전부 확인.
- impl 단계의 새 세션 실증 4종(status 동작, 구 호출명 Unknown, `${CLAUDE_SKILL_DIR}`
  치환+템플릿 읽기, 가드 신규 메시지 차단)은 이후 변경이 없어 유효.
- 잔여 리스크: kickoff/impl/review/release 스킬의 본문 실행은 이번 사이클의 남은
  단계(release)와 다음 사이클에서 자연 검증된다 — 로드·치환 메커니즘은 동일하므로
  낮은 리스크. T06의 GitHub 마켓플레이스 복원은 의도적으로 릴리즈 후로 연기된
  상태다(사유: 지금 복원하면 설치본이 v0.3.0 구 호출명으로 회귀).

## 릴리즈 판정

**가능** — 추천 버전: **v0.4.0 (minor)**

- M3 완료 기준 6개 전부 충족: 새 호출명 동작+구 호출명 제거(실증), 템플릿 치환(실증),
  grep 무검출(이력·경로 예외 분류), 가드 신규 메시지 차단(실증), 패키지 위생 수용
  결정 기록, 마이그레이션 노트 반영
- 차단·권장 이슈 0건, 사소 3건은 수용
- 0.x 단계의 사용자 대면 호출명 개편 + 구조 전환 — minor 적합

## 다음 단계

- `/tide:release v0.4.0`
- 릴리즈 후 권장:
  1. `claude plugin marketplace remove tide` → `add Jongh/tide` → `install tide@tide`
     (GitHub 기준 복원 — 현재는 임시 워크트리 로컬 경로)
  2. 본 저장소(`D:\Code\private\tide`)에서 `git pull`
  3. 새 세션에서 `/tide:status` 1회 호출로 복원 확인
