# tide 회고 (retro)

> 누적 마일스톤·보고서를 가로질러 본 회고. 회고 시점마다 이 문서 **최상단에** 새 섹션을
> 쌓는다(기존 섹션 보존). `/tide:retro`가 생성·갱신하는 읽기 전용 산출물.

## 2026-06-11 (v0.6.0 시점) — M6 도그푸딩 드라이런

### 집계 범위

- 마일스톤: M1~M5 (완료). M6은 진행 중이며 본 회고가 그 산출물 — 입력에서 제외.
- 보고서: impl 5건(M1~M5) + review 5건(M1~M5).
- 릴리즈: v0.2.0 ~ v0.6.0 (5사이클).

### 반복된 문제·이슈 군집

- **설치 캐시 ↔ 원본 괴리: 실호출 검증의 release-후 연기** (등장: M2·M3·M4·M5) —
  편집 대상은 저장소 원본 `skills/`인데 설치된 플러그인 캐시는 release+재설치 전까지 구
  스킬을 쓴다. 그래서 "재설치한 새 세션에서 실제 호출 검증"이 매 사이클 release 후
  도그푸딩으로 미뤄지는 **구조적 반복**. 처리 추이: 자기적용·정적 검증으로 impl 단계를
  대체하고 release 후 실증 — M5에서 자동 발견(매니페스트 무수정)까지 실증돼 패턴이 안정화.
- **문서 ↔ 실동작 동기화 누수** (등장: M2·M3·M4) — 버전 파일 목록 누락(M2), README
  CHANGELOG 구 표기(M3), kickoff `description`이 구 동작만 서술(M4). 처리: 발견 즉시 수정
  또는 "이력 보존" 명시. frontmatter·표 같은 사용자 대면 표면에서 반복 누수.
- **마켓플레이스가 임시 워크트리 경로를 가리킴** (등장: M2·M3) — 도그푸딩 설치 source가
  임시 워크트리 절대경로라 release 후 `remove → add Jongh/tide → install` 복원 필요.
  처리: 운영 절차로 각 release 후 권장에 편입, GitHub 기준 복원은 v0.4.0 후 반영.
- **Windows hook 실행 전제** (등장: M1·M2) — hook 등록 방식의 조용한 무력화(M1, 상대경로로
  수정), ps1 인코딩(M1, 수용), sh 없는 환경(M2, README 전제 명시). 처리: 상대경로 픽스 +
  문서화로 수렴.

### 수용된 트레이드오프

- **tide-guard 보수적 차단의 false positive** (M1-review 사소4) — 명령 문자열 전체를 스캔해
  `echo "git push"`도 차단될 수 있으나 해당 단계에서 그런 명령이 드물어 수용.
- **`idle`에서도 git 차단** (M1-review 권장2) — 의도된 철학; 해제는 `.tide/phase` 삭제로
  문서화해 보완.
- **수동 복사 설치의 일반명 충돌** (M3-review 사소3) — 비권장 경로 경고로 수용.
- **진행/신규 판별이 정성 휴리스틱** (M4-review 사소2) — 양방향 오판 실패가 완만(재실행
  복구)하여 수용.
- **패키지 위생(불필요 파일 포함)** (M2-review 권장3, M3) — 공식 제외 수단 부재로 수용
  종결, 수단 생기면 재검토.
- **cycle "병렬"은 규칙 해석일 뿐 실제 동시성 없음** (M5-review 사소2) — impl이 단일
  실행자라 deps 위상만 보장; 진짜 병렬은 별도 마일스톤 후보.

### 후속 항목 추적

| 후속 항목 | 제안 출처 | 상태 | 반영 위치 |
|---|---|---|---|
| 이 저장소 hook 셀프 설치(도그푸딩 완성) | M1-impl#1, M1-review#3 | 반영 | M2 플러그인 전환(hook 자동 제공) |
| 내장본 이중 관리 해소 | M1-impl#2 | 반영 | M2·M3 `hooks/` 단일화 |
| 구버전 사본 섀도잉 마이그레이션 안내 | M2-review#1 | 반영 | M2 README 노트 + v0.3.0 `.claude/commands` 삭제 |
| GitHub 마켓플레이스 복원 | M3-impl#1 | 반영 | v0.4.0 release 후 절차 |
| cycle 무인자 시작점 공백 | M5-review#1 | 반영 | M5 리뷰 중 수정 |
| sh 없는 Windows 환경 명시 | M2-impl#2 | 반영(문서) | README 전제 |
| 새 세션 재설치 실호출 검증 | M3·M4·M5 | 반복(구조적) | 매 release 후 도그푸딩 — 이번 M6 도그푸딩도 그 일환 |
| **차단 메시지 한국어화** | M1-impl#3 | **미반영** | — (ps1 BOM 규약 미도입) |
| **패키지 위생(불필요 파일 제외)** | M2-review#3, M3 | **미반영(수용 종결)** | — (공식 수단 대기) |
| **진짜 병렬 실행 수단** | M5-impl#2, M5-review | **미반영** | — (별도 마일스톤 후보) |

### 릴리즈 판정·버전 추이

- M1: 가능 → **v0.2.0** (minor) — tide-guard hook + `/tide-status` + 전제조건/프리플라이트
- M2: 가능 → **v0.3.0** (minor) — Claude Code 플러그인 전환(커맨드+hook 단일 설치)
- M3: 가능 → **v0.4.0** (minor) — 스킬 구조 전환 + 호출명 개편(`/tide:status`)
- M4: 가능 → **v0.5.0** (minor) — 브라운필드 킥오프 + `project-context.md`
- M5: 가능 → **v0.6.0** (minor) — `/tide:cycle` 오케스트레이션
- 추이: 5사이클 **연속 "가능", 전부 minor**(0.x 누적). 차단 이슈는 등장 시 리뷰 중 즉시
  수정(M1 1건뿐) — 리뷰 게이트가 작동.

### 회고 메모

1. **미반영 후속 우선순위** — ① 진짜 병렬 실행 수단(cycle 실효성에 직결), ② 차단 메시지
   한국어화(저비용·사용자 대면), ③ 패키지 위생(외부 수단 대기 — 통제 밖).
2. **구조적 반복: release-전 실호출 검증 불가** — 모든 마일스톤이 release 후 도그푸딩에
   의존. retro/cycle처럼 도그푸딩 절차를 사이클에 더 깊이 내장하는 방향 고려.
3. **문서-실동작 동기화는 반복 누수** — frontmatter `description`·커맨드 표·버전 파일
   목록·스킬 수 표기가 사이클마다 갱신 누락 위험. 릴리즈 체크리스트화 가치.
