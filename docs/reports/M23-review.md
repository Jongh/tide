# M23 리뷰보고서 (review)

## 개요

M23(정리 사이클 — 참조 구현 공유 라이브러리화 + 회고 아카이브)을 리뷰했다. 회고 교훈 #1("가산·정리
사이클은 단일 비판 리뷰")에 따라 **집중 비판 리뷰**로 수행하되, 교훈 #2("테스트 통과 ≠ 정합")에 따라
핵심 위험을 직접 확인했다 — 특히 ② 리팩터의 **우연한 green**(로컬 정의가 라이브러리를 섀도잉해 dedup이
실제로 일어나지 않았는데 통과) 가능성과, ③ **테스트로 덮이지 않는 회고 문서**의 무결성. 후자에서 v2.1.0
섹션 헤더 누락을 적발해 **리뷰에서 직접 수정**했다.

**차단 결함 0. 최종 판정: 가능 — 추천 v2.2.1 (patch).**

## 비판점

### 차단 (0건)

없음.

### 권장 (1건 — 리뷰에서 수정 완료)

1. **`retro.md`의 v2.1.0 섹션 `## ` 헤더 누락** — git HEAD의 `retro.md`는 v2.1.0 섹션에 `## 2026-06-12
   (v2.1.0 시점) …` 헤더를 가졌으나, 현재 working tree에선 그 헤더가 사라져 v2.1.0 섹션이 곧바로
   블록쿼트로 시작했다(headerless). 직전 `/tide:retro` 프리펜드 단계에서 v2.2.0 섹션을 끼우며 v2.1.0
   헤더를 함께 소비하고 되돌려놓지 않은 것이 원인이고, M23-T04(아카이브 분리)는 받은 headerless 상태를
   그대로 보존했다. **영향**: 회고 문서 섹션 구조의 fidelity 저하(테스트로 안 잡힘 — 회고는 라이브
   하니스 대상이 아님). **수정**: HEAD의 v2.1.0 헤더를 원문 그대로 복원(현 line 89). v2.0.0·v1.6.0
   섹션은 HEAD에서도 `## ` 헤더가 없던 선재 상태이므로 손대지 않음(sn3).

### 사소 (3건 — 수용)

- **[sn1] 하니스 자기완결성 약화** (출처: M23-impl 후속1) — 하니스가 이제 `tests/lib/`를 source한다.
  **수용 사유(설계 결정)**: 같은 `tests/` 트리 내 상대경로 source라 `sh tests/X/run.sh`·`& tests\X\run.ps1`로
  여전히 실행되고(트리 내 자기완결), 규약 변경 시 1곳만 수정하는 단일 원본 이득이 파일별 자기완결성보다
  크다. 이 사이클의 본래 목적(이중성 축소)과 정합.
- **[sn2] `read_deps`/`ReadDeps` 미추출** — `toposort`가 호출하는 `read_deps`는 라이브러리에 두지 않고
  fleet·fleet-cycle 하니스 로컬로 남겼다. **수용 사유**: 이번 범위는 `discover`/`toposort`. 함수는 호출
  시점 해석이라 source 후에도 정상 동작(검증됨). 추가 단일 원본화는 저위험 향후 정리 후보.
- **[sn3] v2.0.0·v1.6.0 회고 섹션 headerless 유지** — 이 두 섹션은 HEAD에서도 `## ` 헤더 없이 블록쿼트로
  이어진 선재 구조다. M23과 무관하므로 변경하지 않았다(권장1과 달리 회귀가 아님).

## 수정 내용

- **이슈 1 (v2.1.0 헤더 누락)**: `docs/reports/retro.md`의 v2.2.0 메모 #4 직후에 HEAD 원문과 동일한
  `## 2026-06-12 (v2.1.0 시점) — M21 반영 …` 헤더를 복원했다. 복원 후 `retro.md`의 `## ` 헤더는
  v2.2.0(line 6) + v2.1.0(line 89) 2개로, 아카이브된 v1.4.0↓와 분리되어 일관된다.

## 검증

**② 참조 구현 단일 원본화 — 우연한 green 배제(교훈 #2 직접 확인)**:
- **섀도잉 아님(진짜 dedup)**: 4개 하니스(× 2셸)의 로컬 `is_tide_repo`/`discover`/`toposort` 정의 수 =
  **전부 0**(source 라인 + 브레드크럼 주석만 잔존). 로컬 정의가 라이브러리를 가리는 일 없이 실제로 제거됨.
- **라이브러리 단일 정의·클린 프래그먼트**: 각 함수 정의 라이브러리당 **정확히 1개**, 라이브러리에
  top-level 부수효과(`set -u`·픽스처·실행) **0**(순수 함수 정의).
- **바이트 동등(verbatim)**: 라이브러리 `is_tide_repo`+`discover`를 HEAD `tests/fleet/run.sh` 원본과 `diff`
  → **유일 차이는 흡수한 주석**(`# 숨김(dot) … 무시(.tide-fleet 포함)`)뿐, 로직 0 변경.
- **로직 동등성 실증**: fleet-cycle이 자기 압축형 `toposort` 대신 정본(fleet판)으로 **PASS=23** 통과 →
  두 구현이 로직 동등이었음이 입증(T01의 미검증 가정 해소).

**③ 회고 아카이브 — behavior-neutral·바이트 보존**:
- `retro-archive.md`의 v1.4.0↓ 본문을 HEAD `retro.md` 동일 구간과 `diff` → **741행 바이트 동일**.
- `retro.md` 1060→320행 축소 + 아카이브 포인터 1줄. `/tide:retro`는 `retro.md`를 **입력에서 제외**하므로
  (skills/retro/SKILL.md "수집" 절 재확인) 분할이 집계에 무영향. SKILL 노트는 동작 불변(아카이브 존재 안내만).

**라이브 실증(양 셸, baseline 동일)**:

| 하니스 | sh | ps1 |
|---|---|---|
| discover · fleet · fleet-cycle · fleet-verify · multi-repo | 19·41·23·29·10 / FAIL 0 | 19·41·23·29·10 / FAIL 0 |

- 모든 러너 exit 0, 전환 전 baseline과 PASS 수 동일(동작 보존). M22 가드(discover B1/B2/B3) 불변.
- **인코딩 규율**: `tests/lib/*.ps1` 비ASCII **0**, 신규 라이브러리 4 + 수정 하니스 8 모두 **BOM 없음**.

**완료 기준 6개 충족**: ①tests/lib 단일 정의·로컬 중복 제거 ②전 하니스 baseline 동일 통과·fleet-cycle
로직 동등 실증 ③ps1 ASCII·무BOM ④retro 아카이브 바이트 보존·retro.md 축소·`/tide:retro` 불변 ⑤skills/
conventions/project-context 동기화 ⑥비파괴(2.0 stable 계약·M22 가드 불변).

## 릴리즈 판정

**가능** — 추천 **v2.2.1 (patch)**.

근거: 차단 0. 권장 1건(v2.1.0 헤더 누락)은 **리뷰에서 직접 복원**, 사소 3건은 수용. M23은 순수 내부
정리 — 새 커맨드·능력·계약 변화 0이고, 테스트 참조 구현은 추출 후에도 **양 셸 baseline 동일 통과**(동작
보존, verbatim 확인), 회고 분할은 behavior-neutral(바이트 보존·`/tide:retro` 불변)이다. 새 파일(`tests/lib/`·
`retro-archive.md`)은 내부 자산이라 minor 아닌 **patch**가 영향도에 맞다(M15 v1.2.1 "출력 정합" 정리 patch
선례와 동형). 2.0 stable 계약(11종 커맨드·오케스트레이션 규약·`.tide/phase`/tide-guard·보고서·마일스톤
형식)·M22 단일 원본 가드 전부 불변. M22 회고가 부각한 참조 구현 이중성 군집을 *축소*하고 최장수 이월
후속(③)을 종결했다.

## 다음 단계

- `.tide/phase`를 `idle`로 되돌리고 **`/tide:release v2.2.1`**을 사용자에게 넘긴다(cycle은 release 제외 —
  git은 사용자 몫).
- 릴리즈 후(비차단): sn2 — `read_deps`/`ReadDeps`의 추가 단일 원본화는 저위험 향후 정리 후보. mkdocs 빌드
  출력 검증은 환경-이월 지속(다음 release 프리플라이트도 동일 — CI/라이브 회수).
