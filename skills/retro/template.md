# tide 회고 (retro)

> 누적 마일스톤·보고서를 가로질러 본 회고. 회고 시점마다 이 문서 **최상단에** 새 섹션을
> 쌓는다(기존 섹션 보존). `/tide:retro`가 생성·갱신하는 읽기 전용 산출물.

## {기준 시점 — 예: 2026-06-11} (v{X}.{Y}.{Z} 시점){; 메모가 있으면 한 줄}

{제목 꼬리의 `(v{X}.{Y}.{Z} 시점)` 은 예시가 아니라 **요구**다 — 이 회고가 선 기준 버전을 적는다.
단일 원본은 `docs/conventions.md`의 "회고 후속 항목의 소비" 절}

### 집계 범위

{읽은 마일스톤 범위 M{a}~M{b}, impl/review 보고서 수, 릴리즈 범위 v{x}~v{y}}

### 반복된 문제·이슈 군집

- **{주제}** (등장: {M번호들}) — {무엇이 반복됐는지, 처리 추이: 수정/수용/이월}

### 수용된 트레이드오프

- **{항목}** (출처: {M{N}-review/impl}) — {왜 수용했는지}

### 후속 항목 추적

<!-- retro-followups:start -->

| 후속 항목 | 제안 출처 | 상태 | 반영 위치 |
|---|---|---|---|
| `rf-{NN}` {항목} | {M{N}-impl/review} | {상태 — 값 집합의 단일 선언처는 규약이다} | {반영된 마일스톤 또는 —} |

<!-- retro-followups:end -->

{위 마커 두 줄은 지우지 않는다 — `/tide:milestone`이 이 표를 찾는 창이고, 상태 값 집합·읽는 법의
단일 원본은 `docs/conventions.md`의 "회고 후속 항목의 소비" 절이다}

{`항목` 셀 **머리**의 백틱 토큰 `rf-{NN}` 이 **행 키**다 — 행을 **처음 만들 때 발급**하고 **다시 매기지
않는다**(번호는 순서가 아니라 이름이다). 직전 섹션에서 이어받은 행은 **그 키를 그대로** 가져오고, 새 행만
「기존 표의 최대 번호 + 1」로 받는다 — 제목을 다듬어도 키는 불변이다. 키를 **열로 만들지 않는** 이유는 열이
늘면 상태 열의 자리가 밀리기 때문이다(`retro-row-claimed`). 마일스톤이 선언한 키의 행은 **지우지 않고**,
집었다고 선언됐으나 닫지 못한 행은 `미결`로 옮긴다.
단일 원본은 `docs/conventions.md`의 "회고 후속 항목의 소비" 절}

### 릴리즈 판정·버전 추이

- {M{N}: 판정({가능/불가}) → v{x}.{y}.{z} ({major/minor/patch}) — 한 줄 요약}

### 회고 메모

1. {다음 사이클로 가져갈 교훈·패턴 — 특히 미반영 후속의 우선순위}
