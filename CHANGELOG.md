# CHANGELOG (Older Releases)

최신 릴리즈는 [README.md](README.md#changelog) 를 참조하세요.

---

<!-- 아래 [start:notes]~[end:notes]는 사이트(site/docs/changelog.md)가 pymdownx.snippets로
     릴리즈 노트만 인클루드하기 위한 마커다. 위 제목·안내 줄은 사이트에 부적절해 제외된다.
     렌더에는 영향 없음. -->
<!-- --8<-- [start:notes] -->
### [v2.23.0]
- **impl이 자기 마일스톤의 완료 기준을 번호로 대조한다(M55)**: 이 저장소의 반환은 지배적으로
  *"안 한 일"* 이었는데 **되돌림 실측은 구조적으로 그것을 보지 못한다** — 무는 것이 «세운 것이
  사는가»이지 «세워야 할 것을 세웠는가»가 아니기 때문이다. 그래서 **두 사이클 연속 같은 자리에서
  반환됐다**(M53은 자기 측정표를, M54는 자기 완료 기준을 대조하지 않았다). 이제 impl 보고서가
  **「완료 기준 대조」를 필수 섹션으로** 갖고 기준 번호마다 **번호 · 판정 · 근거**를 한 행씩 적는다.
  **번호가 빠지거나 없는 번호를 적으면 기계가 붉힌다.**
- **기계가 무는 것과 사람의 자리를 같은 절에 적었다**: 무는 것은 **선언의 유일성 · 번호 집합의 일치
  (빠짐 0 · 유령 0) · 판정 값의 집합 소속**까지다. **묻지 않는 것 셋** — *"충족이 사실인가"* ·
  *"근거에 알맹이가 있는가"* · *"기준 자체가 방전 가능한 형태인가"* 는 리뷰와 사람의 영역이고
  **그 이상을 주장하지 않는다.** 판정 값 집합(`criteria-verdict:`)과 집행 시작 번호(`criteria-since:`)를
  규약이 선언하고 **소비자는 가리키기만 한다.**
- **소급하지 않는다**: 시작 번호 **미만**의 보고서 54건은 이 절이 없던 시절의 산출물이라 대상이
  아니다 — 소급하면 «사후에 채운 대조»가 되어 **거짓 정합**을 만든다. 경계가 장식이 아님을 경계
  통제가 실측으로 확인한다(시작 번호를 1로 낮추면 어긋난다).
- **세우려던 명제를 이 사이클이 자기 자신에게서 실측했다**: 완료 기준이 요구한 교란 열 가지를 실제로
  걸다가 **되돌림 열일곱 행이 열지 못한 결함**이 나왔다 — 판정 값 선언을 지우면 한 사본은 세고 다른
  사본은 **결과 줄에 닿기 전에 중단**했다. 두 사본에 기본값을 못박아 닫았고 고친 뒤 **264/5로
  일치**한다. *"기준을 번호로 대조하는 것은 되돌림과 다른 행위"* 라는 것이 그 자리에서 증명됐다.
- **리뷰가 문면을 실제 집행 범위로 좁혔다**: 반증 시도를 **서브에이전트로 디스패치**해 성립 **4건**을
  받았고(연속 폴백이 13에서 **0**으로 끊겼다), 그중 둘은 **문서가 집행력을 사실보다 넓게 주장**하던
  자리였다 — 복제 금지가 보는 파일·토큰의 범위, 그리고 못이 **파생 토큰에만** 박혔다는 사실. 둘 다
  실측치와 함께 고지로 적었고 **코드 층은 다음 사이클로 반환**했다(리뷰가 하니스를 고치면 impl의
  되돌림 표가 조용히 낡는다).
- **minor·집행 증가**: 집행은 `tests/discover` **254 → 269**로 늘고 지운 것이 없다. 되돌림 **17행**과
  교란 **10가지**가 **전건 양 사본 일치**이며, 적대 축에서 초록으로 지나간 하나는 **의도된 경계**라
  고지로 처분했다(근거 열의 알맹이는 기계가 묻지 않는다). **하니스 7종 × 네 실행 환경 = 28칸**이
  전부 FAIL 0이다. `skills/impl`이 바뀌어 사용자가 관찰할 변화가 있으나(보고서에 섹션이 하나 는다)
  **시작 번호 미만은 현행 그대로**이고 커맨드 12종 계약이 불변이라 가산 minor다.

### [v2.22.0]
- **적어 둔 것이 읽히게 했다(M54)**: `/tide:retro`는 커맨드 12종 중 **유일하게 산출물을 아무도 읽지
  않는 커맨드**였다 — 규약의 상태 확인 항목 선언에 `retro`가 없고, 스킬의 `retro` 언급은 *"읽기 전용
  원칙이 같다"* 는 비유 한 건뿐이었으며, `/tide:milestone`이 읽는 것에 회고가 없었다. 이제
  **`/tide:milestone`이 회고의 「후속 항목 추적」 표에서 아직 닫히지 않은 행을 읽어** 새 마일스톤이
  그중 무엇을 집는지 보고에 적는다(**advisory — 막지 않는다**). 상태 값 집합(`retro-status:`)과 표를
  여닫는 ASCII 마커(`retro-block:`)를 규약이 선언하고 **소비자는 가리키기만 한다.**
- **왜 이것이 문제였는지가 수치로 남는다**: 회고가 **세 사이클 연속 같은 항목을 1순위**로 적었는데
  한 건도 움직이지 않았고, 같은 구간에 `tests/discover`가 **43건 늘었는데 선존 파트(A~L)의 케이스 수는
  문자 그대로 불변**이었다(늘어난 것이 전부 새 파트였다). 성실함의 문제가 아니라 **읽는 쪽이 없었다.**
- **`E1`이 절반만 한 일을 마저 했다**: 그 줄기의 목표가 *"방향이 「직전 사이클이 남긴 후속 메모」가
  아니라 「선언된 줄기」에서 오게 한다"* 였는데 **전향 쪽(에픽)만 배선하고 후향 쪽(회고·이월 원장)은
  손대지 않았다.** `E1`에 **축 둘**을 세우고 완료 신호 셋을 더했으며, 열린 에픽의 본문을 사이클 중에
  고칠 수 있다는 규칙(그전에는 규약이 침묵했다)을 함께 세웠다.
- **쌓인 것을 전수로 처분했다**: 회고 표의 미처분 행이 **0**이 됐다 — `fix` 셋 · 에픽 승격 넷 ·
  명시 처분 다섯 · **반영(다른 축)** 하나. 그중 **`jq` 경로(14+사이클 이월)** 는 «미실측»이 아니라
  **«확인 부재»**였다: CI의 `posix` 잡이 모든 `run.sh`를 돌리고 있었고 **세 레그 전부** *jq 있음
  (자동 이스케이프 경로)* 로 도는 것이 릴리즈 로그에 그대로 있었다. **경로는 계속 돌고 있었고 아무도
  로그를 읽지 않았다.**
- **회고가 두 번 1순위로 올린 부채를 닫았다**: `G1`·`G4`의 판정에 **픽스처 통제**가 없어 «판정을
  망가뜨려도 붉지 않던» 자리를 메웠다(Part G 16 → 18). 되돌림 대상을 **「새로 늘린 케이스 번호 전부」**
  로 규약이 못박은 것도 같은 처분이다 — «새 축의 변이»로 목록을 짜면 **딸려 온 케이스가 빠진다.**
- **이 사이클도 두 사본이 두 번 갈렸고 둘 다 되돌림이 열었다**: ⑴ PowerShell이 배열 리터럴에서
  `문자열 + [char]`를 **세 원소로 쪼개** 픽스처가 5줄 파일이 됐다 ⑵ `RelPath`가 `$ROOT` **밖 경로**를
  무조건 잘라 트리 사본에서 죽었다 — **`sh` 사본에는 그 가드가 있었고 ps1에만 없었다.** 후자는
  **로컬 루트가 짧아서 영원히 보이지 않던 부류**이고 사본에서만 죽는다.
- **minor·집행 증가**: 집행은 `tests/discover` **241 → 254**로 늘고 지운 것이 없다. 되돌림 **16건 ×
  양 사본**에서 초록으로 돌아온 변이는 **의도된 한계 하나**뿐이고(규약이 그 고지 경로를 이번에 세웠다)
  **하니스 7종 × 네 실행 환경 = 28칸**이 전부 FAIL 0이다. `skills/` 셋이 바뀌어 사용자가 관찰할 변화가
  있으나 **회고를 쓰지 않는 저장소의 동작은 현행과 같고** 커맨드 12종 계약이 불변이라 가산 minor다.
### [v2.21.1]
- **되돌림이 묻는 방향이 한쪽이었다는 것을 재서 고쳤다(M53)**: 되돌림 실측에 **축이 둘**이 됐다 —
  `broken`(세운 것을 깨면 붉는가)과 **`adversarial`(같은 피해에 이르는 「다른 편집」이 초록으로 지나가는가)**.
  뒤 축은 무력화가 아니라 **지어내기**이고, 초록이면 **그 검사는 편집의 한 모양만 막고 있다**는 뜻이다.
- **근거는 전수 실측이다**: M41~M52의 재작업 **17라운드**가 낳은 차단 **25건** 중 **되돌림이 연 것은 0건**,
  **「새로 만든 편집」이 연 것이 16건(64%)**, 그리고 **25건 전부 review에서** 잡혔다 — impl이 자기 결함을
  차단으로 잡은 적이 한 번도 없다. 되돌림이 무력한 것이 아니라 **나머지 방향이 통째로 뒤 단계로
  미뤄져 있었다.** 한 사이클은 변이 38건을 돌렸는데 그중 적대 방향이 **0건**이었다.
- **성립한 적대 변이는 픽스처로 승격한다**: 초록으로 지나간 적대 변이를 고쳤으면 **그 고침이 집행에
  걸려 있어야** 한다. 일회성으로 끝내면 «판정을 망가뜨려도 붉지 않는» 상태가 남고 그것은 통제가 아니다 —
  이 사이클이 **자기 자신에게서 실측**했다(고친 뒤 그 고침을 되돌려도 초록이었고, 상시 통제를 세우고서야
  붉었다).
- **덮는 것과 못 덮는 것을 규약이 함께 적는다**: 기계가 무는 것은 **선언의 유일성과 각 축의 서술**까지다.
  **묻지 않는 것 셋**(변이가 실제로 돌았는가 · 서술에 알맹이가 있는가 · 정말 다른 경로인가)은 보고서와
  리뷰의 영역이며, 체크리스트의 **수 자체도 기계가 세지 않는다**는 사실까지 같은 자리에 적었다.
- **이 사이클은 자기 논지의 반대편에서 한 번 걸렸다**: *"검증을 앞당긴다"* 를 세우면서 **자기 측정표를
  출처와 대조하지 않아** 리뷰가 반환했다. 고치는 과정에서 **두 건이 더** 나왔고 비중이 60% → 64%로
  움직였다. **미룬 것이 미룬 값을 치른 실례**로 기록했다.
- **patch·불변**: 집행은 `tests/discover` **235 → 241**로 늘고 지운 것이 없다. 되돌림 **8건 × 양 사본**에서
  초록으로 돌아온 변이가 없고 **하니스 7종 × 네 실행 환경 = 28칸**이 전부 FAIL 0이다. **`skills/`는 한
  파일도 바뀌지 않았다** — 커맨드 12종 계약·판정 토큰·`.tide/phase` 값 집합·가드·2.0 stable 동결 집합이
  전부 불변이라 가산 minor가 아니라 patch다.
### [v2.21.0]
- **가리킨다고 적은 자리가 실제로 닿는다(M52)**: `/tide:status`의 확인 항목 목록이 **규약 한 곳**에서 선언되고 `/tide:status`·`/tide:fleet`이 **열거하지 않고 읽는다**. 그 전에는 fleet이 *"status의 확인 항목을 조회한다"* 고 **선언만** 하고 여섯을 박아 둔 사본이었고, 그래서 **같은 상태에서 두 커맨드가 다른 답을 냈다** — status는 옳게 판단하는데 fleet은 **이미 릴리즈된 것보다 낮은 버전의 릴리즈**를 권했다. 그 실해가 사라진다.
- **fleet의 판단 taxonomy가 debug를 안다**: 규약의 정규 position에 **`debug 세션 열림` · `debug 릴리즈 가능`** 둘이 서고 **추천 버전을 그 debug 보고서에서 읽는다**가 못박혔다. 수집만 고치면 실해가 남는다는 것이 리뷰의 실측이었다 — 다섯 position이 전부 리뷰보고서만 보고 있었기 때문이다. `tests/fleet`이 «사이클 규칙만 보면 release 가능인 트리»로 그 우선순위를 문다(42 → **45** 케이스).
- **`.tide/autonomy` — 사이클이 개입 없이 이어질 수 있다(옵트인)**: `continuous`를 선언하면 `milestone → impl ⇄ review → 프리플라이트 → 게시 모드 해석 → 버전 → CHANGELOG`가 이어지고 **`git commit` 앞에서 멈춘다**. **선언이 없으면 `manual`이고 현행과 한 글자도 다르지 않다**(소음 0). 재작업 왕복도 자율 구간에 들지만 상한은 **새로 만들지 않고** 기존 «라운드 2 초과 재분해 권고»를 쓴다.
- **근거는 실측이다 — 개입 지점 25자리를 전수로 갈랐다**: 멈추는 게이트 5 · 커맨드 호출을 요구하는 구조적 정지 5 · advisory 8 · 기계 집행 중단 7. 판별자는 승인율이 아니라 **되돌릴 수 있는가 · 대가 · 답이 갈린 적이 있는가** 셋이고, **「형식」으로 판정된 것만** 선언 아래로 옮겼다. 「단계 = 세션」 전제는 **직접 관측으로 반증**됐고(한 세션이 컨텍스트 압축을 넘어 사이클 전체를 돌았다), 「종단 리뷰가 잡는 것은 주로 **안 한 일**」이라는 분포도 함께 기록됐다.
- **안전 바닥은 승인율이 100%여도 남는다**: 자율과 무관하게 유지되는 자리 **열**을 규약이 열거하고, 그중 **표지 있는 일곱**을 기계가 문다 — 자율 조건이 붙은 **줄 수**(`autonomy-lines:`)와 **바닥 게이트와의 항목 창 공존**(`floor-marks:`) 두 축이다. **못 무는 셋과 창 밖 편집은 고지로 남긴다** — 덮는 것과 못 덮는 것을 함께 적는 것이 이 부류의 끝이다.
- **같은 피해에 이르는 편집이 네 모양이었고, 리뷰가 셋을 실측으로 열었다**: ⑴ 게이트에 조건을 **더하기** ⑵ 줄 수를 보존한 채 **같은 항목 안에서 옮기기** ⑶ **하위 불릿**으로 달기. 처음 두 처방은 **편집의 모양**을 닫아 매번 다음 모양이 남았고, 창을 **항목의 부분 트리**로 바꾸고서야 계열이 닫혔다. **물음을 피해로 옮기는 것만으로는 부족하고 그 물음을 재는 창까지 구조여야 한다** — 이것이 이 사이클이 남기는 값이다.
- **되돌림이 도구 자체의 결함 둘을 잡았다**: 양 사본이 갈리는 자리(빈 표지에서 `grep -F`가 **항상 일치**해 한쪽이 공허해진다)와 **공허한 케이스**(창이 한쪽으로만 열려 사본이 반대쪽에 산다). 둘 다 되돌림이 아니면 릴리즈까지 갔을 것이다. **본 검사의 판정을 무력화하면 픽스처 통제가 붉는다**는 구조도 그대로 확인됐다.
- **minor·불변**: 집행은 `tests/discover` **212 → 235** · `tests/fleet` **42 → 45**로 늘고 지운 것이 없다. 되돌림 **38건 × 양 사본 = 76회**에서 초록으로 돌아온 변이가 없고, **하니스 7종 × 네 실행 환경 = 28칸**이 전부 FAIL 0이다. 커맨드 **12종** 계약 · 판정 토큰 · `.tide/phase` 값 집합 · `hooks/tide-guard.*` · 2.0 stable 동결 집합은 불변이다.
### [v2.20.0]
- **대화로 정한 큰 줄기가 세션을 넘어 살아남는다(M51)**: tide에 **에픽(방향) 층**이 섰다. `docs/epics/E{N}.md`가 사이클보다 큰 줄기를 담고, 마일스톤이 `epic: E{N}` 한 줄로 자기가 어느 줄기에 속하는지 밝히며, **그 참조의 실재와 양방향 일치를 하니스가 문다**(`tests/discover` **Part M**, 14 케이스). 그래서 다음 사이클이 줄기 밖으로 새면 **조용히 새는 것이 아니라 붉어진다.**
- **왜 지금인가는 실측이 정했다**: 마일스톤 51개의 `출처:` 줄을 분류하니 *직전 사이클 보고서만 가리키는 것*이 M1~M40에서 **7.5%**였다가 M41~M51에서 **55%**가 됐다. 각 사이클은 성실했고 리뷰 판정도 전부 통과였지만 방향이 **국소 기울기**를 따라갔고, 그 결과가 제품 표면에 나타났다 — **네 릴리즈 연속 `skills/`가 한 파일도 바뀌지 않았다.** 방향을 담는 산출물이 없어 각 사이클의 입력이 직전 사이클이 남긴 것뿐이 된 것이다.
- **커맨드 12종 계약은 한 글자도 바뀌지 않았다**: 늘어난 것은 **산출물**(`docs/epics/`)과 **참조**(`epic:` 한 줄)뿐이다. `/tide:milestone`이 열린 에픽을 읽고 `/tide:status`가 열린 줄기를 한 줄 보여주지만 호출명·개수·카탈로그 행 수·2.0 stable 명단은 그대로다. 스킬 증가분은 **9줄**(1,318 → 1,327)이라 M45가 줄인 것을 되돌리지 않았다.
- **하위 호환이 케이스로 걸려 있다**: **에픽이 하나도 없으면 규칙이 발동하지 않는다.** tide는 남의 저장소에 얹는 물건이라 이것이 계약이고, 에픽을 쓰지 않는 저장소의 동작은 현행과 같다(소음 0). 되돌림 변이로 그 경로를 깨면 **본 검사는 초록인 채** 그 케이스만 붉는다.
- **첫 리뷰가 「양방향」을 반환했고, 되돌림이 그것을 볼 수 없었던 이유가 기록됐다**: 규약은 양방향이라 적었는데 판정은 **마일스톤 기준 한 방향**만 돌아, *에픽이 어떤 마일스톤을 자기 줄기라고 주장하는데 그 마일스톤이 부인하는 자리*를 보지 못했다. **되돌림은 구현되지 않은 축을 볼 수 없다** — 무는 것은 «세운 것이 사는가»이지 «세워야 할 것을 세웠는가»가 아니다. 재작업 1이 에픽 기준 순회를 세웠다(고아 · 남의 마일스톤 주장 · 과거 예외).
- **되돌림이 통제 하나의 공허 통과를 잡았다**: 마커 블록 한정을 통째로 지운 변이가 **초록으로 돌아왔다.** 원인은 **규칙 둘이 순서대로 걸려 뒤엣것이 앞엣것의 부재를 가린 것** — 픽스처의 블록 밖 줄이 집행 시작 경계 미만이라, 창이 사라져 그 줄이 등재로 세어져도 **과거 예외가 삼켰다.** 통제가 *"창이 사는가"* 가 아니라 *"과거 예외가 사는가"* 를 묻고 있었다. 픽스처를 고치고 **경계 조건을 양 사본 주석에 실측과 함께 박았다.**
- **리뷰가 되돌림의 빈칸을 직접 메웠다**: 새로 선 케이스 넷 중 하나가 되돌림 표에 없었고, 그런데도 대조표는 그 케이스의 증거로 **다른 변이**를 적고 있었다. 리뷰가 변이를 직접 만들어 재고(양 사본 210/2, 같은 케이스가 붉음) 가드가 살아 있음을 확인한 뒤 표를 고쳤다. 빠진 이유가 다음 사이클이 볼 형태다 — **판정을 바꾸지 않고 딸려 온 케이스**는 «새 축의 변이» 목록에서 샌다.
- **정직 고지를 사실에 맞게 넓혔다**: 규약이 *"기계가 묻지 않는 것 — 둘이다"* 로 열거했는데 실제로는 넷이었다(에픽의 필수 섹션 존재 · 머리 상태값의 집합 소속). **열거 주장은 나머지가 물어진다고 읽힌다** — 집행력을 사실보다 넓게 주장하는 자리라 규약과 하니스 README를 같은 편집에서 넓혔다.
- **minor·불변**: 집행은 `tests/discover` **198 → 212**로 늘고 지운 것이 없다. 되돌림 **17건 × 양 사본 = 34회**에서 초록으로 돌아온 변이가 없고 PASS/FAIL 수와 붉은 케이스 이름이 한 건도 어긋나지 않았으며, **하니스 7종 × 네 실행 환경 = 28칸**이 전부 FAIL 0이다. 판정 토큰(`가능`/`불가`) · 프리플라이트 게이트 · `.tide/phase` 값 집합 · 보고서 필수 섹션 집합 · `hooks/tide-guard.*` · 2.0 stable 동결 집합은 불변이다.
- **이 태그는 회고 갱신도 함께 싣는다**: `docs/reports/retro.md`가 M47~M50 반영으로 갱신돼 있는데 **M51의 산출물이 아니라 같은 세션에서 먼저 돈 `/tide:retro`의 것**이다. impl 보고서와 리뷰가 그 사실을 기록했다.

### [v2.19.4]
- **사이클마다 반복되던 하니스 실행 고정비를 절반으로 깎았다(M50)**: `tests/discover`의 `sh` 사본이 **같은 판정을 145초 → 69초**에 낸다. 원인은 알고리즘도 I/O도 아니라 **프로세스 생성**이었다 — 1회 실행이 외부 명령을 **3300번** 띄웠고 이 기계에서 하나당 약 **32ms**다. 줄마다 프로세스를 띄우던 자리를 **한 번 도는 `awk`와 셸 파라미터 확장**으로 옮겨 **3300 → 1498(−55%)**이 됐다. **집행은 하나도 늘지도 줄지도 않았다**(케이스 수 불변).
- **비용을 추정이 아니라 계수로 확정했다**: `PATH` 앞에 shim을 두고 *"카운터에 한 줄 적고 진짜를 exec"* 하게 해 외부 명령 호출 수를 직접 셌다. shim이 프로세스를 하나씩 더 쓰므로 같은 실행이 145 → 250초가 됐고, **추가된 3300개에 105초**라 프로세스 1개 비용이 **부수적으로 직접 측정**됐다. 3300 × 32ms ≈ 105초로 **기준선의 72%가 프로세스 생성**임이 확인된다.
- **어디를 고칠지는 수치가 정했다**: 케이스마다 타임스탬프를 찍어 파트별로 나누니 셋이 68%였다 — 인용·자기참조 대조(39.5%) · 공통 통제 대조(14.9%) · 명단 드리프트(13.7%). 각 파트에서 줄마다 또는 파일마다 프로세스를 띄우던 함수를 이름으로 지목하고 그것만 고쳤다. 나머지 파트는 **목표를 세우지 않고 범위 밖으로 뒀다**.
- **판정이 케이스별로 한 건도 달라지지 않았다**: 재작성 전 **케이스별 결과 전체(라벨 · PASS/FAIL · 실측값)를 양 사본에서 「판정 지문」으로 떠 두고**, 단계마다 전건 대조를 통과 조건으로 걸었다. `run.ps1`은 한 줄도 만지지 않아 **두 사본의 동치가 독립 검증**이 된다.
- **그 지문이 회귀를 하나 잡았고, 되돌림 실측으로는 잡을 수 없는 부류였다**: 필드 분해를 파라미터 확장으로 옮기며 `awk print`가 붙이던 줄바꿈을 잃어, 필드를 파이프로 흘려 세는 자리 하나에서 **유일 이름이 4 → 1**이 됐다. 되돌림은 *"토큰을 깨면 붉어지는가"* 를 물어 **리팩터가 출력 형식을 바꾸는 것을 보지 못한다.** 총계로도 부족하다 — **어느 케이스가 왜 달라졌는지**는 케이스별 대조만이 말한다. 그 회귀는 되돌림 변이로 고정했다.
- **되돌림이 선존하던 공허 통과 둘을 드러냈다**: 인용 대조와 자기참조 대조의 **판정을 통째로 무력화해도 하니스가 초록**이다. 이 저장소의 판별법이 *"판정을 망가뜨렸을 때 그 통제가 붉어지지 않으면 통제가 아니다"* 인데 두 케이스가 그 상태였다 — **이번 사이클이 만든 것이 아니라 그 판정 함수를 처음 변이시켜 드러난 것**이다. 성능 축이라 고치지 않고 **다음 사이클로 반환**했고, 그 사이 재작성이 탐지를 망가뜨리지 않았음은 **리뷰가 깨진 인용·깨진 자기참조를 주입해 직접 확인**했다(둘 다 붉었다).
- **리뷰가 「논증이지 실측이 아니다」를 실측으로 바꿨다**: 구획 추출이 옛 파이프라인과 같은가(경계 10종 — 빈 구획 · 중괄호 · 홀수 따옴표 · 연속 따옴표 넷 · 붙은 구획 · 탭 · 다중바이트), 필드 분해가 옛 형태와 같은가(경계 8종 — 필드 셋뿐 · 다섯 · 빈 칸 · 값 안의 콜론)를 각각 옛/새 형태에 같은 입력으로 돌려 **바이트 동일**을 확인했다.
- **이식성을 명시적으로 지켰다**: 새로 쓴 `awk`에 비POSIX 구문이 **0건**이다(열거로 확인했고 `{n,m}` 인터벌까지 피해 `^## `·`^### `로 나눠 썼다). 다만 **로컬 네 환경이 전부 GNU awk**라 BSD awk 실행은 릴리즈 PR의 macOS 레그가 유일한 확인처이며, 리뷰가 그것을 **미검증 잔여 리스크로 명시**했다.
- **규약에 비용 규율을 세우되 못 무는 것을 적었다**: *"줄 단위 판정은 줄마다 프로세스를 띄우지 말고 한 번 도는 `awk`나 파라미터 확장으로 쓴다."* **집행은 세우지 않았다** — *while-read 루프 안에서 프로세스를 띄우는가*를 정적으로 묻는 것은 창의 정의가 어려운 부류라 다음 사이클 후보로 등재했다. 이 축은 **판정이 아니라 비용의 축**이라 어겨도 하니스가 초록이라는 사실도 함께 적었다.
- **patch·불변**: 커맨드 **12종**의 호출명·역할·산출물·git 행위, 판정 토큰(`가능`/`불가`), 프리플라이트 게이트 ①②③, `.tide/phase` 값 집합, 보고서 필수 섹션 집합, `hooks/tide-guard.*`, 2.0 stable 동결 집합, **러너의 인자·출력 형식·종료 코드**가 전부 불변이다. 집행 수도 그대로여서 사용자가 관찰할 변화가 없다.

### [v2.19.3]
- **문서가 자기 수를 틀리는 자리에 기계 수단이 섰다(M49)**: *"개수를 먼저 선언하고 곧이어 열거"* 하는 골격에서 **선언한 수와 실제 열거 항목 수가 어긋나는 것**을 `tests/discover`의 **Part L**이 문다. 발단은 직전 사이클의 차단 하나가 **논리 결함이 아니라 편집 사고**(한 줄이 다른 줄을 덮어써 고지 항목이 사라졌는데 서두는 그대로 셋을 선언했다)였고 그때 하니스 7종 × 네 환경이 **전부 초록**이었다는 것이다. 그 하니스는 **184 → 198**, 저장소 합계는 **359 → 373**이다.
- **닫을 수 있는지를 먼저 쟀고 수치가 창을 하나로 정했다**: 살아 있는 문서 **39개**에서 후보를 뽑아 창 후보 셋을 각각 돌린 결과 **동그라미 열거 기호**만 오탐 0이고, **뒤따르는 불릿 블록**은 전건이, **`·` 구분 인라인 열거**는 대부분이 어긋났다(`·`가 이 저장소에서 열거 기호가 아니라 범용 구분자다). 나머지 둘은 **세우지 않고 경계로 적었다.** 첫 판정은 창을 *문단*으로 잡고 두 열거 계열을 한 수 공간에 합쳐 오탐 5건을 냈는데, 창을 *목록 항목 경계*로 좁히고 계열을 **따로** 세자 0이 됐다.
- **표지를 러너에 적지 않고 규약이 선언한다**: 수사 아홉과 조사 다섯을 규약의 선언 줄 둘(`count-word:`·`count-copula:`)에 두고 두 러너는 **읽기만** 한다 — 그 덕분에 `run.ps1`에 한글 리터럴이 하나도 들어가지 않는다(ASCII-only 규율). 기전은 기존 `state-neg:`·`harness-control:`과 같다.
- **리뷰가 정상 문서를 붉히는 오탐을 반환했고, 그것이 이 사이클의 실질 관측이 됐다**: 첫 판본은 표지를 **낱말 경계 없이** 찾아, 수사가 다른 낱말의 끝 음절로 들어 있는 **평범한 한국어 낱말**을 선언으로 읽었다. 재작업 1이 **표지 앞 경계**(바로 앞이 줄머리이거나 ASCII 문자)로 닫았고, 두 사본이 같은 판정을 쓰는 근거는 *ASCII 문자 ⟺ ASCII 바이트*(비-ASCII 문자는 UTF-8에서 마지막 바이트가 0x80 이상)라 **바이트 산술 없이** 성립한다. 리뷰가 경계 근처 **11개 입력**에서 양 사본 판정이 전건 일치함을 재현했다.
- **왜 완료 기준 11개를 다 채우고도 그 오탐이 남았는가**: 라운드 0의 케이스 열하나가 전부 *"검사가 죽는가"* 를 물었고 *"검사가 과하게 무는가"* 를 묻는 것이 **하나도 없었다.** 되돌림 실측도 구조적으로 같은 방향만 본다. 경계를 통째로 지워도 **본 검사는 초록**이라(그 자리에 열거가 뒤따르지 않는다) 오탐 방향 케이스가 없으면 회귀가 조용히 돌아온다 — 그래서 **양방향 기댓값** 케이스를 세우고 *"새 검사에는 두 방향을 다 세운다"* 를 규약에 적었다.
- **케이스를 한 번 공허하게 세웠다가 되돌림이 그것을 드러냈다**: 창 경계 케이스의 첫 판본은 CRLF 회귀를 노렸는데, 무력화해도 초록이었다. 원인은 **Git Bash의 gawk가 파일을 텍스트 모드로 읽어 CR을 먼저 벗기는 것**이라 그 케이스는 의도한 이유와 다른 이유로 통과하고 있었다. 케이스는 **빈 줄 판정의 폭**을 묻도록 다시 세우고, CR 벗기기는 **케이스 없는 방어**로 선언해 못 무는 것을 적었다.
- **두 사본이 갈리던 주석 술어를 하나로 모았다**: `run.ps1`의 주석 판정 선언처가 둘이었고 **폭까지 달랐다** — 인자 없는 .NET `TrimStart()`는 모든 유니코드 공백을 벗기고 다른 쪽과 `sh`는 ASCII 공백·탭뿐이라, `.sh` 소스에 U+00A0으로 시작하는 주석 줄이 있으면 판정이 갈렸다. 선언처를 **하나**로 만들고 폭을 좁은 쪽(POSIX 셸의 실제 의미)에 맞췄으며 새 케이스가 그 폭을 샌드박스 픽스처로 못박는다.
- **되돌림 실측 34회와 실행 비용의 비대칭**: 변이 **17건 × 양 사본**을 전부 돌려 각 케이스가 자기 변이에서 붉는 것을 확인했다(사본 공통 12건은 양쪽 값이 완전히 동일, 사본 전용 5건은 결함 있는 쪽에서만 붉었다). 그 과정에서 **`run.sh` 1회가 `run.ps1` 1회의 약 15배**라는 것이 처음 수치로 나왔고, 원인은 케이스마다의 프로세스 생성이다 — 다음 사이클 후보로 등재했다.
- **이월 원장의 빈 행을 채웠다**: 표가 한 번호를 건너뛰고 있어 그 사이클의 처분을 한 행으로 적고, 직전 사이클 행에 **라운드 1의 반환과 부인 기록**을 한 문장으로 보충했다(행을 늘리지 않았다 — 이 원장의 방향은 복제 제거다).
- **patch·불변**: 커맨드 **12종**의 호출명·역할·산출물·git 행위, 판정 토큰(`가능`/`불가`), 프리플라이트 게이트 ①②③, `.tide/phase` 값 집합, 보고서 필수 섹션 집합, `hooks/tide-guard.*`, 2.0 stable 동결 집합이 전부 불변이다. 변경은 규약·이월 원장·기여자 하니스에 머물러 사용자 관찰 가능한 동작 변화가 없다.

### [v2.19.2]
- **직전 사이클이 사람으로 잡은 두 부류를 기계로 옮겼다(M48)**: ⑴ 러너 소스에서 `$이름` 뒤에 다중바이트가 오는 형태(셸마다 이름 경계 판정이 갈려 macOS의 bash 3.2에서만 죽는다 — 로컬 네 환경과 우분투 CI는 영원히 초록이다) ⑵ 새 하니스가 기존 하니스의 공통 통제를 갖췄는지의 목록 대조(직전 사이클 반환 셋이 전부 이 부류였다). 둘 다 `tests/discover`의 **Part J·Part K**로 집행되며 그 하니스는 **172 → 184**, 저장소 합계는 **347 → 359**다.
- **닫을 수 있는지를 먼저 쟀고 수치가 설계를 정했다**: 러너 소스 24개 파일에서 원시 매치는 **5건이고 전부 주석 줄**이라, 주석을 제외하지 않으면 오탐이 100%이고 **그중 하나는 이 규율 자체를 설명하는 주석**이다. 살아 있는 위반이 0건이라 추출형이 아니라 **금지형 + 픽스처 통제**로 세웠다.
- **넓은 토큰의 함정을 두 층에서 만났다**: 통제 보유를 문자열로 대조할 때 ⑴ **토큰의 넓이** — 느슨한 토큰은 *부재 사유를 적은 주석*에 걸려 전 종 보유처럼 보인다 ⑵ **토큰이 선 자리** — 첫 판본은 토큰이 어디에 있든 세어, 하니스마다 복붙된 주석 때문에 **실제 대조 코드를 잃어도 초록**이었다. 리뷰가 ⑵를 차단으로 반환했고 **주석 줄 제외**로 닫았다(선언 토큰은 한 글자도 바뀌지 않았고 면제 집합도 그대로다).
- **리뷰가 반환한 차단 둘 중 하나는 논리 결함이 아니라 편집 사고였다**: 한 줄이 다른 줄을 덮어써 **정직 고지 항목 하나가 사라졌는데**, 하니스 7종 × 네 환경이 전부 초록인 채 어떤 가드도 보지 못했다. 이 부류에 기계 수단이 있는지는 다음 사이클로 넘겼다.
- **반증 패스가 「닫혔다고 믿은 반환 항목」을 잡았다**: 새로 세운 발견 명세 픽스처는 *디렉터리 하나의 명세*만 물고 *어느 루트를 훑는가*는 보지 못해, 라운드 0이 실측한 그 훼손이 여전히 초록이었다. 리뷰가 그 자리를 in-review로 닫았다(발견 positive-control을 **루트마다** 묻는 형태로).
- **집행·경계를 함께 적었다**: 정적 판정이라 변수 경유·`eval`로 조립한 명령은 보지 못하고, 통제 대조는 **코드 줄에 토큰이 있는가**를 물지 그 통제가 실제로 동작하는지를 묻지 않는다. **BSD 실행 확인은 여전히 릴리즈 PR의 macOS 레그가 유일한 자리**다.
- **patch·불변**: 커맨드 **12종**의 호출명·역할·산출물·git 행위, 판정 토큰(`가능`/`불가`), 프리플라이트 게이트 ①②③, `.tide/phase` 값 집합, 보고서 필수 섹션 집합, `hooks/tide-guard.*`, 2.0 stable 동결 집합이 전부 불변이다. 변경은 규약·이월 원장·기여자 하니스에 머물러 사용자 관찰 가능한 동작 변화가 없다.

### [v2.19.1]
- **케이스가 자기 대상을 실제로 무는지를 사람이 아니라 기계가 확인한다(M47)**: 직전 두 사이클의 **유일한 차단이 모두 같은 부류**였다 — *"새로 세운 검사가 실제로는 검사하지 않는다."* M46의 픽스처 통제는 실제 판정을 한 번도 호출하지 않는 **동어반복**이었고, 판정을 통째로 무력화해도 양 사본이 초록이었다. 이 저장소는 그 확인을 **사람이 사본에서 손으로** 해 왔다(직전 사이클에만 여섯 번). 이번에 그것을 하니스로 옮겼다 — **일곱 번째 하니스**가 서고 집행은 340 → **347 케이스**로 늘었다.
- **닫을 수 있는지를 다섯 축으로 먼저 쟀고, 그중 하나가 설계를 바꿨다**: ⑴ 케이스를 **접두 식별자**로 지목하려 했으나 유일한 접두는 172 중 **72건(42%)** 뿐이고 직전 사이클이 신설한 아홉 중 **여섯이 중복**이라 열쇠가 되지 못한다 — 대신 **라벨의 보간 앞 안정 접두**가 양 사본에서 **161/161 유일**이라 그것을 쓴다. 두 러너는 **라벨 언어가 다르므로**(한글 ↔ 영어) 각 사본이 **자기 라벨로** 선언을 둔다. ⑵ 케이스의 **정적 열거율은 89%** 라(루프 생성 케이스가 있다) 대상을 한 하니스로 한정했다. ⑶ 비용이 범위를 갈랐다 — 뮤테이션 1건 = 사본 + 대상 하니스 1회이고 이 기계에서 sh 약 72초·pwsh 약 6초(**12배 비대칭**, Windows 프로세스 생성 비용)다. 같은 측정의 CI 대조는 우분투가 **13배 빨라** 전수 스윕은 CI 몫이고 로컬 기본은 **선언한 것만**이다.
- **예상 밖의 배당이 하나 있었다**: 무력화 대상의 주력인 선언 토큰이 **16종 전부 ASCII**다. 세 사이클 전에 *재서술 정합*을 위해 세운 **ASCII 병기어** 규율이, 그때는 의도하지 않은 채 **기계 무력화의 전제**를 미리 만들어 뒀다.
- **되돌림 실측이 「무엇을 되돌릴 것인가」를 정하지 않던 것을 규약 문장으로 닫았다**: 되돌리는 것은 *"이번 사이클이 고친 결함"* 이 아니라 **새로 세운 케이스 하나하나**이며 **통제 케이스도 예외가 아니다**. 각 무력화 대상과 그때 붉어진 케이스 이름을 **표로** 적을 것도 함께 요구한다 — 총계만으로는 어느 케이스가 무엇을 증명했는지 되짚을 수 없다. 직전 사이클의 차단이 정확히 이 빈자리에서 났다.
- **판례가 하나 늘고 일반화가 규약 층위로 올라갔다**: *"픽스처 통제는 「픽스처가 조건을 만족하는가」가 아니라 **「실제 판정이 픽스처를 잡는가」**를 물어야 한다."* 판별법도 한 줄로 못박았다 — **판정을 망가뜨렸을 때 그 통제가 붉어지지 않으면 통제가 아니다.**
- **세 사이클 이월된 후보가 조건 도래로 기각 처분됐다**: *"좁은 부류의 in-review 수정 허용"* 은 재검토 조건이 **판정 `불가` 사이클 둘 누적**이었고 그 둘이 채워졌다. 그런데 두 반환 사유를 대조하니 **어느 쪽도 이 후보가 노리던 부류가 아니었다** — 하나는 경로 파싱 구조와 명세 누락, 다른 하나는 판정 함수 추출이다. 후보가 전제한 *자기 유발·한 줄·기계 검증 가능* 은 **표본 둘에서 0건**이라 기각했고, 재개 조건(그 부류의 반환이 실제로 발생)을 함께 적었다. **조건이 도래했는데 다시 미루는 것도 처분이 아니다.**
- **분해가 실제로 성립한 첫 사이클이다**: 직전 사이클은 무의존 태스크 넷이 쌍마다 같은 규약 문서에서 겹쳐 **병렬 디스패치가 전부 폴백**했고 회고가 그것을 구조 신호로 뽑았다. 이번 마일스톤은 그 교훈을 설계에 넣어 **문서 편집과 하니스 구현을 서로소 집합**으로 갈랐고, 실측에서도 교집합이 공집합이었다 — 폴백 사유가 *"분해 실패"* 가 아니라 *"디스패치 부재"* 로 갈렸다.
- **세 라운드가 같은 부류를 세 번 만에 소진했다**: 반환이 ⑴ 새 하니스가 **자기 케이스 수 선언을 대조하지 않음** ⑵ **선언 줄 유일성 미단언**(진짜 선언을 낡게 두고 앞선 산문에 맞는 수를 넣으면 초록이었다) ⑶ **완주 가드 미보유** 순으로 나왔는데, **셋 다 「기존 하니스 여섯 종이 공통으로 가진 통제를 새 하니스가 빠뜨림」**이다. 앞의 둘은 리뷰가 찾았고 **셋째는 목록 대조가 찾았다** — 추측이 절차로 바뀐 것이 값이며, 그 절차를 규약으로 올릴 근거가 실측으로 셋 쌓였다.
- **만들던 도구가 자기 산출물을 두 번 물었다**: ⑴ 토큰을 **덧붙여서** 바꿨더니 원본이 부분 문자열로 남아 뮤테이션이 무효였는데 하니스가 첫 실행에서 `missed`로 정확히 보고했다 ⑵ 유일성 설명을 문서에 쓰면서 예시 수치를 **선언 형태로** 적었더니 그 검사가 **즉시** 자기 문서를 검출했다. 도구의 값이 사후 주장이 아니라 실측이다.
- **patch·불변**: 커맨드 **12종**의 호출명·역할·산출물·git 행위, 판정 토큰(`가능`/`불가`), 프리플라이트 게이트 ①②③, `.tide/phase` 값 집합, 보고서 **필수 섹션 집합**, **tide-guard**(훅 **무접촉** — 다섯 사이클 연속), 2.0 stable 동결 집합이 전부 불변이다. 변경이 **규약·이월 원장·기여자 하니스**에만 있어 사용자가 관찰할 수 있는 동작 변화가 없다. 하니스 **7종**을 **네 실행 환경**(dash · bash · Windows PowerShell 5.1 · pwsh 7)에서 재실행해 전부 FAIL 0(**347 / 0**).
- **릴리즈 PR의 CI가 이 사이클의 마지막 결함을 잡았다(debug-2)**: 새 러너가 결과 줄에서 `$변수` 바로 뒤에 다중바이트 문자를 두고 있었는데, 셸이 **변수 이름 경계를 어디로 잡는가**가 구현마다 갈린다 — 개발 기계의 bash 5.2와 `dash`는 이름을 끊어 정상이지만 macOS가 `sh`로 쓰는 **bash 3.2는 뒤따르는 바이트를 이름에 포함**해 `set -u`로 죽었다. 로컬 네 실행 환경과 우분투 CI는 **전부 초록**이었고 `posix (macos-latest)` 하나만 붉었다. 중괄호로 이름 경계를 명시해 닫았고 **출력 문자열은 바이트 동일**이며, 같은 형태를 실행되는 줄 전수로 재조사해 **0건**을 확인했다. 기존 러너 여섯 종에는 이 형태가 주석 줄에만 있어 **일곱 번째 러너가 첫 실사용**이었다 — *"리뷰가 볼 수 없는 축은 CI만 본다"* 의 여섯 번째 사례다.
- **남은 것(정직 표기)**: ⑴ **이번엔 POSIX 러너를 새로 하나 추가했고 BSD에서 한 번도 돌려 보지 못했다** — 네 실행 환경에 BSD가 없어 GNU 전용 형태가 로컬에서 영원히 초록이다(첫 판본이 실제로 GNU 전용 형태를 들여왔고 리뷰가 **기존 러너의 도구 사용 목록 대조**로 잡았다). **릴리즈 PR의 macOS 레그를 태그 전에 읽는 것이 지난 릴리즈들보다 무겁다.** ⑵ 새 하니스가 **CI 시간을 늘린다** — 로컬 sh에서 1회 약 2.4분이라 윈도우 레그가 길어질 수 있다. ⑶ 뮤테이션 선언은 **2건 옵트인**이라 나머지 케이스는 묻지 않는다(설계된 침묵이며 문서가 적는다). ⑷ **케이스 선택 수단이 없어** 뮤테이션 1건마다 대상 하니스를 통째로 돌린다 — 그것이 생기면 전수가 로컬에서도 가능해지므로 확대의 선행 조건이다. ⑸ **판정 코드의 무력화는 선언으로 표현되지 않는다**(비교 방향 반전·함수 치환·픽스처 무력화가 전부 다른 편집이다) — 그쪽은 규약이 요구하는 케이스별 되돌림 실측이 계속 덮는다. ⑹ `jq` 경로는 **열한 사이클 연속 미실측**(훅 무접촉이라 새 리스크는 없다).

### [v2.19.0]
- **보고서가 사실과 어긋나도 초록이던 자리를 닫고, 세 사이클 미룬 인용 층위를 처분했다(M46)**: 직전 릴리즈의 프리플라이트가 두 자리를 실측으로 드러낸 것이 출발점이다 — 리뷰 보고서가 **변경하지도 않은 파일을 등급 근거로 들었고**(`skills/review/template.md`), 같은 프리플라이트의 커버리지 체크가 대상 17건 중 **7건을 미상으로 냈는데 전부 위양성**이었다. 둘 다 어떤 검사도 보지 않던 층이고, 둘 다 **프리플라이트가 이미 계산해 둔 같은 집합** 위에서 끝난다. 집행은 `tests/discover` **159 → 172 케이스**로 늘었고 지운 것은 없다.
- **표본 하나로 판정하지 않았다 — 네 사이클로 넓혀 재고 결론대로 갈랐다**: 역방향 대조(*"보고서가 언급한 경로가 실제로 바뀌었는가"*)를 M42~M45 여덟 보고서에 돌리니 실재 경로 토큰 **96개 중 10개**가 플래그됐는데 **진짜 결함은 하나**였다. 나머지 아홉은 **포인터**(단일 원본을 가리키는 줄) · **이력 서술**(되돌림 실험·과거 사이클 언급) · **경로 축약**(`tests/discover/README.md`를 `README.md`로 적은 자리)이다. **위양성 90%** 면 advisory를 사람이 읽지 않게 되어 검사가 공허해지므로 **산문 전역 축은 집행하지 않고 측정치와 경계를 규약에 적었다.**
- **대신 구조화된 선언처로 좁히니 오탐이 0이 됐다**: 대조 범위를 impl 보고서의 `변경 파일 요약` **표**로 한정하고 표의 백틱 토큰 중 **실재하는 것만** 선언으로 인정하면 M42~M45 네 사이클에서 **역방향 오탐 0 · 정방향 누락 0**이다. **행마다 실재를 묻는 트리가 다르다** — `추가`·`수정`은 현재 트리, `삭제`는 마지막 태그의 트리다(리뷰 2회차가 잡았다: 세 행을 전부 현재 트리로 걸면 *"지웠다"* 는 선언이 **한 건도 대조되지 않은 채** 통과한다).
- **위양성 쪽은 한 줄로 닫혔다**: 보고서가 계열을 묶어 적은 형태(`skills/{impl,review}/SKILL.md`)를 매칭 전에 **펼친다**. 직전 릴리즈의 미상 7건이 **전부** 이 형태였고 펼친 뒤 **0건**이 된다. 펼치는 것은 중괄호 토큰뿐이며 글롭은 펼치지 않는다 — M42~M44 구간은 이 형태를 쓰지 않아 펼치기 전후가 모두 0이다(음성 통제).
- **세 사이클 미룬 것을 명시적 수용으로 처분했다**: 인용 대상이 **절이 아닌 표·불릿**인 자리를 두 리뷰가 *"한 줄로 합치면 붉어질 수 있다"* 로 미뤄 왔다. 합쳐 보니 **실제로 붉어졌고**(예고는 맞았고 기전만 달랐다 — `G5`가 아니라 `G8b`였다) 그 과정에서 진짜 원인이 드러났다: 닫아야 할 것은 규칙이 아니라 **줄바꿈이 만든 은폐**였다. 골격을 쓰면서 없는 앵커를 가리키면 가드가 이미 문다. 그래서 *"승격도 재지정도 불가능한 대상은 인용 골격을 아예 쓰지 않는다"* 를 규약에 못박고 **대상을 0건으로 만들어** 처분했다(조건 없는 이월은 처분이 아니다).
- **프로즈로만 있던 진입점 문서 상한이 기계에 물렸다**: 규약이 `CLAUDE.md`에 줄 수 상한을 적으면서 *"기계가 묻지 않는다"* 고 **함께 적고 있었다**. 닫기 전에 전제 둘을 고쳤다 — ⑴ 상한 값이 **두 곳**에 선언돼 있어 하나로 모았고 ⑵ **세는 방법이 판정을 가른다**(개행 없는 마지막 줄에서 `wc -l`은 1 적게 센다: 3줄 파일에서 `wc -l` 2 · 나머지 둘 3). 판정을 두 러너 동형으로 고정하고 상한 값을 규약에서 **추출해** 비교하므로 값이 바뀌면 검사가 따라간다.
- **반증 패스가 몇 사이클 연속 폴백인지가 보인다**: 계측 줄에 **연속 폴백** 필드를 더했다. 폴백 자체를 막으면 서브에이전트가 불가한 세션에서 리뷰가 멈추므로 **막는 것이 아니라 드러내는 것**이 목적이다. 값의 근거는 **직전 리뷰 보고서의 같은 줄**이라 새 원장을 만들지 않는다.
- **재작업이 값을 한 자리 — 통제와 판정을 같은 코드에 묶었다**: 리뷰 1회차가 차단 하나를 반환했다. 새로 세운 픽스처 통제가 *"픽스처가 조건을 만족하는가"* 만 물어 **실제 판정을 한 번도 호출하지 않는 동어반복**이었고, 그래서 판정을 통째로 무력화해도 **양 사본이 초록**이었다. 판정을 함수 하나로 뽑아 실물 판정과 픽스처 통제가 **같은 것을 부르게** 고쳤고, 리뷰 2회차가 **impl과 다른 두 변이**(비교 방향 반전 · 픽스처 무력화)로 붉히는 데 성공해 통제임을 확인했다. 일반화해 남겼다 — *"픽스처 통제는 「픽스처가 조건을 만족하는가」가 아니라 「실제 판정이 픽스처를 잡는가」를 물어야 한다."*
- **직전 릴리즈가 밟은 함정 둘을 문서에 고정했다**: ⑴ CHANGELOG 릴리즈 노트는 마커 **다음** 줄에 넣는다 — 같은 토큰이 바로 위 설명 주석에도 있어 첫 매치로 삽입하면 구획 **밖**에 들어가는데 로컬 가드는 앵커 정규식을 써서 **초록인 채 통과한다** ⑵ 프리플라이트의 사이트 빌드 검증은 **로컬에서 완주할 수 있다**(임시 가상환경 + 레포 루트에서 설정 파일을 지정해 빌드). 관행이 *"도구가 없어 못 한다"* 로 굳어 프록시로 대체돼 왔고, 완주하지 못한 경우엔 **그 사실을 보고에 적을 것**을 함께 요구한다.
- **minor·불변**: 커맨드 **12종**의 호출명·역할·산출물·git 행위, 판정 토큰(`가능`/`불가`), **프리플라이트 게이트 ①②③의 집합·순서·판정 로직**, `.tide/phase` 값 집합, 보고서 **필수 섹션 집합**, **tide-guard**(훅 **무접촉** — M43 이후 네 사이클 연속), 2.0 stable 동결 집합이 전부 불변이다. minor인 이유는 릴리즈 프리플라이트의 커버리지 advisory에 **축이 둘 늘고** 리뷰 계측 줄의 **형식이 바뀌어** 사용자가 관찰할 수 있는 절차 변화가 있다는 것이다. 하니스 6종을 **네 실행 환경**(dash · bash · Windows PowerShell 5.1 · pwsh 7)에서 재실행해 전부 FAIL 0(**340 / 0**).
- **남은 것(정직 표기)**: ⑴ **BSD `awk`·macOS는 로컬에서 못 쟀다** — 이 사이클도 러너 **두 사본을 모두** 만졌으므로 **릴리즈 PR의 macOS 레그를 태그 전에 읽는 것이 실질 게이트**다. ⑵ `jq` 경로는 **열 사이클 연속 미실측**(훅 무접촉이라 새 리스크는 없다). ⑶ 커버리지 두 축의 **런타임 발화**는 프롬프트 규율이라 하니스가 덮지 못하고, 삭제 행 판정은 이번 사이클의 삭제가 0건이라 **아직 발화하지 않았다**. ⑷ **되돌림 실측의 적용 범위가 규약에 없다** — *"무엇을 되돌릴 것인가"* 가 비어 있어 이번 사이클이 판정 케이스만 되돌리고 통제 케이스 자신은 되돌리지 않아 차단이 났다. ⑸ **연속 폴백 값은 실제 연속 횟수와 어긋난 채 시작한다**(소급 금지의 대가). ⑹ 무의존 태스크 넷이 쌍마다 같은 규약 문서에서 겹쳐 **병렬 디스패치를 전부 폴백**했다 — 마일스톤 분해 신호다. ⑺ 진입점 문서 상한 검사는 **초과만 물고 근접은 묻지 않는다**(현재 여유 두 줄).

### [v2.18.0]
- **프로즈로만 존재하던 규율을 기계에 물리고, 기계가 보지 않는 프로즈는 지웠다(M45)**: 이 사이클은 **늘리기와 줄이기를 동시에** 했다 — 집행은 `tests/discover` **147 → 159 케이스**로 늘고, 프로즈는 스킬 보일러플레이트 **−31줄** · `release` 스킬 **−21줄** · `docs/project-context.md` **−22줄**로 줄었다. **집행은 하나도 지우지 않았다.**
- **네 사이클 연속 사람 손에 잡혀 온 부류가 실측으로 결론났다 — ⓐ 닫을 수 있다**: 로케일 고정 규율(M41 `Sort-Object`·`StartsWith` 6곳 · M42 세 표면 전수 감사 · M44 리뷰가 잡은 `sort -u` 누락)에 기계 수단이 있는지를 **먼저 읽기 전용으로 쟀다**. `.sh`에서 `sort`·`uniq`가 등장하는 원시 줄은 **46**이지만 **명령 위치**(줄 선두·`|`·`;`·`&`·`(`·`$(` 뒤)로 좁히면 **22**이고, 남은 미고정 **2건은 전부 의도된 `sort -V`**(M40 유지 결정)였다. 러너에 **전역 `LC_ALL`/`LANG` 설정이 없다**는 전제까지 확인한 뒤에야 판정했다 — 있었다면 줄 단위 요구가 통째로 오탐이 된다.
- **결론대로 `F9~F13` 다섯 케이스가 섰다**: `F9`가 **미고정·미선언 사이트 0건**을 묻고, `F10`·`F11`이 **계열별**(sh·ps1) 추출 positive-control, `F12`·`F13`이 **계열별** 픽스처 통제다(각 계열에 미고정 한 줄을 심으면 붉어진다). 예외는 지우는 것이 아니라 **같은 줄에 `locale-exempt: <사유>`로 선언**한다 — 선언은 *「고치지 않아도 된다」* 가 아니라 **결정을 문서에 남기게 하는 장치**다. `.ps1` 쪽은 **인자 없는 `Sort-Object`에 고정 형태가 없다**로 확정했다(헬퍼로 바꾸거나 선언한다). 광범위 비교 API(`-eq`·`-match`·`IndexOf`·`StartsWith`)를 대상에 넣는 대안은 **오탐 폭발**로 기각했다 — `run.ps1` 한 파일에만 166줄이 걸리고 대다수가 문화권 무관한 ASCII 토큰 비교다.
- **재작업이 값을 한 자리는 「기전을 그대로 믿지 않은 것」이다**: 리뷰 1회차가 차단 둘을 반환했고 재작업이 그 넷을 닫았는데, **차단 2의 진짜 원인은 반환이 지목한 `Ordinal` 탈출구가 아니었다** — 탈출구를 없앤 뒤에도 같은 공격이 **여전히 통과했고**(양 사본 159/0 실측), 실제 기전은 **사이트 종결 문자 명세의 누락**이었다. 차단 1도 마찬가지로 구조적으로 닫혔다: 첫 판본이 사이트를 `파일:줄번호:본문`으로 이어 붙인 뒤 콜론으로 되쪼개 **경로의 드라이브 콜론에서 어긋났고**(같은 트리에서 bash 158/0 · pwsh 157/1로 **두 사본이 갈렸다**), 이제 파일별 스캔 안에서 **줄 자체를 보고** 판정해 콜론 파싱이 사라졌다. 공백 집합도 양 사본 같은 명세(ASCII 공백·탭)로 고정했다 — `[[:space:]]` ↔ `\s`는 무는 폭이 달라 **그 자체가 두 셸 갈림의 씨앗**이었다.
- **어떤 검사도 보지 않던 프로즈가 사라지고 얇은 진입점이 생겼다**: 9개 스킬에 반복되던 「대상 레포」 문단 **40줄**은 `tests/discover` Part C/D/E의 토큰 목록에 **없었다**(대조 완료) — 규약 인용 한 줄이 이미 각 스킬에 함께 있으므로 지웠다. 대신 이 저장소에 **없던** `CLAUDE.md`를 **함정 목록**으로 신설했다: 규칙의 단일 원본이 아니라 *"모르면 조용히 깨지는 자리"* 만 적고 상세는 전부 포인터가 가리킨다.
- **필요할 때 읽는 자리로 내려간 것 둘**: 리뷰의 **반증 절차**가 `skills/review/refutation.md`로 분리됐고, `release` 스킬의 게시 요지가 규약 조각을 가리키게 바뀌었다. 반증 패스를 **13번째 커맨드**로 만드는 것이 자연스러워 보였지만 커맨드 **12종**은 B1(파일 수)·B3(카탈로그 완전성)·2.0 stable 명단이 함께 무는 **계약 표면**이라, 같은 지연 로딩 이득을 **조건부 참조 파일**로 얻고 계약은 불변으로 뒀다.
- **산출물이 아닌 파일에 걸려 있던 규약 인용이 끊겼다**: M43이 내용을 원장으로 옮겼는데도 **살아 있는 문서 2곳**이 아직 세션 운영 메모를 인용하고 있었다 — 그 의존을 제거하고 원장·핵심 도메인 개념·복제 선언 수를 갱신했다.
- **도달 가능성 가중의 세 번째 적용 기록**: 이번 리뷰가 찾은 셋을 전부 `사소`로 매겼다 — 하나는 자기 훼손 트리거에 **요란한 실패**(글롭 0매치 시 산술 오류로 sh 러너가 결과 줄조차 못 냄), 하나는 **오탐**(따옴표 안 산문이 사이트로 잡히는 표면 — 공허 통과가 아니다), 하나는 가드 밖 문서 위생이다. **등급을 내리면서 셋 다 고쳤고 경계를 문서에 남겼다.**
- **minor·불변**: 커맨드 **12종**의 호출명·역할·산출물·git 행위, 판정 토큰(`가능`/`불가`), **프리플라이트 게이트 ①②③의 집합·순서·판정 로직**, `.tide/phase` 값 집합, 보고서 **필수 섹션 집합**, **tide-guard**(훅 **무접촉** — M43 이후 세 사이클 연속), 2.0 stable 동결 집합이 전부 불변이다. minor인 이유는 `skills/review/SKILL.md`·`skills/release/SKILL.md`가 바뀌고 **`skills/review/refutation.md`가 새로 절차의 일부가 됐다**는 것 — **사용자가 관찰할 수 있는 절차 변화**다. 하니스 6종을 **네 실행 환경**(dash · bash · Windows PowerShell 5.1 · pwsh 7)에서 재실행해 전부 FAIL 0(**327 / 0**).
- **남은 것(정직 표기)**: ⑴ **BSD `awk`·macOS는 로컬에서 못 쟀다** — 이 사이클이 러너 **두 사본을 모두** 만졌으므로 **릴리즈 PR의 macOS 레그를 태그 전에 읽는 것이 실질 게이트**다. ⑵ `jq` 경로는 **아홉 사이클 연속 미실측**(훅 무접촉이라 새 리스크는 없다). ⑶ **`F10`·`F11`은 계열별로 쪼개진 뒤에도 「사이트 수 > 0」만 본다** — 추출이 **줄어들었지만 0은 아닌** 경우는 여전히 통과하고, `F12`·`F13`의 픽스처가 부분적으로만 덮는다. ⑷ **따옴표 안 산문의 오탐 표면**은 닫지 않고 **고지로 남겼다**(실측 예시 포함 — 규약·하니스 문서 양쪽). ⑸ 인용 대상이 절이 아닌 **표 이름**인 자리 하나는 층위 문제로 이번에도 손대지 못했다(한 줄로 합치면 인용 가능한 앵커 층위 밖을 가리켜 `G5`가 붉어질 수 있다 — M44 리뷰와 같은 판단을 유지). ⑹ 이번 리뷰의 반증 패스도 **폴백 경로**로 돌았다(성립 **3건** — 세션 정책상 서브에이전트 디스패치 미가용).

### [v2.17.0]
- **규칙을 고치면 그 규칙을 다시 말한 자리가 붉어진다(M44)**: 직전 사이클의 리뷰 지적 **여섯 중 넷**이 *"규약을 고쳤는데 그것을 재서술한 스킬·템플릿·원장이 낡았다"* 였다. 이 사이클은 그 부류에 **기계 수단이 있는지를 먼저 실측**하고(읽기 전용 태스크 — 코드·규약 변경 0) 결론대로만 집행했다. 두 부류가 **서로 다른 형태로** 닫혔다.
- **기전이 특정됐다 — 판단 실수가 아니라 결합점 부재였다**: 직전 사이클이 더한 두 규칙(**종료 조건** · **도달 가능성 가중**)에 **ASCII 병기어가 0개**였고, 리뷰 검증 규율의 나머지 여섯 규칙은 전부 병기어로 결합돼 있었다. 결합할 토큰이 없으니 *"종료 조건을 무조건으로 부정하는 재서술 세 자리"* 가 **트리 그대로 초록**이었다. 병기어를 붙여 결합하고(케이스 둘 + 음성 통제 하나), 규약이 **앞으로의 규칙에도** 병기어를 요구한다 — *"병기어가 없는 규칙은 재서술처가 낡아도 초록이다."*
- **그 측정이 같은 부류의 네 번째 자리를 새로 찾았다**: 리뷰 보고서 템플릿이 *"1건 이상이면 판정은 `불가`"* 를 **예외 없이** 적고 있었다(직전 사이클의 in-review 수정이 세 자리만 고쳤다). 트리거가 *"템플릿을 위에서 아래로 읽는다"* 이므로 **도달 가능**하고, 무는 케이스는 없었다. 예외와 병기어를 넣어 닫았고 이제 그 자리가 세 곳 결합의 하나라 **다시 낡으면 기계가 문다**.
- **닫을 수 없는 형태와 닫을 수 있는 형태를 실측으로 갈랐다**: 인용이 절 **안의 항목**을 열거 번호로 가리키는 부류에서 *"번호가 가리키는 항목이 실재하는가"* 는 **닫지 못한다** — 살아 있는 대상이 **0건**이라 추출형으로 세우면 *"추출 0 = FAIL"* 규율에 걸려 정상 트리가 붉어지고, 해소에는 3단(절 → 명명된 열거 → 번호)이 필요한데 열거 이름이 한글이라 ps1의 ASCII-only 규율과 충돌한다. 대신 **번호로 가리키는 것 자체를 금지**하는 형태로 닫았다(부재 단언이라 대상 0건이 정상값이다). **항목은 이름으로 가리킨다**가 규약이 됐다.
- **금지형도 훑을 대상이 비면 공허하다 — 그 자리를 리뷰가 잡았다**: 첫 판본은 인용 줄 **추출 자체에 통제가 없어** 정규식이나 범위가 망가지면 `0 == 0`으로 조용히 통과했고, 규약·하니스 문서·완료보고서 **셋이** 그것을 *"금지형이라 추출 통제 대상이 아니다"* 로 **정당한 설계처럼 서술**하고 있었다. 리뷰가 추출 positive-control을 세우고 세 문서의 서술을 고쳤다 — 되돌림으로 실증했다: 추출을 무력화하면 그 통제만 붉어진다(네 실행 환경 동일).
- **문자 집합을 두 러너가 각자 자기 단언한다**: 무는 문자는 **U+2460~U+2487**(40자)이고 **양 사본이 코드포인트로 조립**한다(`run.ps1`은 ASCII-only 규율 때문에 필수이고 `run.sh`도 같은 방식으로 맞췄다). 매칭은 `awk`·문자 범위·collation을 **쓰지 않는** 바이트 리터럴 대조뿐이다 — 측정 중 다중바이트 **범위**가 바이트 범위로 해석돼 오탐이 폭발하는 것을 실제로 겪었고(이 저장소가 두 번 데인 부류다) 그 축을 구현에서 제거했다.
- **재서술 편집 규율이 규약이 됐다**: *"규칙을 고치는 편집은 그 규칙의 **재서술처를 같은 편집에서** 함께 고친다."* **포인터**(가리키기만 하는 줄 — 낡지 않는다)와 **재서술**(내용을 다시 말하는 자리 — 반드시 함께 낡는다)의 구별을 복제 선언 수 지표와 **같은 기준**으로 적고, **재서술처를 늘리지 않는 것**을 방향으로 못박았다(새로 적기보다 포인터로 바꾼다). 집행 상태는 사실대로 적었다 — 병기어가 **있는데도** 옆 문장이 규칙과 반대되는 사실을 말하는 층은 **사람의 리뷰 영역**이며, 창 기반 모순 검출을 기각한 근거(예외 문장이 **다음 줄**에 있어 줄 단위로 잡히지 않음 · 표지가 한글이라 ps1 규율과 충돌)를 수치와 함께 남겼다.
- **기존 가드 둘이 만든 사람을 세웠다**: 새 러너 주석에 한글 리터럴이 섞이자 **비-ASCII 0줄** 검사가 즉시 붉었고(직전 사이클이 그 규율을 기계에 물렸다), 새 규약 소절의 제목과 인용이 어긋나자 **상호참조 무결성** 검사가 물었다 — **이 사이클이 다루는 부류가 이 사이클 안에서 재현된 것**이다. 리뷰의 반증 패스는 이 사이클의 편집이 만든 **줄바꿈 인용** 하나를 더 찾았다(규약이 금지하고 가드가 **보지 못하는** 자리라 초록인 채 숨어 있었다).
- **이월 후보가 조건과 함께 처분됐다**: *"좁은 부류의 in-review 수정 허용"* 은 **이월 유지**이며, 그 내용은 **다음 판단 조건**이다 — 직전 사이클이 rework 0이었으나 그 라운드는 **반환이 애초에 없어** 이 후보의 값을 재는 표본이 아니다. 판정 `불가`가 난 사이클이 **둘 누적**되면 그때 정한다. 조건 없는 이월은 처분이 아니므로 **조건 자체가 처분**이다.
- **minor·불변**: 커맨드 **12종의 호출명·역할·산출물·git 행위**, 판정 토큰(`가능`/`불가`), **프리플라이트 게이트 ①②③의 집합·순서·판정 로직**, `.tide/phase` 값 집합, 보고서 **필수 섹션 집합**, **tide-guard**(훅 **무접촉** — M43 이후 두 사이클 연속), 2.0 stable 동결 집합이 전부 불변이다. minor인 이유는 리뷰 스킬과 **리뷰 보고서 템플릿**이 바뀌어(수용 근거 블록 안내가 새로 생겼다) **사용자가 관찰할 수 있는 절차 변화**가 있다는 것이다. **집행은 하나도 지워지지 않고 케이스는 늘었다** — `tests/discover` 147 → **154**. 하니스 6종을 **네 실행 환경**(dash · bash · Windows PowerShell 5.1 · pwsh 7)에서 재실행해 전부 FAIL 0(**322 / 0**).
- **남은 것(정직 표기)**: ⑴ **BSD `awk`·macOS는 로컬에서 못 쟀다** — 이 사이클이 러너 **두 사본을 모두** 만졌고 새 검사가 바이트 대조를 들여왔다. **릴리즈 PR의 macOS 레그를 태그 전에 읽는 것이 실질 게이트**다. ⑵ `awk`는 GNU Awk로만 쟀다(우분투 CI는 mawk). ⑶ `jq` 경로는 여덟 사이클 연속 미실측(훅 무접촉이라 새 리스크는 없다). ⑷ **이름으로 가리킨 항목의 실재는 대조하지 않는다**(사이트 5건 · 관측 결함 0건이라 근거가 아직 없다). ⑸ 인용 대상이 절이 아닌 **표 이름**인 자리 하나는 층위 문제로 손대지 못하고 다음 사이클 후보로 남겼다. ⑹ 이번 리뷰의 반증 패스는 **폴백 경로**로 돌았다(성립 1건) — 직전 사이클의 서브에이전트 경로가 여섯 중 넷을 세웠던 것과 비대칭이며, 그 사실을 후보로 등재했다.

### [v2.16.0]
- **기준을 낮추지 않고 만족하기 쉽게 만들었다(M43)**: 직전 사이클을 여섯 라운드로 만든 원인이 **판단이 아니라 규칙에 없던 체크리스트**였다는 관측에서 출발한다. `docs/conventions.md`에 **새 검사를 세울 때 함께 붙일 것** 넷이 규약 요구로 섰다 — ⑴ 추출 positive-control(입력이 비면 FAIL) ⑵ 선언 줄 유일성(0개도 2개 이상도 FAIL) ⑶ 되돌림 실측(고치기 전에 한 번 붉게) ⑷ 무는 대상 집합의 명세. 직전 사이클의 차단 넷이 **각각 무엇이 빠져서 생겼는지를 표로 대응**시켰다. 경계도 함께 적었다 — **새로 세우는 것에만 걸고**(소급 전수 보강이 아니다), 집행은 **리뷰의 목록 대조**이지 기계가 "체크리스트를 지켰는가"를 묻지는 않는다.
- **등급 루브릭이 도달 가능성을 가중한다**: 묻는 것은 하나다 — **산출물의 정상 편집으로 트리거에 도달할 수 있는가.** 도달 가능하면 `차단`(실례 셋을 직전 사이클 실측에서 인용: 확장자 대소문자·선두 BOM·경로의 공백), **자기 선언·자기 검사 코드를 고의로 훼손해야만** 발동하면 `권장` + 경계 기록이다. **등급이 내려가도 경계 기록은 의무**이며 *"등급 완화가 집행 완화로 번지는 것이 이 조항의 유일한 실패 모드"*라는 문장을 함께 박았다 — 그 경우 다음 리뷰가 이 문장을 근거로 되돌린다. 첫 적용 라운드에서 여섯 지적에 전부 적용했고 **하나도 하향되지 않았다**(전부 문서를 읽는 것만으로 성립).
- **재작업에 바닥이 생겼다 — 다만 판정 토큰은 늘리지 않는다**: 라운드 **2 초과**에서만 열리는 **잔여 위험 수용** 경로를 수렴 조건에 더하고 `skills/review/SKILL.md`에 배선했다(`가능` + 수용 근거 블록 = 무엇을·왜·어디로). **사용자 실행 표면(`skills/`·`hooks/`)에 닿는 차단은 수용 대상이 아니다.** 새로 발명한 규칙이 아니라 직전 사이클이 **사전 등록해 실제로 써 본 기준의 승격**이고, 자동이 아니라 **선택**이다 — 근거 없는 수용은 규율 이탈이다.
- **완료 기준을 쓰는 규율이 생겼다**: 방전할 수 없는 **전칭 문장을 금지**하고 **열거된 교란 목록**으로 쓰게 하며, 사이클 중 값이 변하는 수는 **선언처를 가리키게** 한다. 근거는 직전 사이클의 자기 기록 둘 — 기준 하나는 **방전 불가**였고 하나는 **구조적 충족 불가**였다(그 사이클에 일곱 번 갱신됐다). `skills/milestone/template.md`는 이 규율을 **한 줄로 가리킨다**(재서술 금지). **이 마일스톤의 완료 기준 자신이 그 규율로 쓰였다.**
- **직전 사이클이 반환한 미집행 자리가 소리를 낸다(`F8`)**: `F7`이 *"숨김도 본다"*를 증명하려면 픽스처의 그 파일이 **실제로 숨김이어야** 하는데, ps1 사본의 속성 설정이 삼키는 `catch` 안에 있어 **전제가 무너지면 축이 조용히 사라졌다**. 판별을 *"숨김 미포함 열거에서 그 파일이 보이지 않는다"*로 잡아 두 플랫폼에서 **각자의 숨김 표기로** 성립시켰고, 되돌림 실측이 **`F7`은 초록인데 `F8`만 붉은**(146/1) 자리를 실제로 만든다. 새 체크리스트의 **첫 적용 사례**다. `tests/discover` **146 → 147 케이스**.
- **닫을 수 없는 것은 닫을 수 없다고 적었다**: 직전 릴리즈 PR에서 회귀를 낸 **`awk -v` 생 개행** 부류에 기계 수단이 있는지 실측했고 결론은 **ⓑ 닫을 수 없다 — 기전이 아니라 짝이 막았다**(`awk … -v` 사이트 7곳 전부 한 러너에 있고 `run.ps1` 대응물이 **0건**이라, 짝으로 둘 ps1 케이스가 **구성상 공허**해서 같은 사이클이 세운 체크리스트 ①을 스스로 위반한다). 그래서 집행하지 않고 **측정값과 함께 독립 고지**로 적었다 — 사람이 지킬 규율 한 문장과, 이 부류가 **다섯 환경이 통과하고 여섯째만 붉을 수 있어 로컬 실측으로 판정할 수 없다**는 성질까지. **고지를 늘렸을 뿐 줄이지 않았으므로** 고지 재산정 규율에는 걸리지 않는다.
- **리뷰가 잡은 여섯이 전부 문서 정합이었다 — 그리고 그것이 이 사이클의 가장 유용한 관측이다**(rework 0 · 차단 0 · 권장 5 · 사소 1, 전부 in-review 수정): 검사의 공허나 양 셸 갈림은 **하나도 나오지 않았다**(직전 사이클의 다섯 라운드를 만든 부류다). 대신 **바뀐 규칙을 재서술한 자리들이 낡았다** — 특히 이 사이클의 헤드라인 산출물인 종료 조건이 **자기 스킬 텍스트 세 자리에서 무조건으로 부정**되고 있어, 리뷰어가 위에서 아래로 읽는 경로에서는 수용 경로가 **존재하지 않는 것으로 읽혔다**. 나머지는 미구현 태스크 하나(핵심 도메인 개념 갱신), 지표 오계수(`+1` → 실제 `+3`), 포인터가 이미 닫혀 없어진 항목 번호를 가리킨 것, 서술 불일치다. **새 체크리스트는 새 검사를 겨냥하지 문서 재서술의 정합은 겨냥하지 않는다** — 이 공백이 다음 사이클 후보로 기록됐다.
- **반증 서브에이전트가 다시 값을 했다**: 성립 **6건** 중 메인 리뷰가 독립적으로 찾은 것은 **둘뿐**이고, 반증이 나머지 넷을 세웠다 — 특히 **미구현 태스크와 완료 기준 하나의 미충족**은 메인이 놓친 자리다.
- **minor·불변**: 커맨드 **12종의 호출명·역할·산출물·git 행위**, 판정 토큰(`가능`/`불가`), **프리플라이트 게이트 ①②③의 집합·순서·판정 로직**, `.tide/phase` 값 집합, 보고서 템플릿·필수 섹션, **tide-guard**(훅 **무접촉**), 2.0 stable 동결 집합이 전부 불변이다. minor인 이유는 `skills/review/SKILL.md`·`skills/milestone/template.md`가 바뀌어 **사용자가 관찰할 수 있는 절차 변화**가 있다는 것이다(문서·하니스만 바뀐 patch가 아니다). **집행은 하나도 지워지지 않았고 케이스는 늘었다.** 하니스 6종을 **네 실행 환경**(dash · bash · Windows PowerShell 5.1 · pwsh 7)에서 재실행해 전부 FAIL 0(discover 147 · fleet 42 · fleet-cycle 24 · fleet-verify 30 · multi-repo 37 · site-includes 35 = **315 / 0**).
- **남은 것(정직 표기)**: ⑴ **`external-tool` 축(BSD awk·macOS)은 로컬에서 못 쟀다** — 이 사이클이 러너 두 사본을 만졌으므로 **릴리즈 PR의 macOS 레그를 태그 전에 읽는 것이 실질 게이트**다(직전 사이클이 그것으로 회귀 하나를 잡았고, 이번 사이클이 그 사실을 규약에 적었다). ⑵ `awk`는 GNU Awk로만 쟀다(우분투 CI는 mawk). ⑶ `jq` 경로는 일곱 사이클 연속 미실측(이번 사이클은 가드 무접촉이라 새 리스크는 없다). ⑷ **새 규칙 넷이 실제로 라운드를 줄이는지는 이 사이클이 증명하지 못한다** — 다음 몇 사이클의 rework 추이가 낸다. ⑸ 좁은 in-review 수정 허용 후보는 **이월**로 원장에 남았다(루브릭과 종료 조건이 실제로 왕복을 줄이는지 관측한 뒤 판단하는 것이 순서).

### [v2.15.0]
- **가드가 여덟 사이클 만에 열리고, 조용히 통과하던 입력에서 차단이 회복됐다(M42)**: 훅 stdin의 선두에 콘솔 코드페이지가 뭉갠 바이트가 붙어 도착하면 `ConvertFrom-Json`이 throw하고 `cwd`가 비어 폴백이 걸려 **차단이 통과로 뒤집혔다**(직전 실측 29/1). 마일스톤이 설계한 *"첫 `{` 앞을 버린다"* 만으로는 부류가 닫히지 않는다는 것이 구현 중 드러났다 — CP949 디코딩에서 남은 바이트가 **여는 중괄호 자체를 삼킨다**. 그래서 ps1 사본의 **입력 읽기를 바이트 → UTF-8 디코딩으로 바꿔 디코딩 축 자체를 제거**하고(실패 시 기존 읽기로 폴백) 그 위에 선두 잡음 관용 파싱을 얹었다. sh 사본도 같은 의미로 맞췄다. 부수로 무차단 탈출 비교를 **ordinal로 고정**했다 — 문화권 비교는 매칭 집합이 더 넓어 특정 값에서 차단이 통과로 뒤집힐 수 있다.
- **"같은 문서 안의 사실 모순"에 기계 수단이 있는지를 주장이 아니라 실측으로 결론냈다**: 층 전체는 기계가 물 수 없으므로 **부정 상태어 ↔ 긍정 집행어가 같은 대상(축 이름)을 두고 충돌하는 자리**로 좁혀 후보 수·오탐·표지 민감도·창 크기 1/2/3을 재고, 직전 사이클의 실제 3건이 규칙에 걸리는지를 회귀로 확인했다. 결론이 **"닫을 수 있다 — 단, 축 이름 부류에 한해"** 였으므로 `tests/discover`에 **Part I**를 신설했고, 닫지 못하는 부류(대상이 축 이름이 아닌 모순 · 표지를 쓰지 않은 모순 · 반대 방향)를 규약이 **이름으로 열거**한다.
- **인용 규약이 규약 집합 밖 문서까지 넓어졌다**: 인용 골격이 파일명을 명시하므로 대조 대상을 그 파일명이 가리키는 **실제 파일**로 일반화했다(`G5`~`G7`). 앵커 이름의 집합 전체 유일성은 규약 문서 집합에만 유지한다 — 확대하면 무관한 문서 간 이름 충돌이 폭발하고, 파일별 대조로 충분하다. **처음 돌렸을 때 세 자리가 붉었다**(사람이 찾아 둔 1건 + 검사가 처음 드러낸 2건).
- **문서에만 있던 규율이 처음으로 기계에 물렸다**: `run.ps1`의 **비-ASCII 0줄** 규율은 규약과 하니스 문서가 *"유지된다"*고 적어 왔지만 **어느 케이스도 CI 잡도 그것을 물지 않았고**, 이 사이클 중 실제로 주석에 비-ASCII 한 줄이 섞인 채 전 케이스가 초록이었다. `F5`가 그것을 물게 됐고, 무는 **파일 집합**은 명세(확장자 대소문자 구분 · 숨김 포함 · 디렉터리 제외)로 고정돼 `F7`이 픽스처로 문다. 무는 **폭**은 선언 그대로 `byte>127`이며 그 아래 제어 바이트는 묻지 않는다고 함께 적었다.
- **여섯 라운드가 같은 부류를 여섯 번 잡았다(rework 5)**: 리뷰가 다섯 번 `불가`를 냈고 **다섯 번 다 실측이 근거**였다 — ① 표지 추출이 없어 검사가 통째로 공허 ② 방향 단정이 실측보다 넓음 ③ 금지 집합에 이름만 있고 도달 불가(그 과정에서 `grep`이 줄 가운데 CR을 버린다는 것을 실측해 선언 줄 읽기에서 걷어냈다) ④ 새 검사 둘이 각각 공허 ⑤ 검사는 살아 있는데 **무는 대상 집합이 두 셸에서 갈림**. ⑤가 앞 넷과 다른 점은 처방이었다 — 앞 넷은 positive-control로 닫혔고 ⑤는 **명세를 글로 적고 픽스처로 무는** 수단이 따로 필요했다. 마지막 라운드의 반증 패스는 아홉 종의 교란(공백 경로 · CRLF 전 트리 · NUL · 제어 바이트 · 정션 · 로케일 전환 등)에서 **두 셸의 갈림을 하나도 만들지 못했고**, 그 결과가 이 판정의 근거다.
- **판정자=실행자 구조를 깨는 장치가 값을 했다**: 다섯 번의 리뷰 반증 패스 중 **세 번이 판정에 직접 값을 했다** — 메인이 놓친 공허 통과를 찾았고, 마지막 두 번은 메인이 `가능`으로 닫으려던 자리에서 차단을 세웠다. 특히 다섯째는 메인이 **자기가 방금 고친 줄**에서 폭 하나만 보고 넘어간 것을 잡았다.
- **문화권 기본값의 나머지 표면이 닫혔다**: 직전 사이클이 정렬에 대해 돈 감사를 `Sort-Object`·문자열 비교·해시테이블 키 **세 표면**에 대해 다시 돌았다(정렬 10건 중 7 · 문자열 비교 165건 중 34 · 해시 키 3건 중 1 교체). 미교체는 전부 사유를 남겼고, 감사 중 *"전수"*라 적혀 있던 앞 사이클의 감사에 **4곳이 남아 있던 것**도 함께 고정했다.
- **minor·불변**: 커맨드 **12종의 호출명·역할·산출물·git 행위**, `.tide/phase` 값 집합, **프리플라이트 게이트 ①②③의 집합·순서·판정 로직**, 스킬 절차, 보고서 템플릿·필수 섹션, 판정 표기·계측 줄 형식, 2.0 stable 동결 집합이 전부 불변이다. minor인 이유는 **가드가 이전에 통과시키던 입력을 차단**해 사용자가 관찰할 수 있는 동작 변화가 있기 때문이다(차단 집합이 넓어지는 방향뿐이라 major가 아니다 — 훅을 만진 것은 M36 이후 일곱 사이클 만이다). 하니스 6종을 **네 실행 환경**(dash · bash · Windows PowerShell 5.1 · pwsh 7)에서 재실행해 전부 FAIL 0(discover 146 · fleet 42 · fleet-cycle 24 · fleet-verify 30 · multi-repo 37 · site-includes 35).
- **릴리즈 PR의 CI가 이 사이클의 회귀를 하나 잡았고 그 자리에서 닫았다(debug-1)**: 리뷰까지 `가능`을 받고 나간 PR에서 **`posix (macos-latest)` 잡 하나만** 붉었다(`discover` 144/2 — `I1b`·`I3`). 원인은 **one-true-awk(BSD awk)가 `-v` 값의 생 개행을 거부**하는 것이었다(CI stderr: `awk: newline in string …`). 새 Part I가 표지 목록을 개행으로 구분해 넘기고 있었고, **GNU awk와 mawk는 그것을 받아 주므로 개발 기계도 우분투 CI도 영원히 초록**이었다 — 다섯 환경이 통과하고 여섯째만 붉은 전형이다. 표지는 선언 형식상 **공백을 포함할 수 없으므로** 전송을 공백으로 바꿔 닫았고(같은 함수의 축 목록이 이미 그 전송을 쓰고 BSD에서 통과했다), 로컬 네 환경 회귀 0을 확인했다. **이 결함이 가린 것도 함께 기록해 둔다**: 추출이 죽자 *"모순 0건"*을 단언하는 케이스는 `0==0`으로 조용히 통과했고, 붉힌 것은 **추출 positive-control과 음성 통제 둘뿐**이었다 — 파트마다 그 둘을 붙여 온 규율이 BSD에서 실제로 값을 했다.
- **남은 것(정직 표기)**: ⑴ 가드 sh 사본의 `jq` 경로는 이 기계에 `jq`가 없어 **이번에도 실측하지 못했다** — `sed` 폴백만 탔고, 그래서 되돌림 판별력의 측정값에 그 전제를 함께 적었다. ⑵ `awk` 구현은 로컬에서 GNU Awk로만 쟀다 — 우분투 CI(mawk)는 **통과했고**, macOS(BSD awk)가 위 회귀를 냈다. 그 수정 자체의 BSD 통과는 **로컬에서 확인할 수 없어 PR의 macOS 레그가 낸다**. ⑶ 새 검사 `F7`의 숨김 축은 픽스처 전제(숨김 속성 설정)가 삼키는 `catch` 안에 있어 **전제가 무너지면 조용히 약해진다** — 네 런타임 전부에서 전제가 성립함을 확인했으나 통제는 붙이지 못했고, 다음 사이클로 반환했다. ⑷ **`awk -v`에 생 개행을 넘기지 않는다는 규율이 어디에도 집행돼 있지 않다** — 위 회귀는 그런 규율이 *없어서* 생겼고 통로가 그대로 열려 있다(다음 사이클 항목). ⑸ 완료 기준 하나(*"케이스 수 선언을 한 사이클에 한 번만 갱신"*)는 **구조적으로 충족 불가**임이 드러났다 — 리뷰가 케이스를 더하면 impl 시점 기록이 필연적으로 낡는다. 기준 작성 규율 자체가 다음 마일스톤 후보다.

### [v2.14.1]
- **규약이 자기 문서에는 면제되던 자리를 닫았다(M41)**: M33이 세운 *"카운트는 단일 선언처"*가 정작 자기 커맨드 수에는 닿지 않았다 — `docs/project-context.md`가 **드리프트 가드의 캐노니컬 목록에 없는 채로** 커맨드 수를 세 자리에서 현재형으로 적고 있었고, 커맨드가 하나 늘어도 그 파일만 조용히 낡는 구조였다. 그 선언들을 **선언처를 가리키는 포인터**로 바꿨다(이월 처분 원장 행은 규약이 정한 **이력 서술 예외**라 소급 갱신하지 않았다).
- **2.0 stable 명단이 처음으로 집행된다**: 동결 집합(11종)의 **이름 명단**은 어떤 케이스도 보지 않았다 — 드리프트 가드가 세는 것은 실제 스킬 **파일 수**인데, 동결 집합의 크기는 파일 시스템으로 셀 수 없는 **선언**이라 같은 검사에 얹을 수 없기 때문이다. 명단 열거처를 **규약과 `README.md` 둘**로 정하고 **집합 일치·유일 열거처·음성 통제**를 새 파트(C-4)가 문다. 다음 major에서 승격 후보 하나가 동결 집합에 들어오면 규약·README·러너를 **함께 고쳐야 한다** — 그 비용은 의도된 것이다.
- **지표가 선언만 되고 3사이클 동안 계측되지 않았다**: 복제 선언 수 지표는 규약에 세는 규칙까지 있었는데 이후 세 사이클의 보고서 어디에도 값이 없었다. 원인은 게으름이 아니라 **발동 조건과 기재 위치가 없다는 것**이었다("보고서가 계측한다"고만 적고 *언제·어디에*를 정하지 않았다). **값을 바꾼 사이클만** · **완료보고서의 정해진 자리에** · **사실별 내역과 함께** 적도록 못박았고, 이 사이클이 그 규칙으로 발화한 첫 사례다(**−2**: 커맨드 수 −1 · 2.0 stable 집합 −1). 이 지표는 tide 자신의 기여자 규율이므로 **스킬에는 배선하지 않는다**(사용자 프로젝트에 요구하지 않는다 — 템플릿에 슬롯을 두면 그 슬롯 자체가 지표가 세는 복제를 하나 늘린다).
- **여섯 사이클 묵은 자기참조가 수용이 아니라 fix로 닫혔다**: 같은 파일 안의 절 참조는 인용 골격에 파일명이 없어 *"가드 밖 — 사람의 리뷰 영역"*으로 남아 있었다. **닫을 수 있음을 먼저 실측하고** 닫았다(Part G의 G4) — 후보를 좁히는 표지 셋으로 살아 있는 문서 전체에서 **후보 46건·미해소 0건**이고, 표지를 빼면 오탐이 각각 4건·3건 나며, 후행 표지 자체가 없으면 후보가 165건으로 늘어 대부분이 평범한 인용부호 산문이다. **넓힌 범위가 실제 결함 둘을 잡았다** — 자기 파일의 절을 이름이 어긋난 채 가리키던 참조와, 골격을 쓰지 않아 어느 가드도 보지 못하던 인용이다. `tests/discover` **110 → 119 케이스**(Part C 24 → 30, Part G 6 → 9).
- **가드가 만든 사람도 세웠다**: 리뷰 중 문서를 고치며 예시에 **실제 앵커 이름**을 적었더니 네 실행 환경이 즉시 붉어졌다 — 새 검사가 그것을 살아 있는 참조로 읽었기 때문이다. 진단이 파일과 이름을 찍어 원인이 바로 보였고(직전 사이클에 넣은 자기고발 조치가 값을 한 두 번째 사례), 골격 예시는 자리표 표기로 쓴다는 규율을 그 자리에서 문서에 적었다.
- **세 라운드가 같은 축을 좁혔다(rework 2)**: 리뷰가 두 번 `불가`를 냈고 두 번 다 **실측이 근거**였다 — ⑴ 자기참조가 "닫혔다"고 적었으나 새 검사의 범위가 규약 문서 집합뿐이어서 그 밖에서는 깨진 참조를 심어도 초록이었다(범위를 **살아 있는 문서 전부**로 넓히고 대조를 **그 파일 자신의 앵커**로 바꿔 닫았다) ⑵ 그 수정에 덧붙인 부수 효과 주장("파일명이 두 줄 이상 앞이면 붉어진다")이 반증됐다 — 그 이름이 자기 파일에도 있으면 **조용히 통과**한다. 창을 넓혀도 값이 바뀌지 않아(1·2·3 전부 46건·0건) **경계가 옮겨질 뿐 부류가 닫히지 않음**을 실측하고, 주장을 걷어낸 뒤 **"기계가 묻지 않는 것"을 넷으로** 열거했다. 마지막 라운드는 그 넷째의 이름표가 자기 기전보다 좁다는 것을 잡아 넓혔다(줄바꿈뿐 아니라 **애초에 골격을 안 쓴 인용**도 같은 자리로 떨어진다).
- **문화권 정렬이 두 셸의 의미를 갈라 놓고 있었다**: 새 검사가 **대소문자 머리글자를 섞은 목록**을 비교하자 `run.ps1`의 정렬이 ordinal이 아니라 **문화권 비교**임이 드러났다 — 직전 사이클이 BSD에서 잡은 것과 같은 부류이고, 비교 대상이 섞이기 전까지 **일곱 사이클 동안 잠복**해 있었다. 전용 헬퍼로 여섯 곳을 **전수 교체**했다.
- **규약이 자기 안에서 모순하던 세 자리를 고쳤다**: 실행 환경 축 표는 두 축이 **집행된다**고 적는데 같은 절의 산문은 여전히 *"미배선"·"미집행"*이라 적고 있었고(직전 사이클이 표만 갱신했다), 상호참조 절은 자기참조를 *"가드 밖"*이라 적으면서 아래에서는 *"집행한다"*고 적었다. 셋 다 실태에 맞게 다시 쓰되 **새로 주장한 집행은 없다** — 남은 것(기본 모드가 아닌 경로에서는 여전히 닫히지 않음 · 붉은 CI 위 사용자 확인의 라이브 미실측 · BSD 결함의 기전 미확정)을 함께 적었다.
- **patch·불변**: 커맨드 **12종의 호출명·역할·산출물·git 행위**, `.tide/phase` 값 집합, **tide-guard의 차단/통과 판정·메시지·exit 코드**(훅 **무접촉** — 여섯 사이클 연속), **프리플라이트 게이트 ①②③의 집합·순서·판정 로직**, 스킬 절차, 보고서 템플릿·필수 섹션, 판정 표기·계측 줄 형식, 2.0 stable 동결 집합이 전부 불변이다. **사용자 프로젝트에서 관찰되는 동작 변화가 0**이라 patch다 — 바뀐 것은 문서 위생·기여자 규율·하니스뿐이다. 하니스 6종을 **네 실행 환경**(dash · bash · Windows PowerShell 5.1 · pwsh 7)에서 재실행해 전부 FAIL 0(discover 119 · fleet 42 · fleet-cycle 24 · fleet-verify 30 · multi-repo 31 · site-includes 35).
- **함께 실린 것**: 이번 세션에 생성된 **회고**(M39~M40, `docs/reports/retro.md`). 이 사이클의 산출물은 아니지만 같은 세션의 읽기 전용 집계이며, 프리플라이트 3의 워킹트리 확인에서 드러나 합류했다.

### [v2.14.0]
- **미집행 축 둘에 집행이 붙었다(M40)**: 실행 환경 축 표에서 **"미집행"과 "스킬 미배선"이 사라졌다.** ⑴ `external-tool`(GNU sed·awk ↔ BSD sed·awk) — CI `posix` 잡 매트릭스에 **macOS 레그**를 더해 BSD 도구 환경에서도 하니스가 돈다. 잡 키는 `posix` 그대로라 축 표의 토큰이 바뀌지 않는다. ⑵ `workflow-syntax`(로컬 YAML 유효 ↔ GitHub 컴파일) — 이 축은 로컬에서 닫을 수 없어 확인 자리가 `pr` 모드 PR의 CI뿐인데 **`/tide:release` 절차에 그 조회가 없었다.** 이제 마무리(finalize)가 **태그를 달기 전에** PR CI를 조회해 보고하고, 실패·미완료 잡이 있으면 사용자 확인을 받는다(프리플라이트 게이트 ①②③은 불변 — 새 게이트가 아니라 마무리 경로 안의 확인이다). 근거는 v2.13.0 마무리에서 **사람이 손으로 `gh pr checks`를 친 것이 절차가 아니라 습관**이었다는 실측이다.
- **축 이름도 기계가 푸는 표기를 갖는다**: 표의 축 이름 칸이 `axis:<이름>` 토큰을 하나씩 갖고, `env-axes:` 선언 집합과 **양방향**으로 대조된다(선언에만 있어도, 표에만 있어도 FAIL). 여기에 **데이터 행 수 == 표기 수**를 더해, 표기가 **아예 없는 산문 행**이 축 표에 섞여 드는 자리까지 닫았다. `tests/discover` **102 → 110 케이스**(Part H 13 → 18, Part D 11 → 14).
- **고지를 줄이는 절차가 규약이 됐다**: 직전 사이클이 표기를 넣은 뒤 실측 없이 정직 고지를 줄였다가 리뷰의 반증으로 차단·반환된 일을 **규약 문장으로 승격**시켰다 — *"「기계가 묻지 않는 것」에서 항목을 줄이려면 그 구멍을 재현하는 공격이 이제 FAIL함을 실측으로 보이고 근거를 적는다. 표기·배선·잡이 생겼다는 사실만으로 줄이는 것이 `vacuous-pass`다."* 이번 사이클은 그 절차를 밟고 나서 줄였다 — 미선언 행에 표기를 적음·표기 없는 순수 산문 행·선언된 축의 표기 삭제, **세 공격이 전부 FAIL함을 양 셸에서 확인한 뒤** 항목을 지웠다. 실제로 그 실측이 **설계를 바꿨다**: 집합 대조만으로는 두 번째 공격이 초록이어서 행 수 대조를 추가했다.
- **리뷰가 두 구멍을 실측으로 열었다**: ⑴ 새로 배선한 PR CI 확인을 release 스킬에서 통째로 지워도 **초록**이었다 — 같은 부류의 메커니즘 네 개(커버리지·번호경고·미커밋 범위·캐노니컬 카탈로그)가 전부 Part D 결합을 갖는데 다섯 번째만 없었다. **D5**를 신설했고, 규약 **본체가 아니라 조각**(`conventions-release.md`)을 결합한 첫 사례다. ⑵ 매트릭스에서 macOS 레그를 빼도 **초록**이다 — `job:` 토큰의 해상도는 **잡 키**까지라 레그는 기계에 보이지 않는다. 잡을 통째로 지우는 것은 물지만 **레그 단위는 사람이 본다**는 사실을 고지에 명시했다(닫을 수 없으면 닫을 수 없다고 적는다).
- **정리 경로가 실측대로 고쳐졌다**: v2.13.0 마무리에서 릴리즈 브랜치 원격 삭제가 `remote ref does not exist`로 실패했다 — GitHub이 머지 시 원격 브랜치를 이미 지웠고 **로컬에는 낡은 추적 ref만** 남아 있었기 때문이다. 존재 판정을 **`git ls-remote --heads`(원격에 직접 묻는다)**로 하고, 원격에 없으면 삭제 대신 **`git remote prune`으로 낡은 ref를 거두도록** 규약을 고쳤다. 함께 `/tide:kickoff`가 만드는 신규 프로젝트 규약에 M39가 더한 리뷰 규율 배선은 그대로 유지된다.
- **이월 원장이 다시 이어졌다**: M36에서 끊겨 있던 처분 원장에 **M37·M38·M39** 행을 채우고(각 행의 근거는 해당 보고서 본문 — 지어내지 않았다), 두 사이클 연속 후속으로 남던 누락을 닫았다. `site-includes` 완주 플래그는 **수용(설계)**으로 종결했다 — 그 러너는 결과 줄과 종료 로직 사이에 간격이 없어 플래그를 두면 자기 공허한 단언이 되고, "덜 돌고 초록"은 케이스 수 대조가 이미 덮는다.
- **minor·불변**: 커맨드 **12종의 호출명·역할·산출물·git 행위**, `.tide/phase` 값 집합, **tide-guard의 차단/통과 판정·메시지·exit 코드**(이번 사이클도 훅 **무접촉** — M36 이후 다섯 사이클 연속), **프리플라이트 게이트 ①②③의 집합·순서·판정 로직**, 보고서 필수 섹션, 판정 표기·계측 줄 형식, 2.0 stable 동결 집합이 전부 불변이다. minor인 이유는 CI 실행 환경이 하나 늘고 `pr` 마무리가 사용자 확인을 하나 더 하는 것이 **사용자 관찰 가능**하기 때문이다. 하니스 6종을 **네 실행 환경**(dash · bash · Windows PowerShell 5.1 · pwsh 7)에서 재실행해 전부 FAIL 0(discover 110 · fleet 42 · fleet-cycle 24 · fleet-verify 30 · multi-repo 31 · site-includes 35).
- **새 레그가 첫 실행에서 결함을 잡았다 — 그리고 그것이 이 릴리즈에 실렸다**: `external-tool` 축은 로컬에 macOS가 없어 레그의 판정을 못 본 채 릴리즈 PR로 나갔고, **그 PR의 CI가 붉었다**(GNU 환경 전부 110/0 ↔ macOS 109/1). 걸린 것은 `tests/discover`의 앵커 이름 유일성 검사였고, 원인은 그 파이프라인이 **이 러너에서 유일하게 `LC_ALL=C`가 빠진 `sort`/`uniq`**였다는 것이다 — 로케일이 걸리면 비교가 바이트 동등이 아니라 collation 동등이 된다. 결정적 근거는 **`run.ps1`이 ordinal 비교**라는 점으로, 이 누락은 곧 **두 셸의 의미가 갈려 있었다**는 뜻이고 GNU에서는 두 경로가 우연히 같은 답을 내다가 BSD에서 갈렸다. 같은 부류를 전수 감사해 `tests/fleet-cycle`의 정렬 한 곳을 더 닫았고, `tests/fleet`의 `sort -V` 두 곳은 **macOS 레그가 통과시킨 것 자체가 BSD 지원의 실측**이라 유지했다(그 케이스는 M2·M9·M10 픽스처로 버전 정렬을 직접 구별한다). **기전은 로컬에서 재현하지 못했다** — 그래서 이 검사는 이제 실패 시 **묶인 앵커 이름을 출력**한다(양 셸). 수를 세는 단언이 붉어져도 원인을 말하지 않는 것이 이번에 겪은 문제였고, 다음 실행이 결론을 낸다. 착수 전 정적 감사(GNU 전용 구문 7부류 전부 0건)는 통과 확률의 근거였지 통과의 증거가 아니었음이 그대로 확인됐다.

### [v2.13.0]
- **커버리지 체크가 "태그에 실릴 것"을 본다(M39)**: 이 검사는 커밋된 diff만 봤는데 `/tide:release`는 프리플라이트를 통과한 **뒤에** 스테이징·커밋한다 — 그래서 정상 tide 사이클에서 **범위가 사실상 늘 공집합**이었고(impl·review는 커밋하지 않으므로 HEAD가 직전 릴리즈 그대로다), 정작 release 자신이 태그에 싣는 편승은 검사 밖이었다. 이제 범위는 **커밋된 변경 ∪ 미커밋 변경**(`git status --porcelain`)이다. 스테이징 범위도 세 게시 모드가 **한 자리를 참조**하도록 명시해, 검사가 본 집합과 실제로 실리는 집합을 같게 했다. 출처는 외부 사용자 프로젝트의 **라이브 실측** — 편집기 임시 파일이 그 경로로 태그에 실렸고, 되돌린 주체는 이 검사가 아니라 사람이 읽은 `git status`였다. 규약에 porcelain **경로 추출 규칙**(이름 변경의 화살표 우변 · 삭제는 범위 유지 · 따옴표 경로)도 함께 적었다.
- **실행 환경 축의 집행 칸이 기계가 푸는 표기를 갖는다**: 축 → CI 잡 매핑이 별도 선언 줄과 표 산문 **두 곳**에 있던 것을 **표의 `job:<이름>` 토큰 하나**로 합쳤다(선언처 2 → 1). 환경 축이 아닌 CI 잡(양 셸 하니스 집합의 짝을 보는 `pairing`)은 **면제로 선언**한다 — 축인 척 표에 올리는 것도, 아무 데도 안 적어 조용히 빠지게 두는 것도 틀렸기 때문이다. 그래서 CI에 잡을 더하면 "축인가 면제인가"를 **반드시 한 번 결정**하게 된다. `tests/discover` Part D·H를 재배선해 **96 → 102 케이스**.
- **검사를 고치면서 검사가 무는 범위도 실측했다**: 표에서 잡 토큰을 빼면(미등재) · 미선언 행에 토큰을 숨기면(고아) · 면제가 낡으면 · 실재하지 않는 잡을 **토큰으로** 지목하면 — **양 셸이 전부 FAIL**한다. 반대로 실재하지 않는 이름을 **산문으로** 주장하는 경우는 여전히 초록이고, 그 사실을 규약·하니스 문서·러너 두 사본에 **고지로 남겼다**(닫을 수단이 없으면 없다고 적는다).
- **갓 태어난 프로젝트도 리뷰 규율을 갖고 출발한다**: `/tide:kickoff`가 만드는 규약 골격 요구 항목이 5 → **7**로 늘어 **리뷰 검증 규율**(반증 시도·재검증·재작업 라운드)과 **차단 등급 판례**(`vacuous-pass`·부인 기록)를 요지 수준으로 담는다 — 상세의 단일 원본은 설치된 스킬이다. 외부 프로젝트에서 이 둘이 없는 규약으로 출발해 **두 사이클 뒤 리뷰가 스스로 채운** 사례가 근거다.
- **리뷰가 이 사이클의 차단을 잡았다(rework 1)**: 첫 라운드가 "산문 위장 층이 닫혔다"고 적으며 정직 고지를 **넷에서 셋으로 줄였는데**, 리뷰가 그 주장을 실측으로 반증하고 `불가`로 반환했다 — 표기를 넣었다는 이유로 고지를 줄이는 것이 `vacuous-pass`라는 판례가 규약에 명문화됐다. 재작업이 고지를 복원하고, 같은 드리프트를 놓친 **캐노니컬 카탈로그**도 결합에 넣었다(Part D4).
- **minor·불변**: 커맨드 **12종의 호출명·역할·산출물·git 행위**, `.tide/phase` 값 집합, **tide-guard의 차단/통과 판정·메시지·exit 코드**(이번 사이클도 훅 **무접촉**), 프리플라이트 게이트의 순서·판정 로직, 보고서 필수 섹션, 판정 표기·계측 줄 형식, 2.0 stable 동결 집합이 전부 불변이다. minor인 이유는 커버리지가 보는 범위와 kickoff 산출물이 **사용자 관찰 가능**하게 넓어졌기 때문이다. 하니스 6종을 **네 실행 환경**(dash · bash · Windows PowerShell 5.1 · pwsh 7)에서 재실행해 전부 FAIL 0(discover 102 · fleet 42 · fleet-cycle 24 · fleet-verify 30 · multi-repo 31 · site-includes 35) — **dash 레그를 로컬에서 돌린 첫 사이클**이다(직전 사이클은 환경에 dash가 없어 CI에 맡겼다).
- **함께 실린 것**: v2.12.0 시점에 생성돼 커밋되지 않았던 **회고**(M33~M38, `docs/reports/retro.md`). 이번 사이클 산출물이 아니며 프리플라이트 3의 워킹트리 확인에서 드러나 사용자 확인 후 합류했다.

### [v2.12.0]
- **러너가 덜 돌면 스스로 붉어진다(M38)**: 완주 가드(M37)는 러너의 **중단**을 잡았지만, 죽지 않고 **덜 도는** 경로 — 조건 분기가 파트를 건너뛰거나 발견형 스캔이 0건을 집는 것 — 은 `discover` 한 곳에서만 걸렸다. 이제 하니스 **6종 전부**가 종료 직전 자기 `README.md`의 `cases:` 선언과 **실제 실행 수**를 대조하고, 선언을 **못 읽어도 FAIL**이다("못 읽어서 건너뛰고 통과"가 이 마일스톤이 없애려는 부류 그 자체다). 6종 × 양 셸에서 케이스 하나를 건너뛰게 만들어 **같은 단언에서 같은 수치로** FAIL함을 실증했고, README를 지웠을 때도 FAIL한다. `tests/site-includes/README.md`를 신설해 6종이 같은 구성(`run.sh`·`run.ps1`·`README.md`)을 갖는다.
- **짝 없는 하니스가 조용히 빠지지 않는다**: `run.sh`만 있는 하니스는 PowerShell 잡이 **초록인 채 한 번도 돌리지 않는다**. 새 `pairing` 잡이 두 러너 잡의 **발견 집합을 비교**해 다르면 **빠진 쪽을 이름으로 보고**하고 실패한다(목록 하드코딩 없음·한쪽이 0건이면 그 자체로 실패·둘 다 없는 디렉터리는 대상 아님). M37 리뷰가 사소로 매기며 남긴 부인 기록을 닫았다.
- **조용히 덜 훑던 두 자리를 닫았다**: `tests/discover/run.ps1`의 문화권 기반 `StartsWith` **8곳을 `Ordinal`로** 바꿨다 — 선행 소프트 하이픈 이름 하나로 POSIX 글롭은 파일을 훑는데 ps1만 건너뛰어 **90/0 ↔ 89/1**로 갈리던 것을 실측했다(M37 재작업 3과 같은 방향의 조용한 초록). Part B의 점 디렉터리 규율도 **의미를 먼저 정하고**("점으로 시작하는 디렉터리는 커맨드 스킬이 아니다") 양 셸·양 파트를 그 하나의 의미에 맞췄다 — 한 러너 안에서 파트끼리 규율이 다르던 자리다.
- **기동 맥락을 진단하고 집행했다**: `tests/multi-repo`가 Git Bash에서 **29/1**, 네이티브 호스트에서 **30/0**이던 원인은 BOM도 `$OutputEncoding`도 아닌 **콘솔 입력 코드페이지**였다 — 가드는 stdin을 `[Console]::In`으로 읽고 그 디코딩은 자식이 **띄운 호스트에서 물려받은** 코드페이지를 따른다. CP949에서 선두 BOM이 제3의 형태로 디코드돼 JSON 파싱이 throw하고 가드가 폴백 **통과**해 버렸다. 이제 러너가 첫 단언 전에 **같은 stdin 경로로 자기 채널을 탐침**해 지원 밖이면 케이스 0건·진단 출력·**exit 1**로 거부한다 — 조용히 다른 판정을 내지 않는다. 탐침은 코드페이지 화이트리스트가 아니라 **행위 기반**이다.
- **실행 환경 축이 이름을 갖고 선언됐다**: 규약에 `## 실행 환경 축` 절이 서고 축마다 **이름 · 두 극 · 무엇이 집행하는가**가 적힌다(POSIX 셸 방언 · PowerShell 런타임 · 기동 맥락 · 워크플로 문법 · 외부 도구 구현). 집행이 없는 축은 **"미집행"으로 명시**한다 — 집행 없는 축을 집행되는 것처럼 적는 것이 `vacuous-pass`다. `tests/discover` **Part H**(11 케이스)가 축 이름 집합 일치 · 규약 표 행 실재 · **CI 잡의 워크플로 실재** · **매핑 커버리지**를 집행하고, 가짜 축·가짜 잡 이름 음성 통제를 함께 둔다.
- **리뷰가 같은 부류를 세 번 잡았고 세 번째에 닫혔다**: 규약의 집행 주장을 **두 번 실측으로 반증**했다 — ⑴ CI 잡을 **통째로 지워도** 양 셸 초록이었다(→ 잡 실재 검사 신설) ⑵ 매핑에 **올리지 않는 것만으로** 그 검사를 피할 수 있었다(→ 커버리지 검사 신설). 세 번째는 규약이 **`/tide:release`에 없는 확인을 있다고 적은 것**이라 사실로 좁혔다 — 그 확인은 **사람이 하고 스킬에 배선돼 있지 않다**(배선은 다음 사이클). 그 결과 규약은 이제 **기계가 묻지 않는 층을 넷으로 열거**한다: 잡을 지목하지 않는 집행 칸의 진실성 · 잡을 지목한 칸의 나머지 서술 · 실재하지 않는 이름을 잡인 것처럼 적은 산문 · 선언되지 않은 표 행.
- **minor·불변**: 커맨드 **12종의 호출명·역할·산출물·git 행위**, `.tide/phase` 값 집합, **tide-guard의 차단/통과 판정·메시지·exit 코드**(이번 사이클은 훅 **무접촉** — 변경 0줄), 프리플라이트 게이트, 보고서 필수 섹션, 판정 표기·계측 줄 형식, 2.0 stable 동결 집합이 전부 불변이다. 하니스 6종을 **세 실행 환경**(bash · Windows PowerShell 5.1 · pwsh 7)에서 재실행해 **18회 전부 FAIL 0**(discover 96 · fleet 42 · fleet-cycle 24 · fleet-verify 30 · multi-repo 31 · site-includes 35). **dash 레그는 이번 사이클 환경에 dash가 없어 재현하지 못했고** CI 우분투 레그가 그 축의 확인 지점이다 — 워크플로가 GitHub에서 실제로 컴파일되는지와 함께 **이 릴리즈의 CI가 첫 실증**이다.

### [v2.11.0]
- **리뷰 판정이 다시 신호를 낸다(M36)**: 차단급 결함을 찾은 리뷰는 그것을 **고치지 않고** 판정 `불가`로 impl에 반환한다(`수정 권한 제한`). 무엇이 차단인지는 새 루브릭이 아니라 **이미 내려진 판례**가 정하고(`vacuous-pass` — 가드·검증이 공허하게 통과하거나 문서가 집행력을 사실보다 넓게 주장하면 차단), 판례 부류를 차단으로 매기지 않을 때는 **부인 사유를 남기는 것이 의무**다(`precedent-waiver` — 부인 기록 없는 하향은 규율 이탈). 함께 tide-guard의 **fresh clone 창**(`.tide/phase`가 없어 보호가 0인 구간)을 규약·훅 주석·발행 문서에 명시했다 — 같은 원칙을 가드 쪽에도 적용한 것이다.
- **같은 사실을 적는 곳을 5곳에서 2곳으로 줄이고 기계가 물게 했다(M37)**: "어떤 커맨드가 phase를 쓰는가"가 규약·훅 주석 2사본·project-context·발행 페이지에 흩어져 있었고, 매 라운드가 그중 일부만 고쳐 발산했다. 이제 **열거는 규약과 발행 페이지 둘뿐**이고, 나머지는 포인터만 둔다. 두 곳의 **이름 집합 일치**와 **"열거처가 그 둘뿐"**을 `tests/discover` Part C-3이 집행한다 — 세 번째 파일에 명단을 적으면 양 셸이 FAIL한다(71 → 85 케이스). 곁들여 **복제 선언 수를 세는 규칙**을 규약에 신설하고, `kickoff`가 `.gitignore`에 `.tide/phase`와 `.tide/debug-session` **두 항목**을 넣도록 고쳐 규약과의 모순을 닫았다.
- **하니스가 중간에 죽으면 반드시 붉다**: `run.ps1` 6종이 `try`만 있고 `catch`가 없어, 오류가 나면 남은 파트를 통째로 건너뛰고도 **성공 배너 + exit 0**을 냈다. 6종 전부에 **trap**(중단 줄 번호·원인 출력 → exit 1)과 **완주 플래그**(결과 줄만 세움)를 넣었다. 6종 각각에 오류를 주입해 전부 exit 1을 실증했다 — 구조가 넷으로 갈리는데(try/finally · 중첩 try · try 없음) 전부 물었다. `site-includes`만 trap 단독인데, 그 러너는 결과 줄과 종료 로직 사이에 간격이 없어 플래그를 두면 **자기 공허한 단언**이 되기 때문이다.
- **측정이 선언과 어긋나 있었다 — 런타임을 축으로 세웠다**: 러너 6종이 헤더에서 **Windows PowerShell 5.1**을 선언하는데, `tests/discover/run.ps1`이 **6+ 전용 3인자 `Join-Path`**를 써서 5.1에서는 85 중 **34 케이스만 돌고 exit 0**이었다 — 미실행 구간에 위 C-3의 "유일 열거처" 단언이 들어 있었다. 네 라운드의 모든 측정이 선언되지 않은 런타임에서 이뤄져 이것을 놓쳤다. 코드를 선언에 맞추고, 결과 줄에 **`[runtime: …]`을 각인**해 어느 런타임의 수치인지 러너 자신이 말하게 했다.
- **테스트 CI가 처음 생겼다**: `.github/workflows/`에는 사이트 배포만 있었고 하니스는 **CI에서 한 번도 돈 적이 없다**. `tests.yml`을 신설해 windows × {PowerShell 5.1, 7} + bash(windows·ubuntu) **런타임 매트릭스**로 돌린다. 하니스 목록은 **하드코딩하지 않고 파일 시스템에서 발견**하며 — 하드코딩하면 7번째 하니스가 초록인 채 영원히 안 돌아간다 — **0개 발견은 실패**다. 러너가 작업 디렉터리를 옮기므로 매 호출 전 절대 경로로 복귀한다(리뷰가 잡은 두 결함).
- **순서가 설계였다**: 다섯 라운드 중 넷에서 결함은 늘 **"고치는 변경" 안에** 있었다. 이번엔 결함을 고치기 **전에** 완주 가드를 먼저 넣었고, 그러자 아직 안 고친 차단이 **스스로 고발됐다**(`# ABORTED at line 331` · exit 1). 변경량은 가장 컸는데도 새 차단이 나지 않은 첫 라운드다.
- **minor·불변**: 커맨드 **12종의 호출명·역할·산출물·git 행위**, `.tide/phase` 값 집합, **tide-guard의 차단/통과 판정·메시지·exit 코드**(이번에도 훅은 **주석 전용 변경**, 비주석 0줄), 프리플라이트 게이트, 보고서 필수 섹션, 판정 표기·계측 줄 형식이 전부 불변이다. minor인 이유는 리뷰 절차 변경과 `kickoff`의 `.gitignore` 가산이 **사용자 관찰 가능**하기 때문이다. 전 하니스를 **네 실행 환경**(POSIX dash · POSIX bash · 5.1 · 7)에서 재실행해 **24회 전부 FAIL 0**(discover 85 · fleet 41 · fleet-cycle 23 · fleet-verify 29 · multi-repo 30 · site-includes 34) — 5.1과 dash에서 전 하니스가 통과한 것은 둘 다 처음이다.
- **새 CI가 릴리즈 도중에 두 번 값을 했다**: ⑴ 첫 푸시에서 워크플로가 **컴파일되지 않았다**(잡 0개) — `shell: ${{ matrix.shell }}`을 GitHub이 거부한다. 매트릭스 값을 `env:`로 흘리고 `shell:`은 리터럴로 두어 고쳤고, 겸사겸사 하니스를 **자식 프로세스**로 띄우게 해 작업 디렉터리 누수와 주입된 `$ErrorActionPreference`를 함께 무력화했다. **로컬 YAML 유효성은 GitHub 워크플로 유효성이 아니다.** ⑵ 그다음 실행에서 **우분투 레그가 진짜 결함**을 냈다 — `\xHH`는 POSIX printf에 없는 확장이라 우분투의 `/bin/sh`(dash)가 이스케이프를 해석하지 않고 글자 그대로 내보내, `tests/fleet-verify/run.sh`의 BOM 픽스처에 BOM이 들어가지 않았다(25/4). 로컬 Git Bash는 `sh`가 bash라 **일곱 라운드 동안 보이지 않던** 결함이다. 같은 부류를 전수 조사해 **세 곳**을 POSIX 팔진으로 바꿨다(세 번째는 우분투에서 통과하고 있었으나 GNU sed가 우연히 받쳐 준 경우였다). 이제 로컬 `dash`가 우분투 레그를 재현한다.

### [v2.10.3]
- **규약이 파일 하나에서 "문서 집합"이 됐다(1단계 분할)**: `docs/conventions.md`의 `### 상호참조 무결성`이 규약을 **본체 + `docs/conventions-{주제}.md` 조각**으로 전제한다 — 발견 글롭은 **`docs/conventions*.md`**(목록을 스크립트·문서에 하드코딩하지 않는 것이 요점), **인용은 앵커가 실제로 사는 파일명**을 가리켜야 하고, **앵커 이름 유일성의 범위는 집합 전체**이며, 조각 파일의 요건은 셋이다 — ① `[start:body]`/`[end:body]` 마커 + 도입 문단은 마커 밖 ② 사이트 셸 페이지와 nav 항목을 **같은 사이클에** ③ 본체에 그 조각을 가리키는 **포인터를 제목으로** 남긴다(목차에서 절이 사라지지 않게).
- **`릴리즈 게시 (gh)` 규약을 `docs/conventions-release.md`로 분리**: 126줄을 **문장 무수정으로** 옮겼다 — `HEAD`의 절과 조각 본문을 줄 단위로 대조해 **다른 줄이 정확히 3개**(파일을 건너뛰게 된 자기참조를 인용 골격으로 바꾼 것)임을 검증했다. 이동과 축약을 섞지 않은 것은 의도한 규율이다(섞으면 무엇이 이동이고 무엇이 변경인지 리뷰가 가려낼 수 없다). 최종 실측 — 본체 **927줄/앵커 41** · 조각 **137줄/앵커 9** · 집합 **1064줄/앵커 50**.
- **집행 배선 — `tests/discover` Part G 일반화**: 앵커를 **파일별로** 묶고, 인용 후보 줄에 **등장한 규약 파일명**으로 대조할 집합을 고른다. 그래서 **절을 조각으로 옮기고 인용을 안 고치면 잡힌다**(이 일반화의 핵심 가치 — 양 셸이 같은 단언에서 FAIL함을 실증). 한 줄에 규약 파일명이 둘이면 어느 쪽 집합에든 있으면 통과(안전 측·오탐 없음). 조각이 0개면 집합이 본체 하나뿐이라 일반화 **이전과 stdout이 바이트 동일**하다(회귀 고정). 케이스 수는 **6 유지**(내부 리팩터)이고 데이터 기반 검사라 ps1의 **byte > 127 = 0** 규율도 그대로다.
- **사이트는 러너·CI 수정 0으로 따라왔다**: `tests/site-includes`가 셸을 **발견**하므로 새 셸이 자동으로 검사 대상이 되고(shells 4 → 5, 28 → 34 케이스), 배포 트리거는 paths 필터가 없어(v2.6.0에서 그 목적으로 정리) 새 루트 파일이 누락될 수 없다. 규약만 적고 집행을 미루지 않는다는 원칙이 이번엔 **손댈 배선이 하나(Part G)뿐**이라는 형태로 확인됐다.
- **가드가 계획의 수동 집계를 두 번 교정했다**: 마일스톤은 갱신할 인용을 `릴리즈 게시 (gh)` 문자열 검색으로 **4건**이라 셌지만 실제로는 **5건**이었다(소절 `pr` 모드를 따로 인용한 곳). 4건만 고친 상태에서 하니스가 곧바로 `G1` FAIL을 냈다. 리뷰의 반증 패스는 impl 보고서가 인용 총수를 **68**(분할 이전 값 — 하니스 케이스 수와 우연히 같아 혼동)로 적은 것을 잡아 **74**로 정정했다 — 이 수치가 다음 사이클의 입력이라 정정이 중요했다.
- **리뷰가 고친 것**: 이동으로 **끊긴 참조 2건**(본체→조각 방향 1건 — 마일스톤이 반대 방향만 지시했다 · 파일명 없는 느슨한 참조 1건 — 백틱 표기라 따옴표 감사에도 걸리지 않았다), 포인터가 무관한 소절 안에 끼여 **목차에서 사라진 것**, `run.ps1`의 파일 열거가 **Hidden 속성 파일을 건너뛰어** 셸 간 스캔 범위가 갈리던 것(`-Force` + 선행 점 이름 제외로 POSIX 글롭 의미와 일치), 그리고 보고서 수치 2건.
- **patch·불변**: **규약의 내용이 한 줄도 바뀌지 않았다**(위치만 이동). 커맨드 **12종의 호출명·역할·산출물·git 행위**, `.tide/phase`, **tide-guard(이번에도 무수정)**, 프리플라이트 게이트, 보고서 필수 섹션, 판정 표기·계측 줄 형식 전부 불변이다. 사이트 nav 항목이 하나 늘었지만 그 **내용은 이미 사이트에 있던 것**이고 tide 소비자가 호출하는 것은 아무것도 바뀌지 않았다. 전 하니스를 **양 셸로** 재실행해 225 케이스 동일 판정(discover 68·site-includes 34·fleet 41·fleet-cycle 23·fleet-verify 29·multi-repo 30). **오케스트레이션 절(243줄) 분할과 인용 규약의 문서 일반 확대는 범위 밖**이며, 조각 하나당 스캐폴딩이 13줄 붙으므로 다음 사이클에서 **분할 종료 조건**을 정하는 것을 후속으로 남겼다.

### [v2.10.2]
- **상호참조 무결성 규약 신설(문서 위생·기여자 규율)**: 다른 문서가 규약을 가리키는 **인용**이 개명·삭제로 조용히 끊기지 않게 하는 규약이 `docs/conventions.md`의 "문서 자기서술 정합"에 **세 번째 규약**으로 선다(①카운트 단일 선언처 ②역할 앵커 전파 ③상호참조 무결성). 정의하는 것 — **인용 골격**(`` `docs/conventions.md`의 "{앵커 이름}" 절 ``), **인용 가능한 앵커 = `##`·`###` 제목만**(볼드 항목은 소절로 승격하되 승격이 문맥을 해치면 인용 쪽을 실재 앵커로 고친다 — 층위를 둘로 고정해야 기계 대조가 성립한다), **이름 유일성**(겹치면 상위 맥락을 괄호로 붙여 유일화), **공백만 제거하는 정규화**(`버전·CHANGELOG` ≡ `버전 · CHANGELOG` — 넓히면 오탐이 는다), **인용은 한 줄 안에 쓴다**, **집행은 FAIL**(추출 0건도 FAIL). 대상은 **살아 있는 문서**이고 `docs/milestones/*`·`docs/reports/*`는 역사 기록이라 제외된다.
- **출처 — 인용 표면이 이미 새고 있었다**: 계획 중 실측으로 **끊긴 인용 1건·표기 흔들림 1건·중복 앵커 이름 1건**을 발견해 범위에 넣었고, 구현 중 3건이 더 나와 **총 6건**이었다. 인용이 가리키는 층위가 `##`·`###`·**볼드 항목** 세 가지로 갈려 있어 "절"이라는 말이 실제로는 셋 중 무엇이든 가리켰고, 그런 인용은 개명·삭제되면 **가드 없이 조용히 끊긴다**. 리뷰가 여기에 **줄바꿈된 인용 6건**(가드가 검사한 적조차 없던 인용)을 더 찾아 함께 닫았다.
- **인용 실태 정합화**: `릴리즈 빌드 출력 검증`을 볼드 항목에서 **`###` 소절로 승격**(택한 경로의 근거는 보고서에), `버전 · CHANGELOG` 표기를 제목과 맞춤, **중복이던 `### 불변` 두 소절을 유일화**(`불변 (리뷰 검증 규율)` / `불변 (문서 자기서술 정합)`). 목록 한가운데 항목이라 승격이 문맥을 해치는 둘(`항목 상태값`·`릴리즈 경로`)은 규약이 허용한 대안대로 **인용 쪽을 고쳤다**. 자주 짧게 불리던 소절 제목 넷(`교차 사이클 자동화`·`통합 검증`·`` `release` 모드 ``·`` `pr` 모드 ``)은 제목을 줄여 **인용 6곳이 고치지 않아도 정확해지게** 했다.
- **집행 배선 — `tests/discover` Part G 신설**: **G1** 인용 추출 → 앵커 실재 대조(살아 있는 문서에서 파일명이 든 줄의 따옴표 구획을 뽑아 `##`·`###` 제목 집합과 정규화 비교), **G2** 통제 4종(인용·앵커 추출 0건이면 FAIL·가짜 이름 부재·제목 이름 유일성), **G3** 줄바꿈 인용 통제(후보 줄의 따옴표가 미종결이면 FAIL). 스크립트에 한글 리터럴을 두지 않는 **데이터 기반** 검사라 ps1의 **byte > 127 = 0** 원본 규율을 유지한다(양 셸 **68/68**, 기준선 62에서 +6). 스니펫 인클루드 지시어 줄과 골격 예시의 중괄호 자리표는 인용이 아니라 제외한다 — 후자가 없으면 규약을 적는 행위 자체가 그 규약을 FAIL시킨다.
- **규약이 9 minor 만에 처음으로 줄었다**: `docs/conventions.md` **1066 → 1029줄(−37, −3.5%)**. 새 규약 28줄과 승격 소절이 더해진 위에서 줄었으므로 실제로 걷어낸 서술은 약 67줄이다. 대상은 **중복 서술·그때의 경위 서사·늘어놓은 예시** 셋뿐이고 **규약의 의미는 하나도 지우지 않았다** — 인용 0인 앵커 17개 중 "실제로 죽었다"는 판정은 **1건**뿐이었고(나머지는 상위 절 인용이나 하니스 토큰으로 살아 있었다), 판단이 애매하면 남겼다. 의미 보존은 축약 후 **Part B~G 전부 통과**가 기계로 뒷받침한다(규약이 앵커하던 토큰이 하나도 사라지지 않았다는 뜻).
- **리뷰가 새 가드의 공허 통과를 잡았다**: 인용 이름이 `-`로 시작하면 grep이 그것을 **옵션**으로 읽고, 패턴 인자가 없어지자 **stdin(= 검사 루프의 입력)을 삼켜** 나머지 인용이 통째로 미검사로 남으면서도 **FAIL이 아니라 통과**가 됐다. 하필 "공허 통과를 막는 가드"에서 난 공허 통과이고 양 셸 판정도 갈렸다 — `--`와 `</dev/null`로 닫았다. 함께 셸 간 판정이 갈리던 셋(빈 따옴표 구획·CRLF 앵커·dot 디렉터리 스캔 범위)과 축약이 흔든 규범 1건(fleet-cycle의 "제외"가 "점검"으로 약화, fleet-verify의 "의심 시"가 무조건으로 강화)을 고쳤다.
- **patch·불변**: **정리·견고화**다 — 커맨드 **12종의 호출명·역할·산출물·git 행위**가 불변이고, `.tide/phase`·**tide-guard(이번에도 무수정)**·프리플라이트 게이트 3 + advisory 단계·보고서 필수 섹션·판정 표기와 계측 줄 형식 전부 불변이다. 신설 규약은 **이 저장소 기여자 규율**이라 tide를 쓰는 프로젝트의 동작에 영향이 없다. 전 하니스를 **양 셸로** 재실행해 219 케이스 동일 판정(discover 68·multi-repo 30·site-includes 28·fleet 41·fleet-cycle 23·fleet-verify 29). **파일 분할은 범위 밖**이며, 이번에 세운 상호참조 가드가 그 분할의 안전망이다(분할 사이클은 Part G의 파일명 목록·앵커 집합을 같은 사이클에 확장해야 한다).

### [v2.10.1]
- **문서 자기서술 정합 규약 신설(문서 위생·기여자 규율)**: 문서가 **자기 자신에 대해 하는 서술**이 조용히 낡지 않게 하는 두 규약이 `docs/conventions.md`에 단일 원본으로 선다 — ① **카운트 단일 선언처**: 하니스 케이스 수(총계·파트별)는 **각 `tests/{name}/README.md`의 `cases: N` 선언 한 곳**에만 적고, 다른 문서(`docs/project-context.md`·규약·사이트·스킬)는 수 대신 선언처를 가리킨다. 러너가 그 선언을 **추출해 자기 실제 케이스 수와 대조**하며 어긋나면 **FAIL**이다(추출 실패도 FAIL — 조용한 skip은 가드를 공허하게 만든다). ② **역할 앵커 전파**: 커맨드 **역할**을 바꿀 때 캐노니컬 카탈로그(`docs/commands.md`)의 역할 셀에 **ASCII 병기어(역할 앵커)** 를 남기고 그 커맨드를 소개하는 소비자 문서(`README.md`·`site/docs/getting-started.md`)에 전파한다. 앵커 맵은 캐노니컬 안 주석 한 줄(`<!-- role-anchors: name=token -->`)에 두고 가드가 **추출**해 검사한다. **앵커를 발명하지 않는다** — 앵커 없는 행은 대상이 아니고 역할이 바뀔 때 자연히 늘어나는 **얇게 시작해 두꺼워지는 가드**다. 역사 기록(`docs/milestones/*`·`docs/reports/*`)과 살아 있는 문서 안의 **이력 항목**(이월 처분 원장 행·회고 섹션)은 그 시점 값을 적어도 되며 소급 갱신하지 않는다 — 판단 기준은 문서의 종류가 아니라 **그 문장이 현재를 말하는가**다.
- **출처 — 회고가 꼽은 최대 미반영 군집**: 커맨드 **수**(B1)와 **이름**(B3)에는 가드가 있었으나 **수치와 역할 문구**는 무방비였다. 계획 문서가 하니스 기준선을 틀린 수로 인용했고(14/14 인용, 실측 19/19), 한 사이클 안에서 PASS 수를 세 번 고쳤으며(29→41→45→48), 캐노니컬만 바뀌고 README·사이트의 역할 서술이 갈렸다. 실제로 이 사이클을 시작할 때도 `tests/discover/README.md`는 **29**(Part A 7 + B 12 + C 10)로 적혀 있었다 — 실측은 **61**이고 Part D·E는 문서에 존재조차 하지 않았다.
- **선언 정합 드리프트 가드(집행 배선)**: `tests/discover`에 **Part F** 신설 — **F2** 역할 앵커(맵 추출 → 캐노니컬 **표 행** 실재 + 소비자 문서 전파, 커맨드를 언급하지 않는 문서는 대상 아님), **F3** 통제(추출 0건이면 FAIL·맵의 이름이 실제 커맨드 스킬인지·가짜 앵커 부재), **F1** 케이스 수 자기 정합(자기 README 선언 vs 실제 케이스 수 — F1을 마지막에 두고 `누계 + 1`과 비교), **F1b** 파트별 내역 합 정합(총계만 고치고 내역을 두면 한 겹 아래에서 갈라진다 — 합산 범위는 **선언 줄 한 줄**). 앵커·골격이 전부 ASCII이거나 파일에서 추출한 값이라 ps1 사본의 **byte > 127 = 0** 원본 규율을 유지한다(양 셸 **62/62**, 기준선 48에서 +14).
- **역할 드리프트 1건 실제 회수**: 새 가드를 배선하며 조사한 결과 `/tide:release`의 **gh 게시 옵트인**(v2.4.0~v2.5.0에 이미 있던 능력)이 **캐노니컬에만 있고 `README.md`·사이트 시작하기 어느 쪽에도 없었다** — 세 문서 모두 `/tide:release`를 서술하면서 3~4사이클째 갈려 있었다. 인자 표기(`vX.Y.Z [pr/release]`)와 함께 전파했고 이제 F2가 회귀로 고정한다. 함께 `docs/project-context.md`의 스테일 2건(이미 M28에서 닫힌 항목을 미반영으로 적던 서술 · 이월 처분 원장의 빠진 행)과 하니스 README의 낡은 케이스 수·미서술 파트를 정정했다.
- **집행력 실증(변형 주입 → 원복)**: 새 단언이 공허하게 통과하지 않음을 양 셸에서 세 번 실측했다 — 선언을 1 틀리게 하면 **F1 FAIL**, 소비자 문서에서 앵커를 지우면 **F2 FAIL**, 총계는 그대로 두고 내역만 어긋내면 **F1b FAIL**. 리뷰가 자기 수정에서 곧바로 겪은 것도 남겼다: F1b를 파일 전체 합산으로 두자 README 산문에 인용된 같은 골격까지 더해져 FAIL했고(83 vs 62), 합산 범위를 선언 줄로 좁혔다.
- **patch·불변**: **정리·견고화**다 — 커맨드 **12종의 호출명·역할·산출물·git 행위**가 불변이고(바뀐 것은 이미 존재하던 능력의 *서술*), `.tide/phase`·**tide-guard(이번에도 무수정)**·프리플라이트 게이트 3 + advisory 단계·보고서 필수 섹션·판정 표기와 계측 줄 형식 전부 불변이다. 신설 규약은 **이 저장소 기여자 규율**이라 tide를 쓰는 프로젝트의 동작에 영향이 없다. 전 하니스를 **양 셸로** 재실행해 213 케이스 동일 판정(discover 62·multi-repo 30·site-includes 28·fleet 41·fleet-cycle 23·fleet-verify 29) — 전 하니스 양 셸 실행은 이번이 처음이다.

### [v2.10.0]
- **리뷰 검증 규율 신설(`/tide:review` 절차 가산)**: 판정을 다시 신호로 만드는 네 규약이 `docs/conventions.md`에 단일 원본으로 선다 — ① **반증 시도(refutation)**: 판정 **직전** 자기 판정 후보를 반박하는 독립 패스를 한 번 거친다(Agent 서브에이전트에 **판정 후보와 근거만** 넘기고 추론 과정은 넘기지 않으며, 서브에이전트가 마일스톤·impl 보고서·실제 변경 파일·검증 결과를 **직접 읽어** 답한다). **읽기 전용**이라 반증자는 아무것도 고치지 않고 지적만 목록으로 돌려준다 — 반증자가 스스로 고치면 미검증 변경이 트리에 들어가고 자기 발견을 자기가 닫아 판정자=실행자 구조가 재현되기 때문이다. Agent 불가 시 메인이 명시 절차로 수행하고 **폴백 사실을 보고서에 기록**한다(조용한 생략 금지). **규모가 작으면 깊이를 줄이되 생략하지 않는다.** ② **in-review 수정 후 재검증(re-verify)**: 리뷰가 1건이라도 고쳤으면 검증을 재실행한 결과를 적은 뒤에만 판정한다(불가하면 미검증 잔여 리스크로 명시). ③ **판정 계측**: `## 릴리즈 판정` 섹션에 `계측: in-review 수정 차단 n/권장 n/사소 n · 반증 시도 {수행|폴백} 성립 n건 · 재작업 라운드(rework) n` 한 줄이 필수로 남아 "가능"이 **처음부터 깨끗**이었는지 **고쳐서 깨끗**이었는지 구분된다. ④ **재작업 라운드(rework)와 수렴 조건**: 판정 `불가` 이후 재실행에서만 +1(그 외 재실행은 값 승계)하고, 라운드가 **2를 초과하면** 판정과 별개로 **마일스톤 재분해 권고**를 남긴다(advisory·비차단·자동 재분해 없음).
- **출처 — 루프 엔지니어링 관점 측정**: M1~M31 리뷰 판정이 **31/31 "가능"**이고 통산 차단 이슈는 1건뿐이었다. 리뷰어가 결함을 스스로 고치고 스스로 판정하는 구조에서는 `불가`가 구조적으로 나오지 않아 프리플라이트 게이트 ①(판정 "가능")이 **항상 참인 조건**이 된다. 회고가 M18·M19·M20·M28에서 반복 발견한 "적대 검증이 그린 밖 결함을 잡는다"는 패턴을 **재량에서 기본 경로로 승격**한 것이 이번 변경의 핵심이다.
- **선언 정합 드리프트 가드(집행 배선)**: `tests/discover`에 **Part E** 신설 — E1 `refutation`(규약↔리뷰 스킬)·E2 `in-review`(규약↔스킬↔템플릿)·E3 `rework`(규약↔리뷰 템플릿↔impl 템플릿)·**E4 계측 줄 골격 형식 정합**(`in-review`와 `(rework)`가 **같은 한 줄** — 토큰 존재만으로는 못 잡는 이형 드리프트를 잡는다)·**E5 재검증 선언**(전용 병기어 `re-verify`) + 교차·음성 통제. 토큰이 전부 ASCII 병기어라 ps1 사본이 코드포인트 조립 없이 앵커하며 ASCII-only 원본 규율을 유지한다. 커맨드 수 가드(B1)에 `docs/orchestration.md`를 더해 사이트 본문으로 나가던 카운트 드리프트도 닫았다(양 셸 **48/48**, 기준선 36에서 +12).
- **리뷰 자신이 첫 라이브 실증**: 이번 사이클 리뷰가 새 규약을 선적용해 반증 패스를 실제로 디스패치했고, **성립 8건**을 물어와 전부 반영했다 — 그중 하나는 **"Part E가 재검증 규약을 집행한다"는 자기 서술이 거짓**임을 실증했다(`in-review` 토큰이 계측 줄과 이중 용도라 재검증 절을 통째로 지워도 그린이었다). E5가 그 구멍을 닫았고, 반증자가 통과시킨 바로 그 변형이 이제 FAIL한다. 규약의 기록 규칙이 스킬과 갈린 것, 계측 줄 형식이 구현 중 두 이형으로 갈린 것도 같은 패스에서 잡혔다.
- **minor 가산·불변**: 리뷰 절차가 사용자 관찰 가능하게 한 단계 늘고 보고서 필수 내용이 더해져 patch보다 크나 호환 파괴는 아니다 — 보고서 **섹션 구조**(추가·삭제·개명 0)·판정 표기와 추천 버전의 **형식·위치**·`.tide/phase`·**tide-guard(이번에도 무수정)**·프리플라이트 3게이트·**12종 커맨드(2.0 stable 11종)** 전부 불변이고, 계측 줄이 없는 기존 보고서도 소비자가 그대로 읽는다(읽는 쪽 요구 무변경). 나머지 하니스 baseline 동일(multi-repo 30·fleet 41·fleet-cycle 23·fleet-verify 29·site-includes 28).

### [v2.9.0]
- **릴리즈 커버리지 체크 신설(`/tide:release` 프리플라이트 advisory)**: 마지막 릴리즈 태그 이후 변경된 실질 파일 중 **어떤 미릴리즈 보고서(impl·review·debug)도 설명하지 않는 것**을 릴리즈 시점에 드러낸다 — tide를 거치지 않은 커밋(비-tide 협업자의 변경 등)은 태그가 트리 전체를 담으므로 배포엔 실리나 판정 근거·CHANGELOG는 미릴리즈 보고서의 합집합만 봐 **미검토·미기록으로 편승**할 수 있는데, 그 구멍을 노출한다. `git tag`로 기준 태그를 잡아(없으면 전체 트리) `git diff --name-only`로 변경을 모으고 bookkeeping(`docs/reports/*`·`docs/milestones/*`·`CHANGELOG.md`·버전 파일·`.tide/*`)을 뺀 나머지가 보고서 본문에 경로로 언급되는지 본다. **비차단 advisory**(게이트가 아니라 노출)·**git 읽기만**(가드 무수정). 커버리지 근거 집합은 판정 근거 집합보다 넓다 — 변경을 설명하는 1차 문서인 **impl 보고서를 포함**해야 정상 사이클 impl 변경이 오경고되지 않는다.
- **마일스톤 번호 사전경고 신설(`/tide:milestone` advisory)**: 다음 번호 `M{N}`을 정한 직후·문서 생성 전에 `git log --all --diff-filter=A`로 그 번호가 **다른 ref에 이미 도입됐는지** 확인해 경고한다(병렬 브랜치 번호 충돌 예방). 번호가 파일명에 박혀 있어 경로 충돌 자체는 git이 머지에서 막지만, 이 경고는 **재할당 캐스케이드를 파일 생성 전에** 예방한다. **번호를 자동 변경하지 않고**(사용자 판단), 원격 브랜치는 최신 fetch 전제, 충돌 없으면 조용하다(소음 0).
- **선언 정합 드리프트 가드(집행 배선)**: `tests/discover`에 **Part D** 신설 — 커버리지 체크(`git diff --name-only`)가 conventions와 release SKILL 둘 다에, 번호 사전경고(`git log --all`)가 conventions와 milestone SKILL 둘 다에 선언되는지 결합 + 교차·음성 통제(양 셸 **36/36**, 기준선 29에서 +7). ASCII 메커니즘 토큰으로 집행해 ps1 ASCII-only 유지. 두 검사의 런타임 발화는 프롬프트 규율이라 하니스는 **선언 정합만** 집행하고 발화 자체는 라이브 도그푸딩 영역으로 남긴다(M29/M30 프롬프트 자산 한계와 동형).
- **출처 — 브랜치 간 협업 검토**: 둘 다 tide를 쓸 때의 M번호 충돌과 비-tide 협업자와의 협업(마일스톤 없는 변경)을 검토한 결과다. git이 경로 충돌은 loud하게 잡으나 (a) 비-tide 커밋의 릴리즈 편승[무결성]과 (b) 번호 재할당 캐스케이드가 남아 각각 advisory로 닫았다. 리포트 귀속(기준선 설계)·가드 마찰(phase 파일 무마찰)·"마일스톤 없음"(kickoff 정상 경로)은 이미 설계로 닫혀 손대지 않았다.
- **minor 가산·불변**: 두 advisory 검사가 사용자 관찰 가능하게 더해져 patch보다 크나 호환 파괴는 아니다 — 둘 다 **비차단·읽기 전용**이고 번호를 자동 변경하지 않으며, **12종 커맨드의 호출명·역할·산출물**·`.tide/phase`·**tide-guard(이번에도 무수정)**·프리플라이트 게이트 집합(판정·테스트·워킹트리·사이트 용어)·2.0 stable 11종 계약 전부 불변. 더해진 것은 advisory 단계 하나와 advisory 경고 하나뿐. 나머지 하니스 baseline 동일(multi-repo 30·fleet 41·fleet-cycle 23·fleet-verify 29·site-includes 28). 리뷰가 커버리지 근거 집합에서 impl 보고서 누락(오경고 폭풍)을 직접 수정했다.

### [v2.8.0]
- **`변경 파일 요약` 기준선 확정(규약 신설)**: 보고서·마일스톤의 추가/수정/삭제가 무엇 대비인지 이제 규약으로 정해진다 — **기준선 = 해당 단계·세션이 시작될 때의 워킹트리**이며 git `HEAD`나 마지막 릴리즈 태그 대비가 **아니다**(보고서는 *이 단계가 무엇을 했는가*의 기록이므로, 이미 트리에 있던 파일을 고쳤으면 git 이력과 무관하게 "수정"이다). 기준선 트리에 **미커밋 산출물이 섞여 있으면**(직전 단계 산출물이 아직 커밋 전이면) 표 아래 **한 줄로 고지**한다 — 부수효과 분리(커밋은 release에서만) 때문에 debug 세션은 거의 항상 미커밋 산출물 위에 얹히므로 **이것이 기본 경로**다. **재실행·이어하기**에서는 보고서가 갱신형(누적)이므로 기준선을 재실행 시점으로 다시 잡지 않고 **최초 착수 시점을 유지**한다(그러면 앞선 실행의 변경이 표에서 증발한다). `skills/{milestone,impl,debug}/template.md` 세 템플릿에 반영.
- **정오표(errata) 경로 신설**: 릴리즈된 보고서가 사실과 어긋난 채 남지 않게 하되 **증거를 지우지 않는다** — **본문은 소급 수정하지 않고**(그 판정으로 이미 태그가 나갔으므로 본문은 "그때 무엇을 근거로 릴리즈했는가"의 증거다) 말미에 `## 정오표`를 **append-only**로 단다(날짜·틀린 내용·실제 사실·근거). **미릴리즈 보고서는 본문을 직접 고친다.** 릴리즈 여부는 **기존 태그-트리 기준을 인용**할 뿐 새 기준을 만들지 않으며, 정오표는 그 보고서의 판정을 바꾸지 않고 이미 릴리즈된 보고서는 근거 집합에 다시 들어가지 않으므로 **프리플라이트에 아무 영향이 없다**. 적용 범위는 **사실 오류 정정**이라 릴리즈된 마일스톤의 `/tide:impl M{N}` 재실행(정정이 아닌 새 산출물)을 막지 않는다. impl·review·debug 보고서 모두에 적용.
- **debug 항목 상태값 `확인함` 가산(네 값)**: 상태가 `수정함`/`미해결`/`원인만 규명`/**`확인함`**이 된다 — `확인함`은 **결함이 아니었거나 실측만 한 항목**(검증 기록)으로 증상·재현·원인·수정을 "해당 없음"으로 두고 검증만 채운다. **용도 경계**: 발견-수정 세션 중 **그 작업과 같은 맥락에서** 잔여 리스크를 실측한 경우에만 쓰고 **검증만 하러 debug 세션을 열지는 않는다**(그것은 impl·review의 일 — 허용하면 debug가 아무거나 담는 자루가 된다).
- **선언 정합 드리프트 가드(집행 배선)**: `tests/discover`에 **Part C** 신설 — (C1) 네 상태값이 conventions·`skills/debug/SKILL.md`·`skills/debug/template.md` **세 파일 전부**에 등장 + 존재하지 않는 값 부재를 **파일별 음성 통제**로 확인, (C2) 기준선 선언이 **세 템플릿 전부**에 등장. 규약만 적고 집행을 다음 사이클로 미루지 않는다는 원칙대로 같은 사이클에 배선했다(양 셸 **29/29**, 기준선 19에서 +10). `run.ps1`은 한글 토큰을 코드포인트로 조립해 ASCII 소스·무BOM 유지.
- **출처 — 첫 라이브 도그푸딩**: v2.7.0이 신설한 `/tide:debug` 세션이 외부 사용자 프로젝트에서 실제로 열려 닫혔고, M29 리뷰가 "하니스로 집행할 수 없다"며 남긴 잔여 리스크 3건이 전부 닫혔다 — ① 항목 append가 **컨텍스트 요약을 넘어 생존**(세션 간 전달 과정이 내용을 흘리는 동안 파일은 온전해, "파일이 유일하게 믿을 수 있는 누적 저장소"라는 설계 전제가 실증됐다), ② `done`이 개요·판정 4개 섹션을 실제로 채우고 세션 파일 삭제·phase 복귀까지 규약대로, ③ release가 debug 판정을 근거로 통과 — 그것도 단일 보고서 폴백이 아니라 **"근거 집합 = 미릴리즈인 것 전부"** 경로가 걸려 리뷰보고서와 debug 보고서가 함께 한 릴리즈에 합류했다. 이번 릴리즈는 그 도그푸딩이 드러낸 규약 구멍 3건을 닫은 것이다.
- **minor 가산·불변**: 새 상태값·정오표 섹션·기준선 고지 줄이 사용자 관찰 가능하게 더해져 patch보다 크나 호환 파괴는 아니다 — **12종 커맨드의 호출명·역할·산출물 불변**, 보고서·마일스톤 형식의 **필수 섹션 집합**(마일스톤 7 / impl 5 / review 5)이 그대로라 기존 보고서가 전부 유효하고(더해진 것은 선택적 주석 줄과 조건부 섹션뿐), `.tide/phase`·**tide-guard(이번에도 무수정)**·release 프리플라이트 규칙·2.0 stable 11종 계약 전부 불변. 나머지 하니스 baseline 동일(multi-repo 30·fleet 41·fleet-cycle 23·fleet-verify 29·site-includes 28).

### [v2.7.0]
- **`/tide:debug` 세션 신설(12번째 커맨드·가산)**: 빌드 후 사용자 테스트에서 **발견한 에러**를 마일스톤 의례 없이 tide 안에서 고치는 **발견 우선** 진입점이다. 계획 우선 사이클(milestone→impl→review)은 태스크·`deps`·완료 기준을 미리 안다고 전제하지만 테스트 중 나오는 에러는 그 전제가 성립하지 않아 마일스톤이 의례가 된다. `/tide:debug [증상]`으로 세션을 열고 `/tide:debug done`으로 닫으면 `docs/reports/debug-{N}.md` 보고서 하나가 남는다 — 다른 단계가 문서→행동인 것과 달리 debug는 **행동→문서**(역방향 문서화)이고, 항목이 끝날 때마다 보고서에 append해 세션이 길어져도 발견이 유실되지 않는다. 사이클 밖 커맨드라 `/tide:cycle` 체이닝 대상이 아니며, 출구는 사이클과 같은 `/tide:release`다.
- **가드 무수정으로 부수효과 분리 보존(계약 불변)**: 새 phase 값 `debug`는 phase≠release이므로 **tide-guard를 한 줄도 고치지 않고** 기존 규칙이 그대로 적용된다 — git **쓰기**(commit·태그 생성/삭제·push)는 차단되고 **읽기**(v2.6.0의 커밋 읽기·태그 목록·`--grep`·이력 경로)는 통과해 진단에 필요한 `git log`·`show`·`diff`·`blame`을 자유롭게 쓴다. 급하게 움직이는 순간에 백스톱을 푸는 대신 배포는 종전대로 `/tide:release`로만 간다("규율이 1차, 가드는 백스톱"). `tests/multi-repo`에 `phase=debug` 회귀 6건 추가(양 셸 **30/30**) — 같은 phase에서 읽기 통과와 쓰기 차단을 함께 단언하는 음성 통제로 "가드가 살아 있으면서 읽기만 통과"를 고정했다.
- **릴리즈 경로 가산 확장(하위 호환)**: `/tide:release` 프리플라이트의 판정 근거가 **리뷰보고서 또는 debug 보고서**가 된다 — 두 보고서의 판정 형식(`## 릴리즈 판정`·`가능`/`불가`·추천 버전)을 통일해 **파서를 이중화하지 않는다**. 근거 집합은 **미릴리즈인 것 전부**이며 전부 "가능"이어야 진행하고(여러 세션을 한 번에 릴리즈해도 각 판정이 검사 없이 배포되지 않는다), 미릴리즈 판정은 **마지막 릴리즈 태그 트리 포함 여부**라는 결정적 신호로 한다(git 읽기 — v2.6.0이 읽기를 통과시켜 둔 덕에 성립). 열린 세션(`.tide/debug-session`)이 남아 있으면 release는 중단하고 `done`을 안내한다. **debug 보고서가 없고 열린 세션도 없으면 현행과 완전히 동일 동작**(기존 review 경로 불변). `/tide:status`도 열린 세션·잔재 phase를 드러내고 `done`을 권하되 **읽기 전용 계약은 불변**(세션 파일을 읽기만 하고 지우지 않는다).
- **minor 가산·불변**: 새 커맨드·새 산출물·새 phase 값이 사용자 관찰 가능하게 더해져 patch보다 크나 호환 파괴는 아니다 — 기존 **11종 커맨드의 호출명·역할·산출물 불변**, `.tide/phase`는 여전히 "단계명 한 줄"(세션 번호는 별도 파일 `.tide/debug-session`), **가드 차단 규칙·메시지·exit 2 불변**, 프리플라이트 확장은 순수 가산. 2.0 stable 집합은 **11종 그대로**이고 `/tide:debug`는 **v2.7.0 가산**이다(fleet 3종이 v1.x 가산 후 2.0에서 동결된 선례와 동형 — 다음 major에서 stable 승격 후보). 커맨드 카탈로그·README·사이트가 **12종**으로 정합하고 드리프트 가드가 이를 집행(discover 19), 나머지 하니스 baseline 동일(fleet 41·fleet-cycle 23·fleet-verify 29·site-includes 28). gitignore 범위는 `.tide/phase`·`.tide/debug-session` 둘로(deps·release-mode는 종전대로 커밋).

### [v2.6.0]
- **tide-guard 읽기/쓰기 구분(가산·판정 정련)**: 비-release phase에서 가드가 git **쓰기**(`commit`·태그 *생성/삭제*·`push`)만 차단하고 **읽기**(커밋 읽기·**태그 목록**·메시지 검색 `--grep`·이력 파일 읽기 `HEAD:path`)는 통과시킨다. verb를 git **서브커맨드 위치**에서만 보므로 옵션 값(`--grep=commit`)·경로(`HEAD:src/tag.rs`)·복합 서브커맨드 이름(`commit-graph`·`cat-file commit`)의 부분일치 오차단이 사라졌다. `git tag`는 읽기 옵션만/인자 없음이면 목록(읽기), 쓰기 옵션이나 목록 옵션 없는 위치 인자면 생성/삭제(쓰기)로 가른다 — 맥락 파악용 git 읽기가 release 밖에서도 막히지 않는다.
- **보호 불변·범위 명시**: 차단되던 *직접* git 쓰기 집합은 한 건도 줄지 않는다(보호 보존) — 전역 옵션 접두(`git -C dir commit`·`-c k=v push`)·환경변수 접두·`--force-with-lease` 등 모든 직접 변형이 그대로 차단된다. 가드는 **직접 git 호출을 보는 백스톱**이며 `sh -c '…'`·`eval` 같은 감싼/우회 실행·plumbing(`commit-tree` 등)은 비대상임을 conventions에 단일 원본으로 명시했다(샌드박스 아님 — "impl·review는 git 금지"의 실제 보장은 단계 규율). 양 셸 사본(`.sh`·`.ps1`) 동일 판정.
- **minor 가산·불변**: 가드의 *통과 판정*을 사용자 관찰 가능하게 완화(읽기 통과)하나 호환 파괴 아님 — 통과하던 것이 새로 막히지 않고 직접 쓰기 차단은 보존이라 minor. `tests/multi-repo`에 읽기/쓰기 구분 회귀 12건 추가(양 셸 24/24), 나머지 하니스 baseline 동일(discover 19·fleet 41·fleet-cycle 23·fleet-verify 29·site-includes 28). 게이팅(BOM·cwd·격리·폴백)·인코딩 규율·2.0 stable 계약(11종 커맨드·오케스트레이션 규약·보고서/마일스톤 형식) 불변, 새 커맨드·토큰 0.

### [v2.5.0]
- **`pr` 모드 finalize 릴리즈 브랜치 정리(가산)**: `pr` 게시 모드의 머지 후 마무리(finalize)가 태그·`gh release create`에 더해 **다 쓴 릴리즈 브랜치(`release/vX.Y.Z` 로컬·원격)까지 스스로 거둔다**. 정리는 ②~④(최신화·버전 sanity·태그/릴리즈) 성공 후 태그/릴리즈 생성과 **독립 멱등**으로 수행되고, **현재 브랜치 안전**(기본 브랜치 체크아웃 선행)·로컬 `git branch -d` 우선(squash/rebase 머지로 거부되면 **gh 머지 확인 전제 하에서만** `-D`, 미머지 강제삭제 금지)·원격 `--delete`(이미 없으면 무오류)·**실패 비차단**(정리 실패가 릴리즈를 무르지 않음·경고 보고)이다. 규약 단일 원본은 conventions "`pr` 모드" 절, 스킬은 포인터로 따른다 — 사이클마다 죽은 릴리즈 브랜치가 쌓이지 않는다.
- **tide-guard 입력 견고화(견고화·판정 불변)**: 가드가 hook 입력(stdin JSON)을 파싱하기 전에 **선두 UTF-8 BOM을 제거**한다 — 일부 환경(PS 5.1 `Set-Content -Encoding utf8`·일부 편집기)이 stdin 선두에 BOM을 붙이면 `jq`/`ConvertFrom-Json`이 throw해 `cwd` 추출이 어긋날 수 있던 여지를 닫는다(양 사본 `.sh`·`.ps1`). jq 부재 시 raw 입력 `cwd` 추출 sed 폴백은 키를 JSON 구조 경계로 앵커링해 부분일치 오인을 줄였다. **차단/통과 판정·메시지·exit 2 계약은 불변**(동일 입력 동일 판정) — 실 hook 입력은 BOM 없는 UTF-8이라 production 동작은 애초 불변, 이 strip은 방어적 견고화다.
- **M26 비차단 후속 종결(정리)**: sn1 — `tests/site-includes`에 추출 제외 용어가 마스트헤드 추출 영역에 literal로 실재하는지 positive-control 단언 추가("틀린 이유의 green" 차단, 양 셸 +1). sn2 — `tests/multi-repo` fixture를 no-BOM UTF-8로 쓰고 선두 BOM 입력 회귀 단언 추가(양 셸 +2씩 12/12). 오래 수용돼 온 tide-guard raw-`$input` grep 거칠음(M13 사소3→M18 사소6)도 함께 fix. 이로써 **코드로 손볼 잔여 후속이 0**으로 닫혔다(남는 미반영은 라이브 도그푸딩뿐).
- **minor 가산·불변**: 주 변경은 `pr` finalize의 사용자 대면 가산 동작(브랜치 정리)이라 minor(M24 sn2 finalize 자동화=v2.4.0 minor 선례와 동형). 새 커맨드·토큰 0, `release` 모드·push-only·`gh` 부재 경로 불변. 2.0 stable 계약(11종 커맨드·오케스트레이션 규약·`.tide/phase`/tide-guard 차단·통과 판정·보고서/마일스톤 형식)·드리프트 가드(B1/B2/B3, fleet 계열 19·41·23·29 동일 + multi-repo 12·site-includes 28)·인코딩/줄바꿈 규율 전부 불변. M26 sn1·sn2 + raw-`$input` 거칠음을 종결했다.

### [v2.4.1]
- **참조 구현 BOM 헬퍼 단일 원본화(정리)**: `tests/fleet-verify` 하니스에 로컬 복제로 남아 있던 BOM 제거 헬퍼(`strip_bom`/`StripBom`)를 단일 책임 `tests/lib/encoding.{sh,ps1}`로 추출해 정의를 셸당 1개로 수렴시켰다(source 순서 `encoding→discover→deps→toposort`). v2.2.1~v2.3.0이 추출한 `discover`/`toposort`/`read_deps`에 이어 참조 구현 이중성 군집의 **마지막 코드성 복제면**을 닫았다. 헬퍼 본문 바이트 동일·동작 보존.
- **사이트 스니펫 인클루드 자기완결 검증 하니스 신설**: `tests/site-includes/run.{sh,ps1}`가 **mkdocs 없이** 사이트 스니펫 셸(`conventions`·`orchestration`·`changelog`·`commands`)의 인클루드 타깃 존재·섹션 마커 균형(`[start]`/`[end]` 각 1쌍)·제외 용어 0건(마커 구획 본문)을 정적 검증한다 — release 프리플라이트 수기 스캔의 결정적 로컬 프록시로, 그동안 연속 환경-이월돼 온 빌드 출력 검증의 **로컬 사각지대**를 닫았다. 실제 `--strict` 렌더·nav/link는 CI(`deploy-pages.yml`)의 몫으로 유지(자기완결 가드 ≠ 빌드 대체).
- **순수 내부 정리(가산·patch)**: 새 커맨드·토큰 0, 사용자 대면 능력·계약 변화 없음 — `gh`/`release`/push-only/tide-guard 경로 바이트 동일. 2.0 stable 계약(11종 커맨드·오케스트레이션 규약·`.tide/phase`·보고서·마일스톤 형식)·드리프트 가드(B1/B2/B3, 전 하니스 19·41·23·29·10 동일 + 신규 site-includes 27)·인코딩 규율 전부 불변. M24 sn3·M25 sn3 + 빌드 출력 검증의 로컬 사각지대(환경-이월)를 종결했다.

### [v2.4.0]
- **`pr` 게시 모드 머지 후 마무리 자동화(상태 인지)**: M23~M24가 연 `pr` 모드를 **재실행 가능한 상태 인지**로 완성했다. 같은 `/tide:release vX.Y.Z pr`를 PR 상태로 분기한다 — **없음**이면 릴리즈 브랜치+`gh pr create`로 PR을 열고(생성), **open**이면 부수효과 없이 "머지 후 재실행" 안내(대기), **closed(미머지)**면 중단, **merged**면 기본 브랜치를 최신화해 태그·push·`gh release create`로 **마무리(finalize)**한다. finalize는 버전 범프·CHANGELOG를 건너뛰고(머지로 이미 반영), 분기 결정을 버전 범프 전에 둬 머지된 파일을 재편집하지 않는다. 이로써 `pr` 릴리즈가 PR 생성→머지→마무리까지 한 모드로 닫힌다.
- **멱등·안전(외부 대면 마무리)**: 미머지 PR에는 태그를 달지 않고(merged 확인 후에만), 머지된 버전 파일이 요청 버전과 다르면 태그 없이 중단한다(엉뚱한 머지 방지). 태그와 릴리즈는 **각각 독립으로** 선확인해 이미 있는 것은 건너뛰고 없는 것만 생성한다 — 둘 다 있으면 보고만, 한쪽만 있으면 나머지를 마저 만든다(부분 실패 복구).
- **옵트인 가산·불변**: 새 커맨드·토큰 0(`pr` 모드 동작 확장). `release` 모드·push-only·`gh` 부재 경로는 바이트 동일하고, M24 검증 게이트·tide-guard 비확장(`gh` 비게이트)·"게시는 release에서만"·2.0 stable 계약(11종 커맨드·오케스트레이션 규약·`.tide/phase`·보고서·마일스톤 형식)·드리프트 가드(B1/B2/B3, 전 하니스 19·41·23·29·10 동일) 전부 불변. M24 sn2(`pr` 머지 후 핸드오프)를 종결했다.

### [v2.3.0]
- **릴리즈 게시 모드 — GitHub 릴리즈/PR 택1(`gh` 옵트인)**: `gh` CLI가 있는 레포에서 `/tide:release vX.Y.Z [pr|release]`로 게시 방식을 고를 수 있다. `release` 모드는 현행 commit→tag→push **뒤** `gh release create`로 릴리즈 객체를 추가 생성(가산)하고, `pr` 모드는 기본 브랜치 직접 push 대신 **릴리즈 브랜치 + `gh pr create`**로 PR을 열고 태그·릴리즈는 머지 후로 미룬다. 모드는 **명시 인자 > `.tide/release-mode` 저장 선호도 > (검증 통과 시) 대화형 질문** 우선순위로 정하고, 첫 대화형 선택은 이후 재사용 여부를 물어 `.tide/release-mode`(커밋되는 레포 규약)에 기억한다. `gh` 부재 환경의 동작은 현행(push-only)과 **바이트 동일**한 옵트인 가산이다.
- **게시 견고성 — 검증 게이트·원격 불가 처리**: 게시 전 `git`·`gh` 가용·서브커맨드(`gh release`/`gh pr`)·인증(`gh auth status`)·대상 원격 GitHub 등록(`gh repo view`)을 검증한다. 검증은 **버전 범프·CHANGELOG 편집 전**에 두어, 명시 모드가 실패(원격 미등록·비-GitHub·미인증)하면 작업 트리도 더럽히지 않고 중단·보고한다(조용한 강등 금지). **tide-guard는 확장하지 않는다** — 가드는 git 토큰만 차단하는 2.0 stable 계약이고 `gh`는 게이트하지 않아 tide 밖 정상 `gh` 사용을 보호하며, "게시는 release에서만"은 스킬 절차가 보존한다.
- **테스트 참조 구현 단일 원본화 마무리(sn2)**: M23이 남긴 `read_deps`/`ReadDeps`(+`strip_bom`/`dep_name`)를 `tests/lib/deps.{sh,ps1}` 공유 라이브러리로 추출하고 fleet·fleet-cycle 하니스를 source로 전환(순서 discover→deps→toposort)했다. fleet과 fleet-cycle로 갈렸던 이름 추출 정규식 드리프트를 정본으로 화해했고, fleet-cycle이 정본을 거쳐 통과해 로직 동등성을 실증했다. 전 하니스 양 셸 baseline 동일 통과(19·41·23·29·10, FAIL 0)·드리프트 가드(B1/B2/B3)·커맨드 11종 불변 — 새 능력은 옵트인 가산, 2.0 stable 계약 불변.

### [v2.2.1]
- **테스트 참조 구현 단일 원본화(정리·patch)**: `tests/{discover,fleet,fleet-cycle,fleet-verify}`에 4중(× 2셸)으로 복제돼 있던 발견·위상정렬 참조 구현(`is_tide_repo`·`discover`·`toposort`)을 `tests/lib/{discover,toposort}.{sh,ps1}` 공유 라이브러리 단일 원본으로 추출하고, 하니스가 이를 source하도록 전환했다. 규약 변경 시 한 곳만 고치면 되며, 동작은 전 하니스가 양 셸에서 전환 전과 동일한 PASS 수(19·41·23·29·10)로 통과해 보존됨을 확인했다(fleet-cycle이 정본 `toposort`로 통과해 두 구현의 로직 동등성도 실증). `read_deps`는 하니스 로컬 유지(함수는 호출 시점 해석).
- **회고 문서 아카이브 분리**: 1060행으로 비대해진 `docs/reports/retro.md`를 최근(v2.x)만 남기고 v1.4.0 이하를 `docs/reports/retro-archive.md`로 분리했다(과거 보존·바이트 동일). `/tide:retro`는 종전대로 `retro.md`를 입력에서 제외하고 새 섹션을 최상단에 누적하므로 집계 동작이 불변이다(behavior-neutral).
- **순수 내부 정리(가산)**: 새 커맨드·능력·계약 변화 0 — 2.0 stable 계약(11종 커맨드·오케스트레이션 규약·`.tide/phase`/tide-guard·보고서·마일스톤 형식)·M22 단일 원본 가드 전부 불변. M22 회고가 부각한 참조 구현 이중성 군집을 축소하고 최장수 이월 후속(회고 비대화)을 종결했다.

### [v2.2.0]
- **사이트 단일 원본화 — 커맨드 카탈로그를 캐노니컬 `docs/commands.md`로 동결(정리)**: 그동안 `site/docs/commands.md`에 수기로 복제돼 있던 커맨드 카탈로그(한눈에 보기 표 + 11종 절)를 새 캐노니컬 문서 `docs/commands.md` 단일 원본으로 끌어오고, 사이트 페이지는 `pymdownx.snippets`로 본문을 인클루드하는 **스니펫 셸**로 전환했다(기존 `conventions`·`orchestration`·`changelog`와 동형). 카탈로그 본문은 **바이트 보존 이동**이라 렌더 출력 불변. 회고가 M11~M20 내내 지목한 "수기 사이트 페이지 표류" 군집의 잔존 뿌리를 단일 원본화로 닫았다.
- **드리프트 가드 확장 — 개수→이름 완전성 + 셸 검증**: `tests/discover` 가드를 (B1) `N종` 카운트 선언 정합(캐노니컬 = `docs/commands.md`), (B2) 사이트 카탈로그 페이지가 스니펫 셸인지(인클루드 보유·카운트/표 미재선언, 재수기화 시 FAIL), (B3) 카탈로그 완전성(각 커맨드 이름이 `/tide:<name>` **경계 일치**로 등장 — `fleet`이 `fleet-cycle`에 substring으로 걸려 거짓 통과하지 않도록)까지 확장했다. 새 단일 원본을 더하면 강제 수단도 같은 사이클에(2.0 메타 규칙).
- **라이브 실증**: `tests/discover` 하니스가 감지 임계값 + 단일 원본 동결(B1/B2/B3)을 sh·ps1 양쪽 각 19/19 통과. `tests/fleet`·`fleet-cycle`·`fleet-verify`·`multi-repo` 회귀 불변. 전부 하위 호환 정리 가산 — 커맨드 11종 이름·역할(2.0 stable 계약)·`.tide/phase`/tide-guard·보고서·마일스톤 형식 불변, 사이트 렌더 출력 불변.

### [v2.1.0]
- **오케스트레이션 발견성 — 멀티 레포 맥락 감지 힌트(가산)**: `/tide:status`·`/tide:kickoff`가 본래 작업을 마친 뒤, 현재 세션 위치의 직속 자식 tide 레포(발견 규약 재사용 — git 레포 AND tide 산출물, 숨김 디렉터리 제외)가 **2개 이상**이면 출력 맨 끝에 `여러 자식 tide 레포 N개 감지 — 교차 개요는 /tide:fleet` 한 줄을 advisory로 덧붙인다. **2개 미만(단일 레포 세션 포함)이면 아무 것도 덧붙이지 않는다**(소음 0). 읽기 전용·advisory — status는 종전대로 완전 읽기 전용, kickoff는 골격 생성 외 동작 불변이고 힌트는 fleet을 자동 실행하지 않는다. 발견성 규약은 `docs/conventions.md` "멀티 레포 오케스트레이션"이 단일 원본.
- **커맨드 수 드리프트 가드**: `skills/*/SKILL.md`로 존재하는 실제 커맨드 스킬 개수와 캐노니컬 문서·사이트(`README`·`conventions`·`site/docs/commands`·`site/docs/getting-started`)가 선언하는 "N종" 수가 어긋나면 FAIL하는 가드를 `tests/discover`에 추가했다 — 커맨드 증감 시 선언 갱신을 강제한다(수기 사이트 페이지의 커맨드 수 표류 회귀 고정).
- **라이브 실증**: `tests/discover` 하니스가 감지 임계값(≥2→hint·<2→none·단일 레포→none·숨김 미카운트)과 커맨드 수 가드를 sh·ps1 양쪽 각 16/16 통과. 전부 하위 호환 가산 — 자식 tide 레포 2개 미만 세션의 status·kickoff 출력 불변, 단일 레포 동작·2.0 안정 계약(11종 커맨드·오케스트레이션 규약) 불변.

### [v2.0.0]
- **2.0 안정 계약 재기준(동작 무파괴)**: 오케스트레이션 에픽(로드맵 1~4층) 완성 surface를 **stable로 동결 선언**한다 — v1.0.0이 "안정 선언" major였던 것과 동형의 **계약 재기준**이며 호환을 깨지 않는다. **커맨드 11종**(기존 8종 + `/tide:fleet`·`/tide:fleet-cycle`·`/tide:fleet-verify`)의 호출명·역할과 **오케스트레이션 규약**(부수효과 분리 불변·`.tide/deps` 의존/계약 비교·`.tide-fleet/integration` 통합 훅·tide-guard 백스톱)을 안정 계약으로 동결한다. `.tide/phase`·tide-guard·보고서·마일스톤 형식은 1.0 계약 그대로 유지. 하위 호환을 깨는 변경은 다음 major(v3.0.0)에서만 한다.
- **계약 비교 연산자 확장(가산)**: `.tide/deps`의 버전 제약이 `>=`만이 아니라 **`>=`·`>`·`=`(또는 `==`)·`<=`·`<` 전체 연산자**를 지원한다 — 의존 대상의 현재 버전과 요구 버전을 `vX.Y.Z` major.minor.patch로 비교해 불만족이면 의존 레포 줄에 `⚠ contract` 경고(연산자·요구·현재 명시)를 advisory로 표기한다. 알 수 없는 연산자·비표준 버전은 무시·경고하되 위반으로 단정하지 않으며, 이름 의존은 보존돼 위상정렬 순서·그래프는 불변이다. 기존 `>=`·미선언 deps 동작 불변(순수 가산).
- **통합 훅 git-verb 가드라일(advisory) + 자산 정리·견고화**: `/tide:fleet-verify`가 통합 훅 실행 전 git commit/tag/push·release 토큰(정석 cross-repo 형태 `git -C <dir> …` 포함)을 점검해 경고한다(강제 차단 아님 — verification-only 불변 유지). 백로그 이월 항목을 각각 fix(다중 자리 마일스톤 M10+ 픽스처·gitignore 마이그레이션 노트·가드라일)·수용·환경-이월로 명시 종결. **오케스트레이션 사용 가이드**(`docs/orchestration.md`, 사이트 노출)를 신설해 발견→deps/계약 선언→fleet-cycle→fleet-verify→순서 release를 워크드 예제로 설명한다.
- **라이브 실증**: `tests/fleet`(전체 연산자 satisfied/violation·미지 연산자 이름 의존 보존·다중 자리 M10+·BOM)·`tests/fleet-verify`(git-verb 가드라일 cross-repo 포함·verification-only)를 sh·ps1 양쪽 각 41/41·29/29 통과. 적대적 검증으로 참조 구현↔계약·사이트↔stable 선언 정합을 확인.

### [v1.6.0]
- **오케스트레이션 4층 — 통합 검증(`/tide:fleet-verify`, 11번째 커맨드)**: 자식 tide 레포들이 각자 사이클을 통과한 뒤, 레포를 가로지르는 통합을 대상 부모의 통합 훅(`.tide-fleet/integration`, 옵트인·parent-level)으로 검증한다. fleet-verify가 자식 레포를 발견해 통합 대상으로 보고하고, 부모 cwd에서 훅을 실행해 **exit 0 = 통합 pass / 비0 = fail**(실패 요약)을 보고한 뒤 "통합 pass면 release 핸드오프 순서대로 수동 release"를 안내한다. 훅 미선언이면 통합 검증 생략(옵트인·graceful).
- **verification-only(불변)**: fleet-verify는 git commit/tag/push·release·cross-repo git을 하지 않고, 어떤 레포의 `.tide/phase`도 `release`로 쓰지 않는다. 통합 훅은 검증/테스트 명령이어야 한다 — tide-guard는 phase≠release인 레포의 git을 막는 백스톱(release 차단기가 아닌 phase 잠금)이며, 통합 훅에 cross-repo git을 두지 않도록 안내한다.
- **오케스트레이션 로드맵 1~4층 완성**: 가시성(fleet) → 의존성 선언·계약 비교(`.tide/deps`) → 교차 사이클 자동화(fleet-cycle) → 통합 검증(fleet-verify). 전 계층에서 부수효과 분리 불변(release·cross-repo git 비자동화) 보존. `tests/fleet-verify/` 하니스가 훅 발견/파싱·옵트인 생략·pass/fail·verification-only(스킬 산문 결합)·`.tide-fleet` 발견 무시를 sh·ps1 양쪽 각 18/18 통과. 옵트인 가산 — 단일 레포·훅 미선언 동작 불변, 커맨드 10종·1.0 계약 불변.

### [v1.5.0]
- **오케스트레이션 3층 — 교차 사이클 자동화(`/tide:fleet-cycle`, 10번째 커맨드)**: 상위 폴더 단일 세션에서 발견된 자식 tide 레포들의 `milestone → impl → review`를 `.tide/deps` 의존성 순서(위상정렬·피의존 먼저)로 교차 자동 실행하고, 의존성 순서 release 핸드오프를 제시한다. upstream-behind 계약(M17)이 있는 레포는 핸드오프에서 `contract-blocked`로, 사이클이 중단된 레포의 downstream은 `skip`으로 보류 표기된다. 발견 0이면 단일 레포로 graceful 강등.
- **release·cross-repo git 자동화 제외(불변)**: fleet-cycle은 milestone→review까지만 자동화한다 — 어떤 레포에서도 release를 실행하지 않고, 어떤 레포의 `.tide/phase`도 `release`로 쓰지 않으며, git commit/tag/push·cross-repo git을 자동 실행하지 않는다. release는 순서 있는 핸드오프로 사용자에게 넘긴다. tide-guard는 phase≠release인 레포의 git을 막는 백스톱이고, **사전 점검**이 처리 전 phase=release 잔재(이전 중단된 수동 release) 레포를 제외해 백스톱이 풀린 채 도는 경로를 막는다.
- **라이브 실증**: `tests/fleet-cycle/` 하니스가 처리 순서(위상정렬)·release 제외·contract-blocked·downstream-skip + 불변을 강제하는 스킬 산문(금지 목록·백스톱/사전점검) 결합 검증을 sh·ps1 양쪽 각 23/23 통과. 옵트인 가산 — 단일 레포·미선언 동작 불변, 커맨드 9종·읽기 전용 fleet·부수효과 분리·`.tide/phase` 계약 불변.

### [v1.4.0]
- **오케스트레이션 2층 sub-step — `.tide/deps` `>=` 계약 버전 비교(옵트인)**: 의존 줄에 최소 버전을 선언하면(`<형제 레포명> >= <버전>`, 예 `svc-auth >= v0.3.0`), `/tide:fleet`이 의존 대상 레포의 현재 버전과 semver(major.minor.patch) 비교해 미달이면 권장 순서/advisory에 **`⚠ upstream behind`** 경고(그 레포를 먼저 올려야 함)를 표기한다. `>=`만 지원하고 그 외 연산자·비표준 버전은 경고에 그쳐 위반으로 단정하지 않는다. 위상정렬 순서·그래프는 버전 제약과 무관하게 불변. 버전 제약 없는 줄은 현행 동작 그대로(하위 호환 옵트인).
- **`.tide/deps` BOM 내성**: 파서가 선두 UTF-8 BOM(`EF BB BF`)을 제거해, Windows 편집기·`Set-Content -Encoding utf8`로 만든 BOM 붙은 deps 파일의 첫 줄(주석·의존명)도 올바로 파싱한다.
- 모두 옵트인·advisory만 — fleet은 여전히 읽기 전용이며 경고를 이유로 차단·실행하지 않는다(부수효과 분리 불변). `tests/fleet/` 하니스에 계약 만족/위반/연산자 외 무시/버전 파싱 불가/BOM 시나리오를 더해 sh·ps1 양쪽 각 23/23 통과.

### [v1.3.0]
- **멀티 레포 오케스트레이션 2층 — `.tide/deps` 의존성 인식 권장 순서(옵트인)**: 각 자식 레포가 루트 `.tide/deps`에 의존하는 형제 레포를 선언하면(한 줄에 하나, `#` 주석·빈 줄 무시), `/tide:fleet`이 의존 그래프를 위상정렬해 **권장 처리 순서**(피의존 먼저 — 예: orders가 auth에 의존하면 auth 먼저)를 advisory로 제시한다. 순환 의존은 감지·보고 후 상태 기반 순서로 폴백하고, 미선언 레포는 현행 동작 그대로(하위 호환 옵트인). fleet은 여전히 **읽기 전용·advisory만** — 순서를 제안할 뿐 cross-repo git을 자동 실행하지 않는다(부수효과 분리 불변).
- **`.tide/deps`는 커밋·`.tide/phase`는 로컬**: gitignore 범위를 `.tide/`에서 `.tide/phase`로 좁혀 의존 선언(`.tide/deps`)은 커밋되고 단계 상태(`.tide/phase`)는 종전대로 로컬에 머문다. `.tide/phase`의 의미와 tide-guard 차단 계약은 불변. kickoff의 `.gitignore` 생성도 이에 맞춘다.
- **라이브 실증**: `tests/fleet/` 하니스에 `.tide/deps` 파싱·위상정렬·순환 감지 폴백 참조 구현과 시나리오(위상정렬 순서·순환 폴백·미선언 독립·미존재명 무시)를 추가해 sh·ps1 양쪽 각 15/15 통과. 발견·5분류·1:1 요약·숨김 무시·강등 기존 시나리오 유지.

### [v1.2.1]
- **`/tide:fleet` 출력 정합·결정성(patch)**: 교차 요약을 레포 사이클 위치와 1:1로 대응하는 정규 5버킷(`release 가능 / review 대기 / impl 진행 / milestone 필요 / 보완 필요`)으로 통일하고(임의 합산 제거), advisory 다음 커맨드를 인자 포함으로 고정했다(`/tide:impl M{N}` · `/tide:release v{추천}`). 발견 규약은 숨김(dot) 디렉터리(`.git`·`.claude` 등)를 무시함을 명문화. 분류 taxonomy의 단일 원본은 `docs/conventions.md` "멀티 레포 오케스트레이션" 절이며, fleet 스킬과 `tests/fleet` 하니스가 이를 인용한다(sh·ps1 각 9/9, 숨김·`보완 필요` 시나리오 포함). fleet 호출명·역할·읽기 전용·부수효과 분리는 불변.

### [v1.2.0]
- **멀티 레포 오케스트레이션 1층(읽기 전용 가시성)**: 새 커맨드 `/tide:fleet`이 상위 폴더 아래 자식 tide 레포들을 발견해 각자의 사이클 상태(마일스톤·보고서·판정·버전·phase)를 교차 표로 보고하고 레포별 다음 행동을 advisory로 제시한다. 읽기 전용 — 파일·`.tide/phase`·git을 변경하지 않으며, cross-repo git은 자동화하지 않는다(release는 레포별 수동).
- **오케스트레이션 규약·로드맵 단일 원본**: `docs/conventions.md`에 "멀티 레포 오케스트레이션" 절 신설 — 4계층 로드맵(1층 가시성=이번, 2층 의존성/계약 선언·3층 교차 사이클 자동화·4층 통합 검증은 후속 마일스톤), 부수효과 분리 불변, 자식 레포 발견 규약(직속 1단계), advisory 계획 규칙.
- **커맨드 9종 — `/tide:fleet` 가산**: 기존 커맨드 8종의 호출명·역할은 v1.0 안정 계약 그대로이며, 읽기 전용 멀티 레포 개요 커맨드 `/tide:fleet`이 v1.2.0부터 하위 호환 가산으로 더해진다. README·conventions·project-context 정합.
- **문서↔릴리즈 버전 드리프트 차단**: `docs/project-context.md`가 버전 숫자를 복제하지 않고 `.claude-plugin/plugin.json`을 단일 원본으로 가리킨다 — release가 버전 파일만 범프해도 문서가 stale이 되지 않는다(드리프트를 갱신 단계가 아니라 복제 제거로 구조적 해소).
- **라이브 실증**: `tests/fleet/` 하니스로 자식 레포 발견·사이클 위치 분류·graceful 강등을 sh·ps1 양쪽 각 5/5 통과 확인.

### [v1.1.0]
- **멀티 레포 토대(가산)**: 상위 폴더 한 곳에서 띄운 단일 세션이 그 아래 여러 자식 레포를 오가며 각 위치별로 tide 사이클을 격리해 돌릴 수 있다. 단일 레포 사용(레포 루트에서 세션 구동) 동작은 **불변** — 이 변경은 전부 하위 호환 가산이다.
- **tide-guard repo-root 인식**: 가드(`hooks/tide-guard.sh`·`tide-guard.ps1`)가 명령이 실제로 실행되는 레포 루트의 `.tide/phase`를 읽어 차단/통과를 판정한다(단일 레포에선 `CLAUDE_PROJECT_DIR`와 동일). 레포를 못 찾으면 `CLAUDE_PROJECT_DIR` 폴백, 그래도 없으면 무차단. 차단 규칙·안내 메시지·exit 2·인코딩 규약은 불변.
- **대상 레포 규약 단일 원본**: `docs/conventions.md`에 "멀티 레포 / 대상 레포" 절을 신설해 대상 레포 해석·산출물 앵커링·cwd 규율·레포별 격리를 단일 원본으로 정의. 스킬 8종이 이를 참조해 산출물·`.tide/phase`·git·테스트를 대상 레포 루트 기준/cwd로 수행하고, `release`는 push 대상을 레포 실제 remote·기본 브랜치에 맞춘다.
- **라이브 실증**: `tests/multi-repo/` 하니스로 repo-root 인식·레포별 격리·폴백을 sh·ps1 양 가드에서 각 10/10 통과 확인. 상위 폴더 단일 세션 + 자식 레포 대상 사이클(앵커링·격리)도 세션 레벨에서 실증.

### [v1.0.0]
- **안정(stable) 선언**: 커맨드 8종의 호출명·역할, 단계별 규약, `.tide/phase`·tide-guard 계약, 보고서·마일스톤 형식을 v1.0.0부터 안정 계약으로 선언 — 하위 호환을 깨는 변경(호출명 제거·역할/계약 의미 변경)은 다음 major에서만, minor·patch는 가산·정리·견고화만. 11사이클 연속 "가능"·전부 minor로 굳은 세트를 1.0으로 고정
- **"규약↔실행/인프라 동기화" 메타 규칙 신설**: 규약·단일 원본을 새로 더하면 그것을 강제·반영할 실행 수단(스킬 프리플라이트·hook·CI 트리거·빌드)도 같은 사이클에 함께 손본다 (`docs/conventions.md`)
- **README↔CHANGELOG 단일화**: `CHANGELOG.md`가 릴리즈 노트의 단일 원본이고 README는 포인터만 둔다 — release 절차에서 README CHANGELOG 갱신 단계 제거(이중 갱신·불일치 종결)
- **배포 트리거 견고화**: `deploy-pages.yml`이 `main` 푸시마다 빌드·배포(`paths` 필터 제거) — 사이트가 단일 원본 인클루드하는 루트 파일이 늘어도 누락으로 낡지 않음. `site_url` 소문자 정렬, 홈에 1.0 안정성 반영
- 매니페스트 구조 무수정·자동 발견. 이 라운드 변경은 전부 정리·가산(하위 호환 파괴 없음)

### [v0.12.0]
- **도그푸딩 라운드 — 누적 실증 부채 상환**: 회고가 추적해 온 배포·환경 의존 실증을 한 묶음으로 처리. GitHub Pages 게시 확인(라이브 HTTP 200·홈/규약 정상), `tide-guard.ps1` 한국어 차단 메시지를 PowerShell 5.1에서 런타임 실증(무깨짐·exit 2·BOM), 병렬 디스패치 폴백 경로(겹침→순차·부분 실패→재시도/중단) 워크스루 확인
- **배포 결함 수정**: `deploy-pages.yml` 트리거 `paths`가 `site/**`만 감시해, 사이트가 단일 원본 인클루드하는 루트 파일(`CHANGELOG.md`·`docs/conventions.md`)만 바뀐 릴리즈에서 사이트가 낡던 문제를 수정(두 파일을 paths에 추가) — 변경 이력 페이지 고착(2릴리즈) 적발·해소
- **release 프리플라이트에 빌드 출력 검증 배선**(M10 권장1): 사이트가 있는 프로젝트에 한해 빌드 산출물 기준 제외 용어 0건 확인 단계 추가 — 규약↔실행 일치
- 병렬 디스패치 폴백 규약 보강(`skills/impl/SKILL.md`): 겹침=정규화 경로 교집합·빈/조건부 집합은 잠재 겹침으로 순차, 실패 정의+재시도 1회. 매니페스트 무수정·자동 발견

### [v0.11.0]
- **안정화 라운드 + 병렬 디스패치 런타임 실증**: M9의 "독립 태스크 서브에이전트 동시 디스패치"를 **실제로 사용해 이 사이클을 구현** — 비중첩 태스크 3개를 한 메시지에서 병렬 디스패치(폴백 없음, 벽시계 = 합산 아닌 최댓값)해 M9 병렬 기능의 **첫 런타임 실증**
- `skills/release/SKILL.md`에 "운영 주의" 절: phase↔git 분리(가드가 명령 전 phase를 읽음)·멀티라인 커밋 메시지는 환경 문법에 맞춤·릴리즈 노트의 제외 용어 literal 회피
- `skills/milestone/template.md`에 태스크별 "변경 파일" 권장 필드 — 병렬 겹침 감지를 휴리스틱에서 결정적으로(하위 호환, 필수 아님)
- `docs/conventions.md`에 메타 용어 누수 방지 + 릴리즈 빌드 출력 검증 규약 추가(snippets로 사이트 반영), `docs/project-context.md` 반영. 매니페스트 무수정·자동 발견

### [v0.10.0]
- **진짜 병렬 실행 신설**: `/tide:impl`·`/tide:cycle`의 impl 단계가 deps 위상에서 독립(무의존) 태스크를 서브에이전트로 **동시 디스패치**(같은 메시지에 복수 Agent 호출 = 동시 실행, 레벨 간 배리어) — "병렬은 스케줄링 해석일 뿐"이던 것을 실제 동시 실행으로 전환. 메커니즘·전달 컨텍스트·반환 계약은 `skills/impl/SKILL.md`의 "병렬 디스패치" 절이 단일 원본
- 파일 충돌 안전장치(예상 변경 파일 겹침 → 겹치는 태스크만 순차 폴백, 워크트리 격리는 선택·기본 비활성), 결과 병합(단일 impl 보고서)·부분 실패 처리, tide-guard 정합(서브에이전트도 phase=impl이라 git 차단). Agent 부재·레벨 단일 태스크·deps 이상은 순차 폴백(하위 호환)
- `skills/cycle/SKILL.md`의 "실제 병렬 실행 수단은 구현 판단에 맡긴다" 문장을 impl 단일 원본 참조로 교체, `docs/conventions.md`·`README.md`·`docs/project-context.md` 반영(매니페스트 무수정·자동 발견)
- 회귀 수정: v0.9.0 changelog 노트의 검증 문장에 들어간 외부 저장소명 literal이 단일 원본화(snippets)로 사이트에 유입된 것을 리워딩으로 제거 — 사이트 외부 귀속 표기 0건 회복

### [v0.9.0]
- **tide-guard 차단 메시지 한국어화**: git commit/tag/push 차단 안내를 한국어로("'{phase}' 단계에서는 git commit/tag/push가 차단됩니다. git 작업은 /tide:release 단계에서만 허용됩니다") — `tide-guard.sh`·`.ps1` 동일 문구. **인코딩 규약 확정**: sh=BOM 없는 UTF-8(Git Bash shebang 보호), ps1=BOM 포함 UTF-8(Windows PowerShell 5.1 한글 보존)
- **사이트 문서 단일 원본화**: `pymdownx.snippets`로 사이트 `conventions`·`changelog`가 저장소 원본 `docs/conventions.md`·`CHANGELOG.md`를 인클루드 — 사본 이중 갱신 부채 제거. 원본 도입부의 외부 귀속은 섹션 마커(`[start:body]`/`[start:notes]`)로 사이트에서 제외(빌드 출력 0건 유지)
- **패키지 위생 재확정**: 설치가 추적 파일 트리를 미러링함을 캐시로 실측, 공식 제외 수단 부재로 수용 재확정(`docs/project-context.md` "배포 위생" 절). 런타임 무관 파일은 용량 외 비용 없음
- 검증: `mkdocs build --strict` 통과 + 인클루드 정상(마커 누수 없음) + 빌드 출력 외부 귀속 표기 0건 실증, 편집본 가드 sh 한국어 차단(exit 2) 실증

### [v0.8.0]
- **tide 소개 GitHub Pages 사이트 신설**: MkDocs Material 기반 한국어 문서 사이트(홈·시작하기·개념·커맨드 레퍼런스·규약·변경 이력 6페이지)를 `site/`에 구성하고, GitHub Actions(`.github/workflows/deploy-pages.yml`)로 `main` 푸시 시 자동 빌드·배포 — 사이트 소스를 tide 사이클 기록(`docs/`)과 격리(`site/`, `site_dir: _build` 명시)해 충돌 회피
- 콘텐츠는 `README.md`·`docs/conventions.md`·`CHANGELOG.md`·`skills/*/SKILL.md`에서 정제, tide를 독립 워크플로우로 서술(외부 저장소 귀속 표현 제외). 사이클 다이어그램은 Mermaid로 재구성. **원본 문서는 무수정**
- `mkdocs build --strict` 통과 검증(격리 venv), `site/` 전체 외부 귀속 0건, `.gitignore`에 빌드 출력 `site/_build/` 추가
- 배포에는 저장소 Settings → Pages → Source를 "GitHub Actions"로 1회 전환 필요(코드 외 수동 단계)

### [v0.7.0]
- **`/tide:retro` 회고 신설**: 누적된 마일스톤·보고서를 가로질러 반복 문제·이슈 군집, 수용된 트레이드오프, "후속"의 반영/미반영 추적, 릴리즈 판정·버전 추이를 집계하는 읽기 전용 회고 스킬 — 산출물은 갱신형 단일 문서 `docs/reports/retro.md`(회고 시점마다 최상단 누적), 형식은 `skills/retro/template.md` 동봉
- `/tide:status`와 동일한 읽기 전용 원칙 — 회고 문서 하나만 생성/갱신하고 `.tide/phase`·git에는 손대지 않음
- `docs/conventions.md`(보고서 절·금지 행위 표)·`README.md`(커맨드 표)·`docs/project-context.md`에 retro 반영, 스킬 8종으로 갱신
- 첫 회고 산출 — M1~M5 사이클을 집계한 `docs/reports/retro.md` 생성(반복 군집 4·수용 트레이드오프 6·후속 반영 7/미반영 3·버전 추이 v0.2.0~v0.6.0 전부 minor)

### [v0.6.0]
- **`/tide:cycle` 오케스트레이션 신설**: `milestone → impl → review`를 한 번의 호출로 자동 체이닝 — 각 단계 진입 시 `.tide/phase` 기록, 사이클 종료 시 `idle` 복원, 한 단계라도 전제조건 미충족·실패 시 사이클 전체를 중단하고 중단 지점·사유 보고. git 작업을 하는 `release`만은 체이닝에서 제외하고 review "가능" 판정 후 `/tide:release vX.Y.Z`를 안내 (부수효과 분리·tide-guard와 정합)
- impl 단계에서 마일스톤 태스크의 `(deps:)` 표기를 파싱해 독립=병렬·의존=순차로 스케줄링하는 규칙 명시 (순환 의존·미존재 의존 ID는 감지·보고 후 순차 폴백)
- 무인자 호출 시 최대 번호 마일스톤의 보고서 상태로 시작점 3분기 (impl 없음→impl / impl만 있음→review 이어하기 / 둘 다 완료→새 milestone)
- 신규 스킬이 매니페스트 수정 없이 `skills/*/SKILL.md` 자동 발견됨을 확인 — `plugin.json`·`marketplace.json` 무변경
- `docs/conventions.md`·`README.md`·`docs/project-context.md`에 cycle 반영 (사이클 다이어그램·금지 행위 표·커맨드 표·스킬 7종)

### [v0.5.0]
- **브라운필드 킥오프**: `/tide:kickoff`가 대상 저장소를 신규/진행-중으로 판별(커밋 이력·기존 산출물·소스 규모 기준)하고, 진행-중이면 코드베이스를 조사해 `docs/project-context.md`(스택·디렉터리 구조·진입점·도메인 개념)를 생성 — 이후 단계가 매번 재조사하지 않도록
- `/tide:milestone`·`/tide:impl`이 `docs/project-context.md`가 있으면 먼저 읽어 기존 구조를 파악(조건부 — 없으면 평소대로, 하위 호환)
- `docs/conventions.md`에 "프로젝트 컨텍스트" 절 신설, README 커맨드 표·저장소 구조·설치 절을 브라운필드 동작에 맞게 갱신
- 로드맵 마일스톤 추가 — M5(`/tide:cycle` 오케스트레이션)·M6(`/tide:retro` 회고) 문서를 독립 진행 가능하도록 작성

### [v0.4.0]
- **호출명 개편**: `/tide:tide-status` → `/tide:status` — 커맨드 6종을 공식 권장 `skills/{이름}/SKILL.md` 구조로 전환하며 `tide-` 접두사 중복 제거 (기능·frontmatter 동등, 이력 보존 이동)
- 템플릿을 각 스킬에 동봉 — `skills/{milestone,impl,review}/template.md`, 참조는 `${CLAUDE_SKILL_DIR}/template.md`로 전환 (중앙 `templates/` 폐지)
- 저장소 전체 호출 표기를 실동작과 일치 — 스킬 상호 안내, 가드 차단 메시지(sh·ps1), README·conventions
- 패키지 위생 조사 종결 — 플러그인 파일 제외 수단은 공식 미지원으로 확인, 수용 결정 (근거: M3 보고서)
- README 마이그레이션 노트에 v0.3.0 → v0.4.0 호출명 변경 안내 추가

### [v0.3.0]
- **Claude Code 플러그인으로 전환** — `/plugin marketplace add Jongh/tide` → `/plugin install tide@tide` 한 번으로 커맨드 6종 + tide-guard hook이 함께 활성화 (`.claude-plugin/plugin.json`·`marketplace.json` 신설, 커맨드를 `commands/`로 이동)
- tide-guard hook을 플러그인이 직접 제공 — `hooks/hooks.json`이 `${CLAUDE_PLUGIN_ROOT}` 경로로 등록, 프로젝트별 hook 설치 절차 폐지. kickoff 내장 스크립트 제거로 가드 원본이 `hooks/` 한 곳으로 단일화
- 템플릿 파일화 — `templates/`(마일스톤·완료보고서·리뷰보고서)가 형식의 단일 원본. milestone/impl/review 커맨드가 템플릿을 직접 읽어 생성 (부재 시 폴백)
- 버전 파일 목록에 `.claude-plugin/plugin.json` 추가 (release/status/kickoff)
- README에 구버전(≤v0.2.0) 마이그레이션 노트 추가 — 수동 복사 사본의 플러그인 커맨드 섀도잉, 구 hook 설치물 중복 실행 주의

### [v0.2.0]
- `/tide-status` 신설 — 사이클 현재 상태(마일스톤/보고서/판정/버전/phase)와 다음 권장 커맨드 제시 (읽기 전용)
- tide-guard hook 도입 — `.tide/phase`가 `release`가 아닌 동안 git commit/tag/push를 기계적으로 차단 (`hooks/tide-guard.sh`·`.ps1`, `/tide-kickoff`가 대상 프로젝트 `.claude/hooks/`에 설치)
- 상태 파일(`.tide/phase`) 규약 도입 — 각 커맨드가 시작/종료 시 단계 기록, `.gitignore` 처리
- 전제조건 검사 — impl(마일스톤 문서 존재)·review(완료보고서 존재) 미충족 시 안내 후 중단
- release 프리플라이트 — 리뷰 판정 "가능" + 테스트 통과 + 워킹트리 확인 후에만 git 작업 진행
- `/tide-impl M{N}` 번호 지정 인자 — 특정 마일스톤 재실행·이어하기 지원
- 규약 문서에 상태 파일·tide-guard·단계별 강제 수단 명시

### [v0.1.0]
- tide 워크플로우 슬래시 커맨드 5종 신설: `tide-kickoff`·`tide-milestone`·`tide-impl`·`tide-review`·`tide-release`
- impl/review 단계 작업보고서(`docs/reports/M{N}-impl.md`·`M{N}-review.md`) 규약 포함
- 부수효과 분리 원칙(impl/review는 git 금지, release만 git 조작) 명문화
- `docs/conventions.md` 규약 문서 추가
<!-- --8<-- [end:notes] -->
