# 다음 세션 인수인계 (세션 운영 메모 — tide 산출물 아님)

> **이 파일은 git 추적 대상이다** — 다음 릴리즈 커밋에 함께 실린다(커버리지 체크에서 `.tide/*`는
> bookkeeping으로 분류돼 편승 경고를 내지 않는다). 살아 있는 문서는 이 파일을 인용하지 않는다.

## 0. 현재 지점 (2026-09-22 · M70 리뷰 라운드 4 = 가능 — 다음은 릴리즈)

**다음 커맨드: `/tide:release v2.36.0`** — 3단계에서 `docs/milestones/M70.md`에 `- released: v2.36.0`을 사람이 넣는다(설치본
2.35.0 release 스킬은 표지 단계를 모른다). 넣은 뒤 `O56`·`O23` 초록 확인. 리뷰가 in-review로 첫 표지 조건(리뷰 보고서가 마지막 태그에
없을 때만) · 첫 표지 전 경계 · 폴백 예외 문장을 고쳤다(`docs/reports/M70-review.md`).

**(이력) 라운드 4 impl 시점의 메모**: 재작업 라운드 4(종료 조건 열림).
- 리뷰 라운드 3 차단: `retro-mark-since: M70`이 도달 규칙의 폴백으로 대상 레포에 새어 소급 표지. 선언을 이 저장소 전용으로
  두고(도달 규칙에 예외 항목) 대상 레포는 트리에서 파생(`mark-since-derived` — 선언 · 가장 작은 표지 · 첫 표지는 impl 있는 가장
  큰 번호 하나). 자기 적용 판정 `O56`(가장 작은 표지 == 선언) + `O57`~`O59`. 상태 목록 ⒅.
- 권장: 옛 표지 PR 분기 넷 · 두 픽스처의 도입 번호 경계. 사소: 경계 목록 · retro 절차 5 ⒄ · 보고서 수.
- 494 케이스. 되돌림 마흔여덟 행 · 상태 서른셋을 라운드 4 트리에서 재실측(두 사본). 값의 단일 출처는 `docs/reports/M70-impl.md`.
- v2.36.0 릴리즈 때 M70에 `- released: v2.36.0`을 사람이 넣는다(설치본 release 스킬이 표지 단계를 모른다) — 여전하다.

## 0-옛3. 지점 (2026-09-21 · M70 impl 라운드 3 완료 — 해소됨, 이력)

**다음 커맨드: `/tide:review`** — 재작업 라운드 3이라 종료 조건(2 초과)이 열린다(잔여 위험 수용 · 재분해 권고).
- 차단(서술)은 규약 경계를 「통째 누락은 다음 표지 릴리즈가 자기 버전으로 메운다 · 붉는 것은 부분 누락뿐」으로 좁혀 닫았다.
- ⒄ 상태 추가 · 픽스처 여섯 문서(가장 큰/작은 표지 가름) · 사소 6(머지됐는데 태그 없으면 멈춤) — release 스킬 문면이 바뀌었다.
- 상태 서른둘 · 되돌림 여덟 행을 라운드 3 코드로 재실측, 네 환경 490/0. 값의 단일 출처는 `docs/reports/M70-impl.md`.

## 0-옛2. 중단 지점 (2026-09-18 · M70 impl 라운드 2 진행 중 — 해소됨, 이력)

**재개 커맨드: `/tide:impl M70`** — 재작업 라운드는 이미 **2**로 적혀 있다(보고서 개요). 이어하기이므로 다시 올리지
않는다. `.tide/phase` = `idle`.

**이미 끝난 것(트리에 반영됨 — 문면만)**

- 표지 대상 = 「이번 태그에 실리는 마일스톤 전부」: 번호 ≥ `retro-mark-since:` · `M{N}-impl.md` 있음 · 표지 없음.
  초안(impl 없음)에는 강행이어도 안 씀 · debug만 근거면 안 씀 · 태그 없는 옛 표지는 이번 버전으로 바꿈.
  재서술처 셋 다 고침: `docs/conventions.md`(회고 후속 항목의 소비) · `skills/release/SKILL.md` 3단계 · `docs/commands.md`.
- 규약 상태 목록에 ⒂(⑹에서 직전만 강행 릴리즈) · ⒃(⑹에서 다음 사이클까지 마친 뒤 함께 릴리즈) 추가.
  생명주기 갈래 표에 세 행 추가. 경계 문단을 「가장 최근 릴리즈의 누락만 안 보인다」로 좁힘.
- 「기계가 무는 것」 목록에 **「표지가 있는 마일스톤 아래에 표지 누락이 없는가」를 이미 적었다 — 하니스는 아직 없다.**
  이것이 1순위 잔여다(규약이 집행을 사실보다 넓게 주장하는 상태).
- retro 스킬 절차 5 → 도입 번호 미만 기반 버전 규칙 + 규약 절 포인터(사소 4). impl 보고서 권장 2 문장 정정.
- 중간 측정: `pwsh -Part O` 52/0 · `-Part W` 100/0.

**남은 일(순서대로)**

1. **하니스 새 케이스(Part O, 두 사본 동형)** — 「표지 누락」: 번호 ≥ since · impl 보고서 있음 · 표지 없음 · 그보다
   번호가 큰 마일스톤에 표지가 있음 → 붉음. 실제 트리 본 검사 + 합성 픽스처 통제(누락 있음 → got 1 / 없음 → 0).
   기존 `doc_released`/`DocReleased` · `mst_num`/`MstNum` · `target_fixture`/`TargetFixture` 재사용.
   `.ps1`에 비-ASCII 금지. README `cases:` · Part O 내역 · 머리글 같은 편집.
   **표지 중복·누락 통제의 회귀 방향마다** 되돌림 행(추출 죽임 · 상수 · 전부 포함/제외).
2. **생명주기 상태표 재실측** — ⑴~⒃ 전부(⒂·⒃ 신규 + 후처리 전·후), 두 사본. 갈래 ⓐ 사본(M71 impl·review ·
   M71만 표지 v2.36.0 · 회고 v2.36.0)이 **새 케이스로 붉는지**, M70·M71 둘 다 표지면 초록인지.
   갈래 ⓑ(⒂: M70만 표지 · M71 초안)가 초록인지.
3. 되돌림 표에 새 행 · `reversal-table-main` 갱신.
4. 완료 기준 1~10 재대조(기준 3·4·7의 「상태 목록 전부」가 ⒂·⒃을 덮게).
5. 보고서 「라운드 2 진행 중(이월)」 머리 블록 제거 → 네 환경 전수 → `measure-after-artifacts` 순서로 검증 표 갱신.
6. 다음 리뷰가 라운드 2다 — 또 반환되면 종료 조건(2 초과). 설계가 다시 막히면 표지를 별도 마일스톤으로 재분해 검토.

## 0-옛. 중단 지점 (2026-09-08 · M62 impl 라운드 1 진행 중 중단 — 해소됨, 이력)

**사용자 요청으로 impl 보고서 갱신 **직전**에 멈췄다.** 트리는 정리돼 있다 — 되돌림 측정의 변형은
전부 복원했고(`docs/conventions.md` 백업 복원 확인), `pwsh` 축 **370/0**, `.tide/phase` = `idle`.

| 항목 | 상태 |
|---|---|
| M62 마일스톤 | 작성 완료(`epic: E3`, 완료 기준 11) — `E3` 마커에 양방향 등재됨 |
| M62 impl 라운드 0 | 완료 · 보고서 `docs/reports/M62-impl.md` 존재 |
| M62 리뷰 라운드 0 | **판정 `불가`** · `docs/reports/M62-review.md` (차단 3 · 권장 4 · 사소 2 · 반증 성립 8) |
| M62 impl 라운드 1 | **진행 중 — 코드·규약 수정은 끝났고 보고서 갱신이 남았다** |
| 케이스 수 | `tests/discover` **370**(Part W **24**) — README 선언과 실측 일치 |

### 라운드 1에서 이미 닫은 것 (트리에 반영됨)

- **차단 1(표지 사용의 창)** — 표지 사용 탐색을 **선언이 속한 절**로 좁혔다. 앞 판본은 파일 전역이라
  `scope-same-search`가 fleet-verify 절에서도 쓰여 **경계 문장 ⓑ를 지워도 초록**이었다. 이제 세 표지
  각각을 지우면 `W9`가 붉는 것을 **표지별로 실측**했다. 회귀를 무는 픽스처 통제 **`W22`** 를 세웠고
  창을 파일 전역으로 되돌리면 실제로 붉는다(실측).
- **차단 2(README 선언처 결손)** — Part W 절에 `W19`~`W22` 서술·집계를 넣고 합계를 **24**로 바로잡았다.
  원인은 **여러 줄 앵커 치환이 CRLF에서 조용히 부분 실패**한 것이다(아래 7번에 함정으로 등재).
- **권장 5** — 표지 판정 구현을 **사본당 하나**로 모았다(`marks_used_in` / `MarksUsedIn`).
- **사소 8·9** — 인용 방향(`위` → `아래`) 정정 · `fleet-verify` 스킬의 재서술 축소.

### 남은 것 — **전부 `docs/reports/M62-impl.md` 문면**이다

1. **개요의 재작업 라운드를 `1`로** 올린다(리뷰 판정이 `불가`였다).
2. **차단 3** — 「검증 범위 선언」 문단이 *"일곱 하니스의 `run.sh`·`run.ps1` 전부"* 라고 적은 것을
   **실제로 돈 것**으로 좁힌다. 여섯 하니스의 `run.sh`는 돌지 않았으므로 **사유와 함께 대상 밖**으로
   적거나 「일부를 보았다」로 되돌린다.
3. **권장 4** — T05 항목의 *"삭제 계열 전부"* 를 사실대로(되돌림 표의 `broken` 행은 여덟, sh 대조는 넷).
4. **권장 6** — 개요·T02·T04의 케이스 수 세 곳을 **선언처 포인터**로 바꾼다.
5. **권장 7** — 완료 기준 대조의 **근거 칸**에도 범위를 적는다(`scope-declared`의 자기 적용).
6. **되돌림 표를 아래 측정으로 교체**한다(케이스 수가 370이 되어 이전 값은 전부 무효다).
7. **최종 측정**을 다시 잡는다 — `measure-after-artifacts`.

### 라운드 1의 되돌림 실측 (ps1 축 · 기준선 **370/0** · 전부 복원 확인)

| 행 이름 | 깬 것 | 축 | 붉은 케이스 | 결과 |
|---|---|---|---|---|
| del-scope-decl | 규약의 `scope-decl:` 선언 줄 삭제 | broken | W1;W2;W3;W4;W5;W6 | 364/6 |
| dup-scope-decl | 같은 선언 줄을 하나 더 둠 | adversarial | W1 | 369/1 |
| strip-scope-defn-bold | 규약 정의 줄의 굵은 표기 제거 | adversarial | W4 | 369/1 |
| del-review-alias | review 스킬의 백틱 토큰 삭제 | broken | W5 | 369/1 |
| strip-impl-tpl-backtick | impl 템플릿의 백틱만 벗김 | adversarial | W6 | 369/1 |
| del-impl-tpl-alias | impl 템플릿의 백틱 토큰 삭제 | broken | W3;W6 | 368/2 |
| dup-scope-limits | `scope-limits:` 선언 줄을 하나 더 둠 | adversarial | W7 | 369/1 |
| del-scope-fact-use | 경계 문장에서 `scope-fact` 사용 제거 | broken | W9 | 369/1 |
| del-scope-same-search-use | 경계 문장에서 `scope-same-search` 사용 제거 | broken | W9 | 369/1 |
| del-scope-exhaustive-use | 경계 문장에서 `scope-exhaustive` 사용 제거 | broken | W9 | 369/1 |
| filewide-window | 표지 탐색 창을 파일 전역으로 되돌림 | adversarial | W22 | 369/1 |
| del-mc-decl | 규약의 `measure-cache:` 선언 줄 삭제 | broken | W13;W14;W15;W16;W17 | 365/5 |
| dup-mc-decl | 같은 선언 줄을 하나 더 둠 | adversarial | W13 | 369/1 |
| strip-mc-defn-bold | 캐시 병기어 정의 줄의 굵은 표기 제거 | adversarial | W16 | 369/1 |
| del-mc-skill-alias | impl 스킬의 백틱 토큰 삭제 | broken | W15;W17 | 368/2 |
| strip-mc-skill-backtick | impl 스킬의 백틱만 벗김 | adversarial | W17 | 369/1 |
| dup-verify-scope-decl | `verify-scope:` 선언 줄을 하나 더 둠 | adversarial | W19 | 369/1 |
| del-verify-scope-decl | 규약의 `verify-scope:` 선언 줄 삭제 | broken | W19;W20 | 368/2 |
| del-verify-ran-use | 정의 문장에서 `verify-ran` 사용 제거 | broken | W21 | 369/1 |
| del-verify-skipped-use | 정의 문장에서 `verify-skipped` 사용 제거 | broken | W21 | 369/1 |
| del-verify-uncovered-use | 정의 문장에서 `verify-uncovered` 사용 제거 | broken | W21 | 369/1 |

**`reversal-table-main` 후보**: `W1 W4 W5 W6 W7 W9 W13 W16 W17 W19 W21 W22` — 전부 단독 행이 있다.
`W3`·`W15`는 결합의 논리적 약화형이라 구조상 단독 행을 가질 수 없어 목록에서 뺀다(라운드 0과 같은 판단).

**sh 축은 재측정이 남았다** — 케이스 수가 370으로 바뀌어 라운드 0의 sh 값(369 기준)은 무효다.
완료 기준 10이 요구하는 **삭제 행 넷**(`del-scope-decl` · `del-mc-decl` · `del-review-alias` ·
`del-impl-tpl-alias`)을 sh 축에서 다시 재야 한다. **행당 10~13분**이다.

### 이 환경의 측정 비용 (실측)

| 축 | 한 번 | 비고 |
|---|---|---|
| `tests/discover` @ pwsh 7 · PS 5.1 | 각 ~50초 | 되돌림은 이 축으로 돈다 |
| `tests/discover` @ Git Bash `sh` · `dash` | **각 10~13분** | 되돌림을 이 축으로 돌면 비용이 폭발한다 |
| CI `posix (ubuntu-latest)` — 일곱 하니스 `run.sh` 전부 | **38초** | 같은 집합이 **21배** 빠르다(2026-09-07 두 런 실측) |
| CI 워크플로 전체 벽시계 | ~13분30초 | `posix (windows-latest)` 813초가 전부를 지배한다 |

**CI는 push로만 돈다** — 가드가 `release` 밖의 push를 막으므로 impl·review 단계에서 CI에 위임할 수
없다. 로컬에서 sh 축을 재느냐, 릴리즈 시점 CI에 맡기느냐의 선택이며 라운드 0에서 사용자가 **네 환경
로컬**을 골랐다.

## 1. 지금 어디까지 왔는가 (2026-09-07 · 릴리즈 완료)

| 항목 | 상태 |
|---|---|
| 최신 릴리즈 | **v2.27.0** (M61) — PR #36 머지 · 태그 `5d9c8ba` · GitHub 릴리즈 게시 · 브랜치 정리 완료 |
| HEAD | `5d9c8ba` (`main`) · 워킹트리 **클린** |
| `.tide/phase` | `idle` |
| 진행 중 사이클 | **없음 — M62 마일스톤이 다음이다** |
| 하니스 | 머지된 트리에서 일곱 하니스 pwsh 축 전부 FAIL 0 · CI 6잡 전부 pass |

**이번 세션이 한 것**: impl 라운드 4 → 리뷰 4(`불가`) → impl 5 → 리뷰 5(`가능`) → **릴리즈 v2.27.0**.
`Part V`의 **같은 한 줄**에서 다섯 라운드가 났고(블록 부재 · 두 침묵의 차집합 · 줄 복제 · 줄 접기 ·
닫는 태그 뒤), 라운드 5가 **금지 조건 누적 → 한 줄의 형태를 통째로 단언**으로 처방의 형태를 바꿔
그 계열을 접었다. 집행 313 → **346**(discover) · 45 → **51**(site-includes).

## 2. 다음 커맨드

```
/tide:milestone
```

**M62를 연다.** 재료는 아래 3번(회고 이월)과 9번(외부 fleet 보고서 두 회차)이다.

## 3. M62가 받을 것 (우선순위 순)

1. **외부 fleet 보고서 두 회차를 이월 원장에 등재한다** — `docs/project-context.md`에 **한 행**으로.
   근거와 에픽 갱신안은 아래 9번이 든다. **M62의 첫 할 일이다**(두 세션째 미등재).
2. **에픽 갱신을 M62 마일스톤과 같은 편집에서** 넣는다(Part M 양방향) — `E2` 축 ⑵의 하위 물음 ·
   `E3` 축 ⑶ 확장과 축 ⑷ 신설 후보 · `E1` 축 ⑵ 보강.
3. **M61이 남긴 부채 둘**: ⑴ 변이된 트리의 **두 사본 동형**이 sh 축에서 확인되지 않았다(되돌림이
   ps1 축 위주 — impl 후속 12) ⑵ `Part V`는 여전히 **「표가 참인가」·「목록이 빠짐없는가」**를 묻지
   못한다(규약의 「묻지 않는 것」 넷이 그 경계이고 리뷰 라운드 5의 실측이 그것을 다시 확인했다).

## 4. 리뷰가 남긴 판단 재료 (재분해 권고 — M61에서 발동, 해소됨)

재작업 라운드가 이제 **5**라 규약의 **마일스톤 재분해(`/tide:milestone`) 권고**가 계속 발동한다
(advisory). **이 사이클의 차단 다섯(라운드 0~4)이 전부 `Part V`의 같은 한 줄**이다 — 블록 부재 ·
두 침묵의 차집합 · 줄 복제 · 줄 접기 · 닫는 태그 뒤. 라운드 4까지는 **금지 조건을 하나씩 더하는**
처방이라 다음 변종이 남았고, **라운드 5가 형태 단언으로 바꿔 그 계열을 접었다.**
- **다음 리뷰가 또 반환을 내면 재분해를 먼저 판단한다** — 그때는 「처방의 형태가 틀렸다」가 아니라
  **「이 자리가 한 태스크가 아니다」**가 증거가 된다. `T03`을 **「선언 형식」**과 **「집행 범위」**로
  가르는 것이 그 갈래다.

**`잔여 위험 수용`(residual-risk-acceptance)은 이번에 쓰지 않았다.** 형식 요건(기여자 인프라 한정)은
충족했으나, 남은 차단이 **이 사이클이 세우려던 보증 자신(`V8`)에서 거짓 초록을 내는 자리**라 수용하면
조항이 의례가 된다는 것이 근거다(리뷰 보고서 판정 근거에 기록). **성격이 다른 차단이면 판단은 달라질
수 있다.**

## 5. 이 사이클이 세운 것 (누적)

- **`tests/discover`**: 313 → **346** (`Part V` **33 케이스**)
- **`tests/site-includes`**: 45 → **51** (`ci-sweep1`~`ci-sweep6`)
- **되돌림 표 53행**, 본 검사 **열넷**이 전부 단독 행을 갖는다
- 리뷰 라운드 2가 **in-review로 고친 것 1건** — impl 템플릿이 다음 사이클을 12건 붉히던 것을 1건으로

## 6. 측정 도구 조리법 (이번 세션이 확보한 자산 — 그대로 재사용하라)

스크래치패드에 `rev5.py`(되돌림 53행 · `--dry`로 앵커만 검증) · `apply5.py`(표 채우기 + 붉은 케이스
칸을 실측으로 덮어쓰기) · `sweep5.py`(4환경 전수, 칸마다 원장 기록) · `probe_fold.py`·`probe_after.py`
(실물 트리에 변이를 걸고 바이트 동일 복원 — 리뷰가 차단을 재현한 도구)가 있다.
**세션이 바뀌면 사라진다.**

- **메모리가 모자라면 스윕 드라이버가 죽는다(2026-09-07 실측 — 두 번).** 자유 메모리 1.3GB/16GB에서
  한 번은 `mutation` 칸에서, 한 번은 첫 칸에 닿기도 전에 죽었다. **처방은 칸을 쪼개는 것**이다 —
  값싼 스물넷은 포그라운드 세 묶음(각 10분 상한 안), 비싼 넷(`discover` sh 두 축 · `mutation` sh
  두 축)은 **한 번에 하나씩** 백그라운드로.

- **분리 실행은 파이썬 드라이버로만 띄운다.** `Start-Process`로 MSYS `bash.exe`를 숨김 콘솔 +
  리다이렉트로 띄우면 **한 번도 실행되지 않은 채 살아 있다**(88분에 CPU 0.06초 — 실측).
- **셸과 pwsh를 절대 경로로 못박고, PATH에 Git의 `usr/bin`을 선주입한다.** 안 하면 `sh`를 못 찾거나,
  찾아도 **스크립트 안의 `grep`·`awk`가 없어 `NO-RESULT`가 조용히 나온다**(8행이 그렇게 버려졌다).
- **`bash`의 파일 리다이렉트는 블록 버퍼링이라 로그가 빈 채다.** 진행은 **산출 파일과 프로세스
  생존**으로 본다(파이썬은 `-u`로 띄운다).

- **소요 실측(2026-09-07 · 346 케이스)**: `discover` sh 축 1회 **dash·bash 각 20분대**(ps1은
  20~38초) · 되돌림 53행(ps1 위주) **약 17분** · `mutation`의 sh 축 1회 **약 40분**(대상 하니스를
  사본 트리에서 두 번 돌린다). **4환경 전수 28칸은 칸을 쪼개 도느라 약 2시간**이 들었다.

**드라이버가 남아 있는 상태(그대로 쓰면 안 되는 자리 둘)**

- `rev5-results.json`에 **53행이 기준선 346으로 채워져 있다.** 다음 라운드가 러너를 고치면 그 값은
  **전부 무효**다 — 원장을 비우고 다시 재야 한다(값을 옮겨 적지 않는다. 라운드 3이 그 사각에서
  **공허해진 행 하나와 죽은 앵커 하나**를 실제로 만들었다).
- `rev5.py`의 `discover` 행은 **ps1 축만** 잰다(sh 축은 행당 20분 이상). 삭제 행 넷의 양 사본 대조가
  필요하면 그 행만 sh로 따로 돌려야 한다 — `measure()`가 그 갈림의 유일한 자리다.

## 7. 이 저장소에서 데는 자리 (누적 — 반복 금지)

1. **드라이버를 중간에 죽이면 트리가 변이된 채 남고, 다음 드라이버가 그것을 「백업」으로 승격한다
   (신규 — 이번 세션 실측).** 그러면 매 행의 「복원」이 오염을 되돌려 놓는다. 규약의 선언 줄 하나가
   지워진 채 백업됐고 **`v4-dupdecl`이 334/8**(선언 줄 하나 복제가 여덟을 붉힐 수 없다)이라는 **값의
   크기**로 발견됐다. **죽인 뒤에는 반드시 트리를 복원하고 기준선을 재확인한 다음** 다음 드라이버를
   띄운다.
2. **셸 heredoc에 파이썬을 넣으면 역슬래시 이스케이프가 접힌다.** 이번 세션에 **네 번** 물렸다 —
   한 번은 `run.sh`에 **단독 CR 바이트**를 심어 `T4`가 붉었다(하니스가 즉시 잡았다). 편집 도구를
   쓰거나 `chr(10)`으로 우회한다.
3. **프로세스 정리를 이름으로 하지 않는다** — `bash` 전부를 죽였다가 **모니터와 자기 자신까지** 껐다.
4. Python으로 파일을 고칠 때 **개행을 대상 파일에 맞춘다**(`docs/*.md`·`*.ps1` CRLF · `tests/**/*.sh` LF).
5. **마커 리터럴을 산문·표 칸에 적지 않는다**(편집 스크립트가 첫 매치를 잡는다).
6. **규약 문서에서 대시로 시작해 백틱 키를 적는 줄은 그 자체가 선언 줄이 된다.**
7. **긴 스윕은 시스템 메모리 부족으로 죽을 수 있고, 죽으면 `/tmp` 사본에 하니스가 고아로 남는다
   (신규 — 2026-09-07 실측).** `tests/mutation` 칸에서 드라이버가 종료됐는데 `sh /tmp/tide-mutation.*`
   여섯이 계속 돌고 있었다. **칸마다 원장을 파일에 쓰면 잃는 것은 한 칸**이고, 재개 전에 `ps -ef`로
   고아를 확인해 **PID로** 죽인 뒤 `/tmp/tide-mutation.*`를 지운다(이름으로 일괄 kill 금지 — 위 3번).
8. **되돌림용 적대 픽스처는 「자기가 무는 축 밖에서 깨끗해야」 한다(신규 — 같은 세션 실측).**
   M62 사본을 최소 골격으로 만들었더니 **에픽 등재 누락(`M5`)과 완료 기준 번호 불일치(`P4`)**가 함께
   붉어 `adv-*` 네 행이 **단독 행이 아니게 됐다**. 그대로 표를 채웠으면 `V8`이 붉었을 것이다 —
   픽스처에 **에픽 행과 완료 기준 절을 함께 심는다.**

## 8. 미커밋 상태 (16개 — 전부 정상)

`docs/conventions.md` · `skills/impl/SKILL.md` · `skills/impl/template.md` ·
`tests/discover/{run.sh,run.ps1,README.md}` · `tests/site-includes/{run.sh,run.ps1,README.md}` ·
`docs/reports/M61-impl.md`(신규) · `docs/reports/M61-review.md`(신규) · `docs/milestones/M61.md`(신규) ·
`docs/epics/E2.md` · `docs/reports/retro.md` · `docs/project-context.md` · `.tide/NEXT-SESSION.md`

**되돌릴 것이 없다** — 커밋·태그가 없으므로 `reset`도 `tag -d`도 필요 없다.

## 9. 별건 — fleet 리포트(외부 실사용)가 도착했다

RIMS(자식 레포 6개) 세션이 `/tide:fleet` 계열에 대한 실측 리포트를 **두 번** 냈다 — 2026-09-03
(도입 평가)과 **2026-09-07(도입 이후 첫 전면 사이클 결과)**. **둘 다 아직 원장에 등재되지 않았다.**

- **1회차 결론**: ⑴ 권고 셋(훅 드리프트 노출 · 발견 근거 표기 · 역방향 조회)은 이미 회고 백로그에
  있는 항목의 멀티 레포 실례 ⑵ **계약 층은 진짜 새 축이라 회고가 `E3` 승격을 판정해야 한다.**
- **2회차가 가져온 것**(6/6 태그·미커밋 0으로 플릿 정렬, 크로스-레포 계약 T-12 양쪽 종결):
  1. **「검사가 자기가 볼 것만 본다」가 축을 바꿔 가며 네 번 재발**(grep 패턴 → 파일 → 대조 항목 →
     **검색 범위**). 넷째는 보고서를 쓰다가 발견됐고 **이미 릴리즈에 실려 있었다**(상위 폴더의 살아
     있는 문서를 「죽은 참조」로 판정). → **`E2` 축 ⑵에 하위 물음**: 완료 기준이 「무엇을 검사했다」를
     검사자가 정하게 두는가. 처방 후보는 *"전 히트를 열어 분류하고 대상 밖 히트는 사유를 적는다"*.
  2. **`measure-after-artifacts`가 빌드 캐시로 위조된다**(2회) — Gradle이 `test`를 UP-TO-DATE로
     건너뛰어 **3개월 전 XML**이 「마지막 측정」이 됐다. → **`E3` 축 ⑶(집행의 국소성)**: 이 규율은
     tide 저장소에 빌드 캐시가 없어서 성립해 왔다.
  3. **`fleet-verify`의 pass가 「빌드가 안 깨졌다」까지인데 산출물이 그 범위를 안 적는다**(훅 스모크
     자리가 빈 채 pass). → `E3`.
  4. **차단 7/7 · 성립 31건을 전부 반증 패스가 찾았고 메인 리뷰 자력은 0건.** → **`E2` 축 ⑵의
     경계 근거**(개입 지점 중 **지우면 안 되는 쪽**의 첫 외부 실측).
  5. **릴리즈 노트를 「신설 ↔ 해소」로 잇기.** → **`E1` 축 ⑵**(후향 전달이 사용자 표면으로 확장).
  6. **크로스-레포 계약 T-12가 양쪽에서 닫힌 완결 실례** → 1회차가 보류한 **`E3` 축 ⑷(레포를
     가로지르는 계약)** 신설의 근거. **새 에픽은 아직 이르다**(실례 하나).
- **경계**: 보고서 5절의 남은 항목(`alert_code` 표준화 · `inspection/#` 구독 · T-08 xlsx · Spring
  Boot 버전 · fanuc 이월군 · `.vscode/`)은 **RIMS 레포들의 백로그**이고 tide 항목이 아니다.
- **주의**: 처방 1을 규약에 옮길 때 **「이것은 기계가 무는 자리가 아니라 리뷰가 목록으로 대조하는
  층」**을 같은 자리에 적어야 한다 — 안 적으면 그 문장 자신이 `vacuous-pass` 판례에 걸린다.

**이 대화가 사라지면 근거가 사라진다** — 다음 세션이 `docs/project-context.md` 이월 원장에 **두
회차를 한 행으로** 등재하고, 에픽 갱신은 **M62 마일스톤과 같은 편집**에서 넣는다(Part M 양방향).

## 이월 — M62 라운드 2의 마지막 측정 한 축 (2026-09-09 세션에서 중단)

**상태**: `.tide/phase` = `impl`(일부러 되돌리지 않았다 — 라운드 2가 아직 끝나지 않았다는 표시다).
`docs/reports/M62-impl.md`는 **본문·판정 칸·되돌림 표가 전부 확정**돼 있고 수치도 채워져 있다.

**남은 일은 하나뿐**: `dash tests/discover/run.sh`를 **현재 트리에서 한 번** 돌려
`371 / 0`이 나오는지 확인한다.

- 확인되면 → `.tide/phase`를 `idle`로 되돌리고 `/tide:review M62`로 간다. 보고서는 **고치지 않는다**
  (수치가 이미 그 값이고, 그 확인 실행이 곧 마지막 측정이 된다).
- 값이 다르면 → 보고서 「테스트 결과」의 `dash` 칸만 실측치로 고치고, **네 축을 다시 한 번** 돌려
  마지막 측정을 맞춘다(라운드 1이 이 순서를 어겨 반환됐다).

**왜 한 축만 남았나** — `measure-after-artifacts`를 지키려고 측정을 **두 번** 돌렸다. ⑴ 수치를 얻는
측정 ⑵ 수치를 채운 뒤 「이 값이 마지막 측정」임을 확인하는 측정. ⑵에서 **PowerShell 5.1 · pwsh 7 ·
pwsh 하니스 6종 · sh 하니스 5종 · sh discover**가 전부 ⑴과 같은 값을 냈고, **`dash` discover만
371 중 333까지 돌다가 세션을 접었다**(이 환경에서 축 하나가 25~40분이고, 같은 기계에서 다른 세션의
Docker 빌드가 돌아 한때 85분까지 늘어졌다).

**이미 끝난 것**(다시 돌릴 필요 없다):
- 라운드 2의 코드·문서 수정 전부 — 창의 앵커를 **선언 줄**로 바꾸고(`decl_lines`와 같은 술어),
  적대 통제 `W23` 신설, `W22` 기준선 상수성 주석, sh 표지 순회의 글롭 차단, README Part W 재작성
  (`cases: 371`), 템플릿 발화 조건 확대, `fleet-verify` 인용 한 줄화, 보고서 수 정정.
- 되돌림 **ps1 축 23행 전건** + **sh 축 삭제 행 넷**(양 축 케이스 집합·총계 일치 → 완료 기준 10 충족).
- 차단 1의 처방 검증: `cite-key-ahead` 행이 **라운드 1에서 초록이던 교란**이고 지금은 `W9`를 붉힌다.
- `del-scope-limits` 행이 `W8`을 **처음으로** 붉혔다(권장 3).
